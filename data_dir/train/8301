#include <bits/stdc++.h>
using namespace std;

int a, b, c;

int main(){


    cin >> a >> b >> c;
    if (a < b) swap (a, b);
    while (c % a){
        if (c < b) break;
        c -= b;
    }
    if (c % a) cout << "No";
    else cout << "Yes";


}
