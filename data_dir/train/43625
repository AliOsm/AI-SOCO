#include<stdio.h>

int main(void)
{
    int n, i, j, x, cnt = 0;
    scanf("%d %d", &n, &x);

    for (i = 1; i <= n; i++) {
        if (x%i == 0 && x/i <= n) cnt++;
    }

    printf("%d\n", cnt);


    return 0;
}
