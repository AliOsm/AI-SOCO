#include <bits/stdc++.h>
#define f(i,x) for(int i=0;i<n;i++)
using namespace std;
int main()
{
    int n,k;
    cin>>n>>k;
    int A[n];
    f(i,n)
    cin>>A[i];
    sort(A,A+n);
    for(int i=n-1;i>=0;i--)
      if(k%A[i]==0)
      {cout<<k/A[i];break;}
}