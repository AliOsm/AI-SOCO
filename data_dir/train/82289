/*
ID: omar22
LANG: C++11
TASK:
*/
#include <bits/stdc++.h>

using namespace std;
const int N = 1e5 + 5;
int main() {
#ifndef ONLINE_JUDGE
	freopen("input.in", "r", stdin);
#endif
	string a, b;
	int n;
	cin >> a >> b >> n;
	while (n--) {
		cout << a << " " << b << endl;
		string x, y;
		cin >> x >> y;
		if (a == x) {
			a = y;
		} else {
			b = y;
		}
	}
	cout << a << " " << b << endl;
	
	return 0;
}
