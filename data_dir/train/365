#include <bits/stdc++.h>
using namespace std;

int N, V[100005];

int main() {
    scanf("%d", &N);

    for(int i=0;i<N;i++)
        scanf("%d", &V[i]);

    for(int i=0;i<N;i++)
        printf("%d%s", V[i] + V[i+1], (i < N - 1) ? " " : "\n");

    return 0;
}
