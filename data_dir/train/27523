#include <bits/stdc++.h>
using namespace std;

int N;
string S;
set <string> M;

int main() {
	cin >> N;

	while(N--) {
		cin >> S;
		cout << (M.count(S) ? "YES\n" : "NO\n");
		M.insert(S);
	}

	return 0;
}