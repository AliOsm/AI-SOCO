#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int l, r, x, y, k;
    cin >> l >> r >> x >> y >> k;

    for(int i = x; i <= y; ++i){
        if(i * (long long)k >= l && i * (long long)k <= r){
            cout << "YES";
            return 0;
        }
    }
    cout << "NO";
}
