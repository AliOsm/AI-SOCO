#include <bits/stdc++.h>

#define SZ(x) (((int)x.size()))

typedef long long ll;
typedef long double ld;

using namespace std;

int n, k;

int main()
{
	cin >> n >> k;
	cout << (2 * n + k - 1) / k + (5 * n + k - 1) / k + (8 * n + k - 1) / k;
	return 0;
}
