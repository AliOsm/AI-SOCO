#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

ll l, r, x, y, k;

int main() {
    cin >> l >> r >> x >> y >> k;

    for(ll b=x;b<=y;b++) {
        if(l <= b * k && b * k <= r) {
            cout << "YES" << endl;
            return 0;
        }
    }

    cout << "NO" << endl;

}