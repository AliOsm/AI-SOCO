#include <bits/stdc++.h>
using namespace std;
#define ll long long




int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
  //  freopen("input.txt","r",stdin);
    ll x, y, z, a, b, c;
    cin>>x>>y>>z>>a>>b>>c;

    a-=x;
    if (a<0)
    {
        cout<<"NO";
        return 0;
    }
    if (a+b < y)
    {
        cout<<"NO";
        return 0;
    }
    ll s = a+b+c;
    s-=y;
    if (s<z)
    {
        cout<<"NO";
        return 0;
    }
    cout<<"YES";





    return 0;
}
