#include <bits/stdc++.h>

using namespace std;

const int MaxN = 1e5 + 17;
const int INF = 1e9 + 17;
double d, L, v1, v2;
int main ()
{ 
    #ifdef Elibay 
        freopen (".in", "r", stdin); 
    #endif 
    cin >> d >> L >> v1 >> v2;
   	printf ("%.6f", (L - d) / (v1 + v2));
    return 0; 
}