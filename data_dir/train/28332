#include <bits/stdc++.h>
using namespace std;

int n,k,ans;

int main(){




    cin >> n >> k;


    for (int i = 1;i <= n;i++){

        int x;
        cin >> x;
        if (k % x == 0) ans = max(ans,x);
    }

    cout << k / ans;


}
