#include "bits/stdc++.h"

using namespace std;

int main() {
	int x1, y1, x2, y2, x, y;
	cin >> x1 >> y1 >> x2 >> y2 >> x >> y;
	x1 = abs(x1 - x2);
	y1 = abs(y1 - y2);
	if (x1 % x == 0 && y1 % y == 0 && ((x1/x) % 2 == (y1/y) % 2)) cout << "YES";
	else cout << "NO";
}