#include <bits/stdc++.h>
using namespace std;
int main() 
{
	int n ;
	cin >> n ;
	char x[n] ; 
	cin >> x ;
	int a=0 , b=0 ;
	for(int i = 0 ; i < n ; i++)
	{
		if(x[i]=='A')
			a++;
		else
			b++;
	}
	cout << ((a>b) ? "Anton":((a==b)? "Friendship":"Danik")) << endl ; 
 	return 0;
}