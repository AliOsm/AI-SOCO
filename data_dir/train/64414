#include <bits/stdc++.h>
using namespace std;

int main() {
  char s[15];
  scanf("%s", s);
  char mx = *max_element(s, s + strlen(s));
  for (int i = 0; i < strlen(s); ++i)
    if (s[i] == mx)
      printf("%c", s[i]);
  puts("");
  return 0;
}