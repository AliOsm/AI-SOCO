#include <bits/stdc++.h>

using namespace std;

int a[123];
int n;

main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
	}
	sort(a + 1, a + n + 1);
	if (a[1] == a[n / 2 + 1]) {
		puts("Bob");
	} else {
		puts("Alice");
	}
}	
