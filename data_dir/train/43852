#include<bits/stdc++.h>

#define SZ(x) ((int(x.size())))

typedef long long ll;

using namespace std;

int a, b, tmp;

int main()
{
    cin >> a >> b;
    cout << min(a, b) << " ";
    tmp = a;
    a -= min(a, b);
    b -= min(tmp, b);
    cout << a / 2 + b / 2;
	return 0;
}
