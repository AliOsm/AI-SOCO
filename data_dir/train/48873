#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll n , a[200002],ans,p;
int main() {
	scanf("%d",&n);
	for(int i = 1 ; i <= n  ;i++)scanf("%d",&p),a[p]=i;
	for(int i = 1 ; i < n ;i++)ans+=abs(a[i]-a[i+1]);
	cout << ans << endl;
	return 0;
}