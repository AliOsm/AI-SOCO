#include<bits/stdc++.h>
using namespace std;
main() {

    double a, b, c, d, p, q;
    scanf("%lf%lf%lf%lf", &a, &b, &c, &d);

    p = a / b;
    q = (1 - a / b) * (1 - c / d);

    cout << p / (1 - q);
    return 0;
}
