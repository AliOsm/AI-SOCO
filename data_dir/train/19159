#include <bits/stdc++.h>
using namespace std;

string a, b;
int n;

int main(){
  ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
  cin >> a >> b;
  cin >> n;
  cout << a << " " << b << '\n';
  while (n--) {
    string x, y;
    cin >> x >> y;
    if (x == a) a = y;
    else b = y;
    cout << a << " " << b << '\n';
  }
  return 0;
}
