#include <bits/stdc++.h>
#define fr(i, n) for (int i = 0; i < n; i++)
#define frab(i, a, b) for (int i = a; i < b; i++)
#define pb push_back
#define mp make_pair

using namespace std;

int main() {
    //freopen("a.in", "r", stdin);
    string a, b;
    cin >> a >> b;
    if (a == b)
        cout << a;
    else
        cout << 1;
}
