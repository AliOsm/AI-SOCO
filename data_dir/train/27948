#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n,k;
    cin >> n >> k;
    long long Diff = n/k;
    long long e = n - (Diff*k);
    if(e < k && Diff % 2 != 0)
    {
        cout << "YES";
        return 0;
    }
    cout << "NO";
    return 0;
}
