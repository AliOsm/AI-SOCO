#include <bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	int n, k, x;
	cin>>n>>k>>x;
	int ans=0;
	for(int i=n; i>0; i--){
		int f; cin>>f;
		ans+=(i<=k?x:f);
	}
	cout<<ans;
}