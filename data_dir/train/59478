#include <cstdio>
#include <cstring>
using namespace std;

char a[105],b[105],c[210],s[210];

int main(){
    gets(a); for(auto x:a) s[x]++;
    gets(b); for(auto x:b) s[x]++;
    gets(c); for(auto x:c) s[x]--;
    for(auto x:s) if(x) return puts("NO")&0;
    puts("YES");
}
