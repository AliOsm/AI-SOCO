#include<bits/stdc++.h>
using namespace std;

int n, m, x, y, z;

int main()
{
    cin >> n >> m >> x;
    int ans = 0;

    while(x){
        ans += 2 * (n + m) - 4;
        n -= 4;
        m -= 4;
        x--;
    }

    cout << ans << endl;

    return 0;
}
