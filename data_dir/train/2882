#include <bits/stdc++.h>
using namespace std;

int n;

int main() {
	scanf("%d", &n);

	int cnt = 0, temp = 0;

	for (int i = 1; i <= 10000; i++) {
		temp += i;
		cnt += temp;

		if (cnt > n) return 0 * printf("%d\n", i - 1);
	}
	return 0;
}	
