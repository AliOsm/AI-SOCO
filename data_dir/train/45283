#include <bits/stdc++.h>

using namespace std;

int main()
{
    int cnt = 1;
    int n;
    scanf("%d", &n);
    for(int i=2; i<n; i++){
        if(n%i == 0) cnt++;
    }
    cout << cnt;
}
