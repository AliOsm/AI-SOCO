#include <bits/stdc++.h>

using namespace std;

long long a[11];

int main () {
	for(int i=1;i<=6;i++){
		cin>>a[i];
	}
	cout<<((a[1]+a[2]+a[3])*(a[1]+a[2]+a[3]))-(a[1]*a[1])-(a[3]*a[3])-(a[5]*a[5]);
	return 0;
}