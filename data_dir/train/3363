#include <iostream>

int main()
{
    __int64 a, b, s = 0, q = 0;
    std::cin >> a >> b;
    for (int i = 1; i <= a / 2; ++i)
        s += a - (2 * i) + 1;
    for (int j = 1; j <= b / 2; ++j)
        q += b - (2 * j) + 1;
    std::cout << s * q;
}