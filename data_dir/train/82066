#include <bits/stdc++.h>
using namespace std;
long long n,k,a,ans;
main ()
{
    cin>>n>>k;
    
    for (int i=1; i<=n; i++)
    {
        cin>>a;
        
        if (k%a==0) ans=max(ans,a);
    }  
    
    cout<<k/ans<<endl;
}