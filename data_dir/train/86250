#include <bits/stdc++.h>

using namespace std;

#define ll long long;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int x,y,z,w;
    cin >> x >> y >> z >> w;

    int ans=((x^y)&(z|w))^((y&z)|(x^w));
    cout << ans << endl;
}