#include <bits/stdc++.h>
#define LL long long
using namespace std;

int main()
{
    int n;
    string s;
    cin>>n>>s;

    set<char> st;
    for (auto c: s)
        st.insert(c);

    cout << (st.size() != n || n==1 ? "Yes": "No")<<endl;
}
