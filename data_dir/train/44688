#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	scanf("%d", &n);
	set<int> st;
	int x;
	for (int i = 0; i < n; ++i) {
		scanf("%d", &x);
		if (x != 0)
			st.insert(x);
	}
	printf("%d",(int)st.size());
}
