#include<bits/stdc++.h>
using namespace std;

int n, x, ans;
map<int,int>mark;

int main()
{
    cin>>n;
    while(n--)
    {
        cin>>x;
        ++mark[x];
        ans=max(ans, mark[x]);
    }
    cout<<ans;
}