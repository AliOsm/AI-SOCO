#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, m, p, q;
    scanf("%d %d %d", &n, &m, &p);
    q = ((p-1)%(2*m))/2+1;
    printf("%d %d ", (p-1)/(2*m)+1, q);
    if(p%2)
        printf("L\n");
    else
        printf("R\n");
    return 0;
}
