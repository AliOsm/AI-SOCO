#include <bits/stdc++.h>
 
using namespace std ;
 
int main()
{
	ios_base::sync_with_stdio(0) ;
	cin.tie(0) ;
	int n ;
	cin>>n ;
	while(n--)
	{
		long long x , y ;
		cin>>x>>y ;
		if((x - y) % 2 == 0 || x-y > 2)
			cout<<"YES\n" ;
		else
			cout<<"NO\n" ;
	}
	return 0 ;
}