#include <bits/stdc++.h>

using namespace std;

int main()
{
    set<int> S;
    int n;
    scanf("%d", &n);
    for(int i=1; i<=n; i++){
        int x;
        scanf("%d", &x);
        if(x) S.insert(x);
    }
    cout << (int) S.size();
}
