#include <bits/stdc++.h>

using namespace std;

map<int, int>m;
int n, x, sol;

int main()
{
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> x;
    m[x]++;
    sol = max(sol, m[x]);
  }
  cout << sol;
  return 0;
}
