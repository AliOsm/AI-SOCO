#include <bits/stdc++.h>
using namespace std;

int n, m, r;

int main(){
    scanf("%d %d",&n,&m);
    for(int a=0; a*a<=n; a++){
        int b = n-a*a;
        if(b*b+a==m)
            r++;
    }
    printf("%d\n",r);
    return 0;
}