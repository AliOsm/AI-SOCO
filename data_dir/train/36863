#import<iostream>
using namespace std;
int i,n,o=1000,p=1000,q=-1000,r=-1000,x[5],y[5];
main()
{
	for(cin>>n;i++<n;)
	{
		cin>>x[i]>>y[i];
		o=min(o,x[i]);
		p=min(p,y[i]);
		q=max(q,x[i]);
		r=max(r,y[i]);
	}
	n=(q-o)*(r-p);
	cout<<n-!n;
}