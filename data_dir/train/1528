#include <stdio.h>
#include <vector>
#include <map>
using namespace std;
 
int main() {
	int n, s = 0;
	scanf("%d", &n);
	
	for (int i = 0; i < n; i++) {
		int x;
		scanf("%d", &x);
		s += x;
	}
 
	printf("%lf", (double)s / n);
}