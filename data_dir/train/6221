#include<bits/stdc++.h>
using namespace std;

int main()
{
  int n;
  cin>>n;
  vector<int>s(n); int sum=0;
  for(int i=0;i<n;i++)
    {cin>>s[i];sum+=s[i];}
  sort(s.begin(),s.end());
  
  for(int i=s[n-1];;i++)
    {
      if(i*n>2*sum)
	{	cout<<i<<endl;
      break;}
    }
  

  return 0;
}
  
