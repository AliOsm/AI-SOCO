#include <bits/stdc++.h>
using namespace std;
int main() {
	long n,d=0;
	string s;
	cin >> n >> s;
	long i=0;
	while (s[i]=='<' && i<n) {
		i++;
		d++;
	}
	i=n-1;
	while (s[i]=='>' && i>=0) {
		i--;
		d++;
	}
	cout << d;
}
