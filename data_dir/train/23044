#include <bits/stdc++.h>
using namespace std;

int main(){
	int n, ans;
	cin >> n;
	ans = 0;
	for (int i = 1; i <= n / 2; i++){
		if (n % i == 0) ans++;
	}
	cout << ans << '\n';
	return 0;
}