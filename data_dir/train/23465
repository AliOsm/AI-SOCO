#include <bits/stdc++.h>

using namespace std;

int n, x, y;
bool ans;

int main()
{
	for(scanf("%d", &n); n--;)
	{
		scanf("%*s %d %d", &x, &y);
		if(x >= 2400 && y > x)
			ans = true;
	}
	printf(ans ? "YES\n" : "NO\n" );
}