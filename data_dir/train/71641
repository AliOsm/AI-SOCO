#include <bits/stdc++.h>
  using namespace std;
  int main (void)
  {
	int n, k, m[3000];
	  while (cin >> n >> k)
	  {
		  int cnt=0;
		  for (int i = 0; i < n; i++)
		  {
			  cin >> m[i];
			  if (5 - m[i] >= k)
                  cnt++;
		  }
		  cout << cnt / 3 << endl;
	  }
  }