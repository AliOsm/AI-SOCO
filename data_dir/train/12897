#include <bits/stdc++.h>

using namespace std;

#define fr first
#define sc second
#define pb push_back
#define mp make_pair
#define y1 yy1

typedef long long ll;
typedef long double ld;

const int inf = 1e9 + 124234;
const ll infll = 1e18 + 12342345623;
const int maxn = 250123;
const ll mod = 998244353;
const ld eps = 1e-7;

void solve()
{
    int n;
    cin >> n;
    vector<int> a(n);
    for(int i = 0; i < n; i++)
        cin >> a[i];
    sort(a.begin(), a.end());
    cout << a[(n - (1 - (n&1))) / 2];
}

int main()
{
    srand(time(0));
    ios_base::sync_with_stdio(0);
//    freopen("input.txt", "r", stdin);
//    freopen("output.txt", "w", stdout);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
//    int t = 1000;
//    cin >> t;
//    int cnt = 1;
//    while(t--){
//        cout << t << endl;
//        cout << "Case #" << cnt << ": ";
//    while(t--)
//        solve();
//        for(int i = 0; i < 20; i++)
//            used[i] = 0;
//        cnt++;
//        cout << "\n";
//    }
    return 0;
}
