#include<bits/stdc++.h>
using namespace std;
int a,s,d,f,g,h,j,l=1000000,k,i,n,m;
main(){
    cin>>n>>m>>k;
    for(i=1;i<=n;i++){
        cin>>a;
        if(!a) continue;
        if(a<=k) l=min(l,abs(m-i)*10);
    }
    cout<<l;
}
