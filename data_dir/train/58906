#include <bits/stdc++.h>

using namespace std;

set<int> s;
int n;

int main(){

    cin >> n;
    for (int i=0;i<n;i++){
        int t; cin >> t;
        s.insert(t);
    }
    s.erase(0);
    cout << s.size() << endl;

}

