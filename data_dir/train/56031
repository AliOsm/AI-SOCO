#include <bits/stdc++.h>
using namespace std;

signed main(){
    long long a, b;
    cin >> a >> b;
    if( a < b ) cout << "-1" << endl;
    else cout << fixed << setprecision( 10 ) << ( a + b ) / ( 2. * ( ( a + b ) / ( 2 * b ) ) );
    return 0;
}
