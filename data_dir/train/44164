#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int x , y;
    scanf("%d%d" , &x , &y);
    if(y == 0 || x < y-1 || (y == 1 && x))
        printf("No");
    else if((x - (y-1)) % 2)
        printf("No");
    else
        printf("Yes");
    return 0;
}