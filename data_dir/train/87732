#include <stdio.h>

void main()
{
   char *s1,*s2;
   s1=new char[100];
   s2=new char[100];
   gets(s1);gets(s2);
   for(int i=0;s1[i];i++)
    if (s1[i]!=s2[i])printf("%d",1);   
    else printf("%d",0);
   delete []s1;
   delete []s2; 
}