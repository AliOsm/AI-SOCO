/*
0172A. Phone Code
*/

#include<bits/stdc++.h>
using namespace std;

int main(){
  int n;
  string s, p;
  cin >> n >> p;
  n--;
  while(n--){
    cin >> s;
    while(p != s.substr(0,p.length())){p = p.substr(0,p.length()-1);}
  }
  cout << p.length();
}