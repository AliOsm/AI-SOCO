#include <bits/stdc++.h>
#define SZ(x) (int)x.size()
typedef long long ll;
const int SZ=1e5+5;
using namespace std;

int main()
{
//  freopen("input.in", "r", stdin);

    ll n;
    cin>>n;
    
    if(n==1)
        cout<<-1;
    else
        cout<<n<<' '<<n+1<<' '<<n*(n+1);
}