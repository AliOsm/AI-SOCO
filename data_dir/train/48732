#include <bits/stdc++.h>
using namespace std;

const int MAXN = 105;
int ans;

int main()
{
    int n;

    scanf("%d", &n);
    for(int i = 0, x, y; i < n; ++i)
        scanf("%d%d", &x, &y), ans += (y - x > 1); 
    printf("%d\n", ans);

    return 0;
}
