#include<stdio.h>
int main(void)
{
	int i, n, t, x;
	scanf("%d%d", &n, &t);
	for(i=1;i<=n;i++)
	{
		scanf("%d", &x);
		t -= 86400-x;
		if(t<=0)
			break;
	}
	printf("%d\n", i);
	return 0;
}