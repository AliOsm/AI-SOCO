#include<stdio.h>
#include<algorithm>
using namespace std;
int main(void)
{
	int a, b, n, m;
	scanf("%d%d%d%d", &n, &m, &a, &b);
	printf("%d\n", min(n*a, min((n/m+1)*b, n/m*b+n%m*a)));
	return 0;
}