#include <bits/stdc++.h>
using namespace std;
string a, b;

int main() {
    cin >> a;
    cin >> b;
    string res;
    for (int i = 0; i < a.size(); i++) {
        if (a[i] < b[i]) {
            cout << -1 << endl;
            return 0;
        }
    }
    cout << b << endl;
}
