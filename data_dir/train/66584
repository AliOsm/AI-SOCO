#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a, b, c;
    cin >> a >> b >> c >> n;
    if (a + b >= c && a + b - c < n && a >= c && b >= c)
        cout << n - (a + b - c);
    else
        cout << -1;
    return 0;
}
