#include <bits/stdc++.h>
using namespace std;

typedef long long   ll;

int main(){
    int w, h, k;
    cin >> w >> h >> k;
    int ans = 0;
    for(int i = 0; i < k; i++){
        ans += 2*(w - 4 * i + h - 4 * i) - 4;
    }
    cout << ans;
    return 0;

}
