#include <bits/stdc++.h>
#define ll long long
#define f first
#define s second
#define pb push_back
using namespace std;
int const inf=1e9+7,N=3e5+6;
ll dp[N][2],a[N],b[N],p[N],n,k,md,m,x,x1,q,x2,st,y,r,d;
string s;
bool us[N],uss[N];
int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
        cin>>n;
    if (n==1) cout<<1; else 
    if (n==2) cout<<2; else 
    if (n==3) cout<<6; else
    if (n%2==1) cout<<n*(n-1)*(n-2); else
    if (n%3==1 || n%3==2) cout<<n*(n-1)*(n-3); else 
    cout<<(n-1)*(n-2)*(n-3);
}
