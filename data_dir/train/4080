#include<bits/stdc++.h>
#define fr first
#define se second
#define TIME ios_base::sync_with_stdio(0);
using namespace std;
char a[1001];
bool used[1001];
bool mon=0;
char z;
int ans;
int main()
{
    TIME
    int n,y,m,x;
    cin >> n >> m >> x >> y;
if ( ( ( n > y + 1 ) || ( y > 2 * n + 2 ) ) && ( ( m > x + 1 ) || ( x > 2 * m + 2 ) ) )
    cout<<"NO";
else
    cout<<"YES";
}


