#include<bits/stdc++.h>
using namespace std;
typedef long long int uli;
int main(){
   uli n,k;
   cin>>n>>k;
   k=min(k,n/2);
   uli ans=0;
   uli x=n;
   for(uli i=0;i<k;i++){
      ans+=x-1+x-2;
      x-=2;
   }
   printf("%lld\n",ans);
   return 0;
}

