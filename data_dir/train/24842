#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b ;
    int ans = 0;

    cin >> a >> b ;
    for(int i=1; i<=a; i++)
        if(b%i == 0 && b/i<=a)
            ans ++;

    cout << ans << endl;
}
