#include <bits/stdc++.h>
#define r_0 return 0;
#define ll long long
#define ld  long  double
#define pb push_back
#define fr first
#define se second
#define in freopen("input.txt","r",stdin);
#define out freopen("output.txt","w", stdout);
#define TIME ios_base::sync_with_stdio(NULL);
#define tcn cin.tie(NULL);
#define tct cout.tie(NULL);
using namespace std;
ll n;
ll v0,v1,l,cnt,ans,sum,m,a,b,c,d;
string s,s1,s2,s3;
ll used[1001];
ll use[1001];
vector<int>v;
char ch;
int main(){
     TIME tcn tct
   cin >> n;
   for( int i=0;i  <n;i ++ ){

    cin >> a >> b >> c >> d;
        if( a!=c )
        cout<<a<<" "<<c<<endl;
   else if( b!=d )
    cout<<b<<" "<<d<<endl;
    else
        cout<<a<<" "<<d<<endl;
   }
}
