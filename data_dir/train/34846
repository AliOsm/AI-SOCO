# include <bits/stdc++.h>
using namespace std;
# define fi cin
# define fo cout
# define x first
# define y second
int main(void)
{
    int n,p,x,s=0;
    fi>>n>>p;
    while (n --)
    {
        fi>>x;s += x;
    }
    s = abs(s);
    return fo << (s + p - 1) / p << '\n',0;
}
