#include <bits/stdc++.h>

using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;

    double a = 1.0*y*log(1.0*x), b = 1.0*x*log(1.0*y);

    if (abs(a-b) < 1e-6)
        cout << "=";
    else if (a < b)
        cout << "<";
    else
        cout << ">";
}