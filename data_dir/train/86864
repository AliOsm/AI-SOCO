#include <bits/stdc++.h>
using namespace std;

typedef long double ld;

const ld pi = 3.1415926535897932384626433832795;
int n;
ld R, tmp;

int main() {
	cin >> n >> R;
	tmp = (ld)sin((ld)pi / (ld)n);
	cout << fixed << setprecision(10) << (((ld)R * (ld)tmp) / ((ld)1 - (ld)tmp));
}
