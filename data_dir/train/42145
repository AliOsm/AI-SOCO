#include <iostream>
using namespace std;

int main() {
	long long n, x, y;
	cin >> n >> x >> y;
	long long white = max(x - 1, y - 1);
	long long black = max(n - x, n - y);
	if (white <= black) cout << "White";
	else cout << "Black";
	return 0;
}