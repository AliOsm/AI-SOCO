//Bismillah
#include <bits/stdc++.h>
using namespace std; 
#define FOR(i,begin,end) for (int i=begin;i<=end;i++)
#define rep(i,t) for (int i=0;i<t;i++)
#define pb push_back
#define mp make_pair
#define endl '\n'
typedef long long ll;

int main()
{
    ios_base::sync_with_stdio(false);
    double l,a,b;
    cin >> l >> a >> b;
    //hameye barkhord ha too 1 noghte (hamoon noghteye barkhorde avval)
    cout << fixed << setprecision(6) << l*(a/(a+b)) << endl;
    return 0;
}