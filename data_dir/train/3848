#include <bits/stdc++.h>
using namespace std;

typedef long long i64;

int main()
{
    i64 a, b, c, d;
    cin >> a >> b >> c >> d;

    i64 best = a % b * d;
    best = min(best, (b - a % b) * c);

    cout << best << '\n';
    return 0;
}
