#include<bits/stdc++.h>
using namespace std;

const int N = 100005;

typedef long long ll;

int main(){
    double d,l,a,b,ans; cin >> d >> l >> a >> b;
    ans = (l - d)/(a + b);
    cout << fixed << setprecision(15);
    cout << ans << '\n';
    return 0;
}
