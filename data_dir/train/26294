#include "bits/stdc++.h"
using namespace std;
const int N=1e5+20;
int n,u,v,f[N];
int main()
{
	scanf("%d",&n);
	while(--n) scanf("%d%d",&u,&v),f[u]++,f[v]++;
 
	for(int i=1;i<N;i++)
		if(f[i]==2)
			return 0 * printf("NO");
	printf("YES");
}