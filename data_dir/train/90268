#include <bits/stdc++.h>
using namespace std;

const int N = 1000000;

int main() {
	double a, b, n;
	cin >> a >> b >> n;

	for (int i = -N; i <= N; ++i)
		if (pow(i, n) * a == b)
			return cout << i, 0;

	cout << "No solution";
	return 0;
}