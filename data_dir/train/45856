#include <bits/stdc++.h>

using namespace std;

int n, sum;

int main() {
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		int tmp;
		scanf("%d", &tmp);
		sum += tmp;
	}
	for (int i = 0; i < n; i++) {
		int tmp;
		scanf("%d", &tmp);
		sum -= tmp;
	}
	puts(sum >= 0 ? "Yes" : "No");
}
