#include <iostream>
#include <map>
using namespace std;

int main() {
	int n;
	cin >> n;
	while(n --> 0) {
		int L, v, l, r;
		cin >> L >> v >> l >> r;
		cout << L / v - (r / v - l / v + (l % v == 0)) << endl;
	}
	return 0;
}