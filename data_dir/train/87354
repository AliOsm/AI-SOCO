/*input

*/
#include <bits/stdc++.h>
using namespace std;
const long long md=1e9+7;
long long a[1000005],n;

int main(){
	#ifdef ONLINE_JUDGE
	ios_base::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	#endif
	long long q,m,k,h,ans=0,sum=0,p,x,y;
	cin>>n;
	if(n%10==0) return cout<<n,0;
	else if(n%10<=5) return cout<<n-n%10,0;
	else return cout<<n+(10-n%10),0;
}