#include <bits/stdc++.h>
using namespace std;

int N, MX, V[100005];

int main() {
    scanf("%d", &N);

    for(int i=0;i<N;i++) {
        scanf("%d", &V[i]);
        MX = max(MX, V[i]);
    }

    printf("%d\n", MX);

    return 0;
}
