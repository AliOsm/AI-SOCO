#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll n, m, k, l;

int main() {
	cin >> n >> m >> k >> l;
	l += k;
	ll tmp = (l + m - 1) / m;
	if (tmp >= 1 && tmp * m <= n)
		cout << tmp;
	else
		cout << -1;
}

