#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
	ll test;
	cin >> test;
	while(test--) {
		ll n, s, t;
		cin >> n >> s >> t;
		cout << n - min(s, t) + 1 << "\n";
	}
	return 0;
}