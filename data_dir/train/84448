#include <iostream>
#include <map>
#include <vector>
#include <set>
#include <algorithm>
#include <cmath>
#include <assert.h>
#include <queue>
#include <iomanip>
using namespace std;

#define all(a) (a).begin(), (a).end()

template <typename T, typename T2>
void update(T& a, T2 b) {
    a = min(a, b);
}

int main() {
    // freopen("in.txt", "rt", stdin);
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    long long x, y, z;
    cin >> x >> y >> z;

    long long a, b, c;
    cin >> a >> b >> c;

    cout << (x <= a && y <= a - x + b && z <= a + b + c - x - y ? "YES" : "NO") << "\n";
}
