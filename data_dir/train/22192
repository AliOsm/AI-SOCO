#include <bits/stdc++.h>
#define int __int64
using namespace std;
main() {
    int a, b, c;
    cin >> a >> b >> c;
int    mx = max(a, max(b, c));
    cout << (mx > a ? mx - a - 1 : 0) + (mx > b ? mx - b - 1 : 0) + (mx > c ? mx - c - 1 : 0);
}
