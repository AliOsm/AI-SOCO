#include <bits/stdc++.h>
using namespace std;
int main() {
    long long a, b, x, y, z, blue, yellow;
    cin >> a >> b >> x >> y >> z;
    yellow = 2 * x + y;
    blue = y + 3 * z;
    cout << max(0ll, yellow - a) + max(0ll, blue - b) << "\n";
    return 0;
}