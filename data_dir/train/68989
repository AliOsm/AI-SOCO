#include <bits/stdc++.h>
using namespace std;
string s,t;
int main () {
	ios::sync_with_stdio(false);
	cin>>s>>t;
	if (s==t) printf("-1\n");
	else printf("%d\n", max(s.size(), t.size()));
	return 0;
}