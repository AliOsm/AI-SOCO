#include <bits/stdc++.h>

using namespace std;


int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int x, y, z;
  cin >> x >> y >> z;
  int a, b, c;
  cin >> a >> b >> c;
  if (a >= x && (a - x + b) >= y && (a - x + b - y + c) >= z) {
    cout << "YES\n";
  } else {
    cout << "NO\n";
  }
  return 0;
}