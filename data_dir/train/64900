#include <bits/stdc++.h>

using namespace std;

int main()
{
	int x = 0, n;
	string s;
	cin >> n;
	
	for (int i = 0; i < n; i++)
	{
		cin >> s;
		
		if (s == "X++" || s == "++X")
			x++;
		else
			x--;
	}
	
	cout << x;
}