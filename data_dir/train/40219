#include<bits/stdc++.h>
using namespace std;
main() {

    int a, b, s;
    scanf("%d%d%d", &a, &b, &s);

    a = abs(a);
    b = abs(b);

    if (a + b > s) {
        printf("No");
        return 0;
    }

    printf((s - a - b) & 1 ? "No" : "Yes");

    return 0;
}
