#include <iostream>

int main()
{
    long long n, r = 0, val = 0, a;
    std::cin >> n;
    for (int i = 0; i < n; i++) {
        std::cin >> a;
        a ? r++ : val += r;
    }
    std::cout << val;
}
