#include <bits/stdc++.h>
using namespace std;

int N;
long long S, A, B, C;

int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	cin>>N;
	
	for (int i=0;i<N;i++) {
		cin>>S>>A>>B>>C;
		cout<<S/C+B*(S/(A*C))<<'\n';
	}
	
	return 0;
}
