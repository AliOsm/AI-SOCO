#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    //ifstream cin("in.in");
    ll sk, kop = 0, sum = 0;
    cin >> sk;
    for(ll i = 1;kop+i <= sk;i++) kop+=i,sum++, sk-=kop;
    cout << sum;
    //cin.close();
    return 0;
}
