#include <bits/stdc++.h>

using namespace std;

int main() {
	int N;
	cin >> N;
	string digits;
	for (int i = 1; i <= N; i++)
		digits += to_string(i);

	cout << digits[N - 1];

	return 0;
}

