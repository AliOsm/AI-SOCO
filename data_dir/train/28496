#include <bits/stdc++.h>

using namespace std;

const int N = 100005;
char s[N];

int main() {
#ifndef ONLINE_JUDGE
  freopen("input.in", "r", stdin);
#endif
  int n, a, b;
  scanf("%d %d %d %s", &n, &a, &b, s);
  printf("%d\n", s[a - 1] == s[b - 1] ? 0 : 1);
}
