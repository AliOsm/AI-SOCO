#include <bits/stdc++.h>
using namespace std;

int x1, y1, x2, y2;

int main(){
    scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
    printf("%d\n", ((abs(x1 - x2) + 1 + (x1 == x2)) + (abs(y1 - y2) + 1 + (y1 == y2))) * 2 );
    return 0;
}
