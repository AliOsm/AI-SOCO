#include<bits/stdc++.h>
#define r_0 return 0;
#define fr first
#define ll long long
#define se second
#define pb push_back
#define real_madrid ios_base::sync_with_stdio(NULL);
using namespace std;
ll n,a,cnt,ans,sum=1,b,c,d,mn=10e9,mx;
int main()
{
    real_madrid
    cin >> n;
    ll arr[n];
    for( ll  i = 0;i < n;i ++ ){
        cin >> arr[i];
      mx+=arr[i];
    }
    for( int i=0;i<n;i++ ){
        cnt+=arr[i];
        if( cnt*2>=mx ){
            cout<<i+1;
            r_0
        }
    }
 }

