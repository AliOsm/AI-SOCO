#include <bits/stdc++.h>
using namespace std;

int main()
{
	long long a, b, c;
	cin >> a >> b >> c;
	long long ans = 2 * c;
	long long m = min(a, b);
	a -= m;
	b -= m;
	ans += 2 * m;
	if(a or b) ans++;
	cout << ans << '\n';

	return 0;
}