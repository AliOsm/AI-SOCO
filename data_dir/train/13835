#include <bits/stdc++.h>
using namespace std;

// why am I so weak

long long n, k;

int main() {
	cin >> n >> k;

	if ((n + 1) / 2 >= k) {
		printf("%lld\n", (k - 1) * 2 + 1);
	} else {
		k -= (n + 1) / 2;
		printf("%lld\n", k * 2);
	}

	return 0;
}

