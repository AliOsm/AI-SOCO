#include <bits/stdc++.h>
using namespace std;
int n, k;

int main() {
    cin >> n >> k;
    if (n == k || k == 0) {
        cout << "0 0" << endl;
        return 0;
    }
    cout << "1 ";
    cout << min(n - k, k * 2);
}
