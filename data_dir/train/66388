#include <bits/stdc++.h>
using namespace std;
long long n, k, ans, res;
int32_t main(){
    ios_base :: sync_with_stdio(false);
    cin >> n >> k;
    ans = k + 3 + 3 * (n - 2) + 2;
    res = n - k + 4 + 3 * (n - 2) + 2;
    cout << min(ans, res);
}