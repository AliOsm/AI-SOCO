#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include <string.h>
#include <iostream>
#include <algorithm>
#include <string>
using namespace std;
 
 
 
int main() {
	string a, b, c;
	cin >> a >> b;
	cout << a << " " << b << "\n";
 
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> c;
		if (a == c)
			a = "";
		else
			b = "";
 
		cin >> c;
		if (a == "")
			a = c;
		else
			b = c;
 
		cout << a << " " << b << "\n";
	}
}