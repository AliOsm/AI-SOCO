//          be name khoda

#include <iostream>
#include <cmath>

using namespace std;

typedef long long ll;
const int maxN = 4 * 1000 * 10000;

int main()
{
    ll n;
    cin >> n;
    ll ans = 4 * (ll)(n * sqrt(2.0));
    if(!n)
        ans = 1;
    cout << ans << endl;
}