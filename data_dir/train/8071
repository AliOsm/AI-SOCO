#include <bits/stdc++.h>
using namespace std;

int main(){
	int T;
	cin >> T;
	string s;
	int n;
	while (T--){
		cin >> s;
		n = s.size();
		sort(s.begin(), s.end());
		if (s[0] == s[n - 1]) cout << -1 << '\n';
		else cout << s << '\n';
	}
	return 0;
}