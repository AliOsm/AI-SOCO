#include<bits/stdc++.h>

using namespace std;


int R, C, k;
vector<vector<int>> board;
vector<vector<int>> visited;
int main()
{

	int k; cin >> k;
	while(k--) {
		int x; cin >> x;
		if(x < 4) {
			cout << 4 - x << endl;
		}
		else{
			cout << x % 2 << endl;
		}
	}
}


