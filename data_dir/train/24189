#include <bits/stdc++.h>

using namespace std;

int main()
{
	int n;
	cin >> n;
	long long sum=0,mx=0;
	while(n--)
	{
		int x; cin >> x;
		sum+=x; mx=max(mx,1LL*x);
	}
	if(sum%2 || mx*2>sum)
		cout << "NO" << endl;
	else
		cout << "YES" << endl;
}