#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	string s, res;
	cin >> s >> s;
	for (ll i = 0, j = 1; i < s.size(); i+=j, j++) {
		res += s[i];
	}
	cout << res << endl;
}
