#include <bits/stdc++.h>
using namespace std;

void solve() {
  int n, f[5];
  scanf("%d %d %d", &f[0], &f[1], &n);
  f[2] = f[1] ^ f[0];
  n %= 3;
  printf("%d\n", f[n]);
}

int main() {
  int qq;
  scanf("%d", &qq);
  while (qq--)
    solve();
  return 0;
}