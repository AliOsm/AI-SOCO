#include <bits/stdc++.h>
using namespace std;
const int N = 100 * 1000 + 5, AL = 26 + 2;
int mark[N];
int ans;
set<int> s;
int main() {
	int n, x, y;
	cin >> n >> x >> y;
	int nd = (n * y + 99) / 100;
	cout << max(0, nd - x);
	return 0;
}
