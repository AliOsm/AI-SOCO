#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ll n;

    cin >> n;

    ll ans = (n / 4);

    if(n % 4 == 0) ans--;

    if(n % 2 == 0) cout << ans << endl;
    else cout << 0 << endl;

    return 0;
}
