#include<iostream>

using namespace std;

int main(){
	
	int s1,s2,s3,s4;
	cin>>s1>>s2>>s3>>s4;
	
	int change=0;
	if(s2==s1)change++;
	if(s3==s2 || s3==s1) change++;
	if(s4==s3 || s4==s2 || s4==s1) change++;
	cout<<change;
	return 0;
}