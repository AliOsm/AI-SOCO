#include <bits/stdc++.h>
using namespace std;
#define LL long long
int main()
{
    LL a,b,tot=0; cin>>a>>b;
	do
    {
        if (b>a)swap(a,b);
        tot+=a/b;
        a=a%b;
    }
    while (a%b!=0);
    cout<<tot;
return 0;
}
