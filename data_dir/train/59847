#include<stdio.h>

int main()
{
    long long n, x, y, max = -1;
    scanf("%lld", &n);

    while (n--)
    {
        scanf("%lld %lld", &x, &y);
        if (x + y > max)
            max = x + y;
    }

    printf("%lld\n", max);
    return 0;
}
