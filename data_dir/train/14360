#include<bits/stdc++.h>
using namespace std;
long long N, K, i;
int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin>>N>>K;
	for (i=2;i<=K;i++) {
		if (N%i<i-1) {
			cout<<"No\n";
			return 0;
		}
	}
	cout<<"Yes\n";
}
