#include <bits/stdc++.h>

using namespace std;

int main()
{
  int a,b;
  cin >> a >> b;
  int av = ceil((a+b)/2.0);
  int ca,cb;
  ca = abs(av-a);
  cb = abs(av-b);
  int aa,bb;
  aa = (ca*(ca+1))/2;
  bb = (cb*(cb+1))/2;
  cout << aa+bb << endl;
}