#include<bits/stdc++.h>
using namespace std;
typedef  long long  ll;
const long long int mod=1e9 +7;
ll m,k,n,a[200005],dp[200005],edge[505][505];
set<ll>st,st1,st2;
string s1="",s;
vector<ll>v[2000005],vnode[200005],v1,v2,v3;
vector< pair<ll,ll> >vv;
vector<pair< pair <ll,ll>,ll > > vvv;
priority_queue <ll> pq;
map<ll,ll>mp;




int main(){
	ios::sync_with_stdio(false);
    cin.tie(NULL);
	ll i=0,j=0,z=0,res=0,q,ans=0,mx=0,nb=0,nbmax=0,nbs=0,sm=0,nb2=0,mm=5e8,idx=0,ok=0,l,r,negatif=0,positif=0;
	ll x,y;
	set<ll>::iterator it,it1,it2;
	char c;
   //freopen("a.txt","r",stdin);  
    cin>>n>>m;
    for(i=1;i<=n;i++)cin>>a[i];
    double ress=0.0000000;
    for(i=1;i<=m;i++){
    	cin>>x>>y>>z;
    	ress=max(ress,1.0*(a[x]+a[y])/(double)z);
	}
	if(n==1)return cout<<"0.0000000000",0;
	
	cout<<fixed<<setprecision(12)<<ress;
 }