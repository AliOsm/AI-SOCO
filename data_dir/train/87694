#include <bits/stdc++.h>

using namespace std;

int main(){
    int x, y, z, a, b, c;

    scanf("%d %d %d", &x, &y, &z);
    scanf("%d %d %d", &a, &b, &c);

    if(a < x) return printf("NO\n"), 0;
    else a -= x;

    if(a + b < y) return printf("NO\n"), 0;
    else a -= y;

    if(a + b + c < z) return printf("NO\n"), 0;
    printf("YES\n");
}
