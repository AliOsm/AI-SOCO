#include<bits/stdc++.h>
using namespace std;
int ans;
main()
{
	int i,j,n;
	cin>>n;
	for(i=1;i<=n;++i)
		for(j=i;j<=n;++j)
			{
				int c=i*i+j*j;
				int r=sqrt(c);
				if(r*r==c&&r<=n)ans++;
			}
	cout<<ans;
}