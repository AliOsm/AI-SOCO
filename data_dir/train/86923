// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    int n, a, b;
    cin >> n >> a >> b;
    cout << (n > min(a, b) ? "No" : "Yes");
    return 0;
}
