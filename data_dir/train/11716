#include<iostream>
#define ll long long
using namespace std;

int main(){
	ll s, v1, v2, t1, t2;
	cin>>s>>v1>>v2>>t1>>t2;
	if (v1 * s + 2 * t1 > v2 * s + 2 * t2) cout<<"Second";
	else if (v1 * s + 2 * t1 < v2 * s + 2 * t2) cout<<"First";
	else cout<<"Friendship";
}
