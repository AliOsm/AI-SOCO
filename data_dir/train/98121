#include<iostream>
#include <algorithm>
#include <cmath>
#include <fstream>
#include <cstdio>

using namespace std;

int a[2111][2],b[11111];

int main() {
 // freopen("input.txt","r",stdin);
 // freopen("output.txt","w",stdout);
 int j,i,n,m;
 string st;   
 cin>>n>>m;
    
 if (n*(n-1)/2>m) {for (i=1;i<=n;i++) 
             printf("0 %d\n",i);
          
          }
       else cout<<"no solution"   ;
// system("pause");  
}
