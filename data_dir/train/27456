#include <bits/stdc++.h>

using namespace std;

#define ll long long

main() {
	ll n, m, k, l;
	cin >> n >> m >> k >> l;
	ll asd = (k + l + m - 1) / m;
	if (asd * m > n) {
		puts("-1");
		return 0;
	}
	cout << asd;
}
