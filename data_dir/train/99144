#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  scanf("%d", &n);
  int sum = 0;
  for (int i = 0; i < n; ++i) {
    int foo;
    scanf("%d", &foo);
    sum += foo - 1;
    sum %= 2;
    puts(sum == 1 ? "1" : "2");
  }
  return 0;
}