#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int n, t;
	cin >> n >> t;
	long double p = pow(1.000000011, t);
	long double ans = n * p;
	cout.precision(15);
	cout << fixed << ans << "\n";
	return 0;
}