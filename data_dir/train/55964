#include<bits/stdc++.h>
using namespace std;
#define ll long double
#define MAX_INT 1000000000
int main()
{
    int n;
    cin>>n;
    int x,y,i,sm=0;
    for(i=0;i<n;i++){
        cin>>x>>y;
        sm=max(x+y,sm);
    }
    cout<<sm;
    return 0;
}