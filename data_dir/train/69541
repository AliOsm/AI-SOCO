#include <bits/stdc++.h>
using namespace std;

char arr[1002];

int main(){

    scanf("%s",arr);
    printf("%s",arr);
    for(int i=strlen(arr)-1; i>=0; i--)
        printf("%c",arr[i]);
    printf("\n");

    return 0;
}
