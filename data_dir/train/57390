#include <bits/stdc++.h>

using namespace std;

#define MOD 1000000007
#define N 100005

int a[N];

int main(){
	ios::sync_with_stdio(false);
	//freopen("input.txt", "r", stdin);
	int i, n; 
	cin>>n;
	for(i=0; i<n; i++){
		cin>>a[i];
	}
	sort(a, a+n);
	cout<<(a[(n-1)/2]);
	return 0;
}