#include <bits/stdc++.h>

#define ll long long

using namespace std;

int n;

main() {
  scanf("%d", &n);
  ll sum = 0;
  for (int i = 3; i <= n; i++) {
    sum += i * 1LL * (i - 1);
  }
  cout << sum;
}
