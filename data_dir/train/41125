#include <bits/stdc++.h>
using namespace std;

long long s, s1, s2, a, b, c;

int main(){



    cin >> s >> s1 >> s2;
    a = sqrt(s1 * s2 / s),
    b = sqrt(s2 * s / s1),
    c = sqrt(s * s1 / s2);
    cout << (a + b + c) * 4;

}
