#include<bits/stdc++.h>
using namespace std;

const int N = 1e5;
int n,a[N];
int main () {
  ios::sync_with_stdio(false);
  cin>>n;
  for(int i=0;i<n;i++)cin>>a[i];
  sort(a,a+n);
  cout<<(a[n-1]-a[0]+1)-n<<endl;
}