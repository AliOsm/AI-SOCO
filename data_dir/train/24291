#include <bits/stdc++.h>

using namespace std;

int main() {
  int a, b, c, n;
  cin >> a >> b >> c >> n;
  if (a < c || b < c) {
    puts("-1");
    return 0;
  }
  int all = a + b - c;
  int ans = n - all;
  if (ans <= 0) ans = -1;
  cout << ans << endl;
  return 0;
}
