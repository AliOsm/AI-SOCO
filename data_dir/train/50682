/* In the name of Allah */
#include<bits/stdc++.h>
using namespace std;

const int N = 100 + 5;
int n, x, y, a[N];

int main() {
	cin >> n >> x >> y;
	while (x--) {
		int p;
		cin >> p;
		a[p] = 1;
	}
	for (int i = 1; i <= n; i++)
		cout << 2 - a[i] << ' ';
}
