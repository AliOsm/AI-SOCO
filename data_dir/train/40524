#include <iostream>

using namespace std;

int main()
{
    int x,y,z,a,b,c;
    cin>>x>>y>>z>>a>>b>>c;
    if(x>a)cout<<"NO";
    else{
        a -= x;
        if(y > a+b) cout<<"NO";
        else{
            if(z+y>a+b+c)cout<<"NO";
            else cout<<"YES";
        }
    }
    return 0;
}
