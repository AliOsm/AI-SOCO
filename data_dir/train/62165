#include <bits/stdc++.h>

using namespace std;

int main () {
	ios_base::sync_with_stdio (0);
	cin.tie (0), cout.tie (0);
	int n;
	cin >> n;
	int ans = 0;
	for (int i = 1;i <= n;i ++) {
		int x = n - i;
		if (x >= i && x % i == 0) ans ++;
	}
	cout << ans;
	return 0;
}