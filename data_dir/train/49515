#include <bits/stdc++.h>

using namespace std;

#define done return 0
#define f first
#define s second
#define pb push_back
#define mp make_pair
#define boost ios_base::sync_with_stdio(false)
#define eb emplace_back
#define OUT(x) {cout << x; return (0); }
#define rep( a, b) for(int i=(a); i<=(b); ++i)
#define reps( a, b,x) for(int i=(a); i<=(b);i+=(x))
typedef unsigned int ui;
typedef long long ll;
typedef unsigned long long llu;
typedef pair <int, int> PII;
typedef pair <ll, ll> PLL;
typedef pair <PLL, PLL> PP;
typedef pair <PII, int> PPI;
typedef vector <int> vi;
const ll mod = 1e9+7;
const int inf = 1e9+9;
const ll MOD = 1e9+696969;
const ll INF = 1e18;
inline Int()
{
    int n;
    scanf("%d",&n);
    return n;
}
inline I64()
{
    ll n;
    scanf("%I64d",&n);
    return n;
}
inline I64u()
{
    llu n;
    scanf("%I64d",&n);
    return n;
}
int t;
#define maxn 500100
#define take Int()
#define takes I64()

char s[maxn];
int n;

int main()
{
    int a=take,b=take,c=take,d=take,e=take,f=take;

    int p=max(c,e),q=d+f;
    int r=max(c,f),s=d+e;
    int t=max(d,e),u=c+f;
    int v=max(d,f),w=c+e;
    int x=c+e,y=max(d,f);
    int z=c+f,zz=max(d,e);
    int xx=d+e,xxx=max(c,f);
    int yy=d+f,yyy=max(c,e);
    if((p<=a && q<=b)|| (r<=a && s<=b) || (t<=a && u<=b) ||(v<=a && w<=b))
    {
        OUT("YES\n");
    }
    else if((x<=a && y<=b)|| (z<=a && zz<=b)||(xx<=a && xxx<=b)||(yy<=a && yyy<=b))
    {
        OUT("YES\n");
    }
    else OUT("NO\n");
    done;
}


