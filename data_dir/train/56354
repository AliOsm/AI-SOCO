#include <bits/stdc++.h>

using namespace std;

#define pb push_back
#define mp make_pair
#define ll long long
#define F first
#define S second
#define FOR(i, j, k) for (int i = j; i < (int)k; i++)

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int x;
    cin >> x;
    FOR(i, 0, 3)
    {
        int y;
        cin >> y;
        x = min(x, y);
    }
    int a, b;
    cin >> a >> b;
    int c = 0;
    for (int i = a; i <= b && i < x; i++)
        c++;

    cout << c << endl;
}