#include <bits/stdc++.h>
using namespace std;
const int N = 100000;
int a[N], mark[N];
int main() {
	double x, y, v, u;
	cin >> x >> y >> v >> u;
	double ans = abs(x - y) / abs(v + u);
	cout << fixed << setprecision(10) << ans;
	return 0;
}
