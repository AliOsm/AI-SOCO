#include <iostream>
#include <cstdio>
#include <cstdlib>
using namespace std;
int x;
int main()
{
	scanf("%d",&x);
	for (int i=1;i<=x;i++)
	 for (int j=i;j<=x;j++)
	  if (j%i==0 && (j/i<x) && (j*i>x)) {printf("%d %d\n",j,i); return 0;}
 	printf("-1\n");
 return 0;
}