#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    int a,b;
    cin>>a>>b;
    int d = abs(a-b);

    int ats = (d/2)*(d/2+1);

    if(d&1)ats+=d/2+1;

    cout<<ats<<"\n";
}
