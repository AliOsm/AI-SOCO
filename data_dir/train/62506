#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

#define IO ios_base::sync_with_stdio(0); cin.tie(); cout.tie()
#define all(x) x.begin(), x.end()



int main() {
	IO;

	int n, x, ans = 0;
	cin >>  n >> x;
	for(int i = 1; i <= n; i++){
		if(x % i == 0 && x / i <= n)
			ans++;
	}
	cout << ans;

	return 0;
}
