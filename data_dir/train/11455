#include <bits/stdc++.h>

using namespace std;

int main()
{
  int n;
  int a[111];
  cin >> n;
  for(int i = 0; i < n; ++i) cin >> a[i];
  int sum1 = 0, sum2 = 0;
  for(int i = 0; i < n; ++i){
    if(a[i] < 0){
      sum2 += a[i];
    } 
    else sum1 += a[i];
  } 
  cout << sum1-sum2 << endl;
}