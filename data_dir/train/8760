#include <bits/stdc++.h>
using namespace std;
int main() {
	int n, a, b;
	scanf("%d%d%d", &n, &a, &b);
	a += b;
	while(a < 0) a += n;
	a %= n;
	if(!a) a = n;
	printf("%d\n", a);
	return 0;
}