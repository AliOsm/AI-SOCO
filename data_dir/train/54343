#include <bits/stdc++.h>
using namespace std;

int n, a, r, ans;

int main() {
	cin >> n;
	for (int i = 1; i <= n; ++i) {
		cin >> a;
		ans += (i > r);
		r = max(r, a);
	}
	cout << ans;
	return 0;
}