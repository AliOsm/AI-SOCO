#include<bits/stdc++.h>
using namespace std;




int main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int k, n, s, p;
    cin >> k >> n >> s >> p;
    int x = (n + s - 1) / s;
    int y = x * k;
    cout << (y + p - 1) / p;
}
