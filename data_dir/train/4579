#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k;
    cin>>n>>k;
    int a=k-n*2;
    if(a==0) cout<<n;
    else
    {
        cout<<max(0,n-a);
    }
    return 0;
}