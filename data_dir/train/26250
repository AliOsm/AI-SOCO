#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	long long a, b, c, d;
	cin >> a >> b >> c >> d;

	long long ans = 2*(b+1) + a + 2*(d+1) + c + (a-c);
	cout << ans << '\n';

	return 0;
}