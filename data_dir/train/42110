#include <bits/stdc++.h>

int main() {
	double S, a, b, c, x, y, z;
	scanf("%lf%lf%lf%lf", &S, &a, &b, &c);
	if(a + b + c == 0) {
		x = y = 0;
		z = S;
	} else {
		double k = S / (a + b + c);
		x = a * k;
		y = b * k;
		z = c * k;
	}
	printf("%.20lf %.20lf %.20lf\n", x, y, z);
}
