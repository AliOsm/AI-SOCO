#include<bits/stdc++.h>
#define In_the_name_of_allah_the_merciful ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define Alhamdulillah return 0;
#define pb push_back
#define lb lower_bound
#define ub upper_bound
#define ins insert
#define fi first
#define se second
#define pi pair<ll,ll>
#define all(v) v.begin(),v.end()
#define rall(v) v.rbegin(),v.rend()
#define sz(v) ((ll)((v).size()))
#define reset(v,val) memset(v,val,sizeof(v));
#define forn(i, x, n) for ( i = x; i<=(ll)(n); i++)
#define iforn(i, x, n) for( i = x; i<(ll)(n); i++)
#define rep(i, x, n) for ( i =x; i>=(ll)(n); i--)
#define irep(i, x, n) for( i =x; i>(ll)(n); i--)
using namespace std;
typedef long long int  ll;
const long long  mod=1e9+7;
ll n,a[500005],b[500005],k,m,res=0,sm=0,edge=-1,nb=1,ans=0;
ll vis1[100005],vis[100005]={0};
multiset< ll >mst;
set< pair<ll,ll> > st,st2;
string s1,s2,t1,t2;
vector<ll>v[500005],vp,vans,v3;
vector< pi >vv,vv1,adj,v1,v2;
vector< pair< pi , ll> > vvv;
priority_queue <pair<pi,ll> > pq;
set< pair<ll,ll> > ::iterator it;
set< pair<pi,ll> >::iterator it1,it2,it3;
map< ll,ll>red,mplose;
ll dx[]={0,0,-1,1};
ll dy[]={1,-1,0,0};
int voyel(char c){
	if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u'||c=='y')return 1;
	return 0;
}



int main(){
In_the_name_of_allah_the_merciful
	ll i=0,j=0,x,y,z=0,mx=0,nb2=0,nb1=0,nbmax=0,nbinf=0,mm=5e9,idx=0,ok=0,l,r,negatif=0,positif=0,q,d,res1=0,flag;
	char c;
  //freopen("a.in","r",stdin);
  cin>>m>>n;
  for(i=1;i<=m;i++){
    cin>>k;
    memset(vis,0,sizeof(vis));
    for(j=1;j<=k;j++){
        cin>>x;
        vis[x]=1;
        v[i].pb(x);
    }
    for(k=1;k<i;k++){
        ok=1;
        for(j=0;j<v[k].size();j++){
            x=v[k][j];
            if(vis[x]){
                ok=0;break;
            }
        }
        if(ok)return cout<<"impossible",0;
    }

  }
   cout<<"possible";

Alhamdulillah
 }
