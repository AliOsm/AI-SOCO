#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    int n; cin>>n;
    if (n%2==1)
        cout<<-1<<endl;
    else {
        for(int i=1;i<=n/2;++i)
            cout<<i*2<<' '<<i*2-1<<' ';
        cout<<endl;
    }
    return 0;
}
