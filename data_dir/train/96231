#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll ans[2],sum,n,x;

int main() {
	cin >> n;
	for(int i = 0 ; i < n ; i++){
		scanf("%d",&x);
		if(x%2==0)ans[0]++;
		else ans[1]++;
		sum+=x;
	}
	cout << ans[sum%2] << endl;
	
	return 0;
}