#include <bits/stdc++.h>

using namespace std;

int x[4];

int main() {
	for (int i = 0; i < 4; i++)
		scanf("%d", x + i);
	sort(x, x + 4);
	puts(x[0] + x[1] + x[2] == x[3] || x[0] + x[3] == x[1] + x[2] ? "YES" : "NO");
}
