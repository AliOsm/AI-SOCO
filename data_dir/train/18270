#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y, a, b;
    cin >> x >> y >> a >> b;
    cout << b  / ((x * y) / __gcd(x, y)) - (a - 1) / ((x * y) / __gcd(x, y)) << endl;
    return 0;
}
