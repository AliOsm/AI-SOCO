#include <bits/stdc++.h>
using namespace std;
#define forn(i, n) for(int i = 0; i < (n); i++)

string S;

int main(){
    cin >> S;
    if( isupper(S[0]) ) cout << S << '\n';
    else {
        S[0] -= 32;
        cout << S << '\n';
    }
    return 0;
}
