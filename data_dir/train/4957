#include <bits/stdc++.h>
#define pb push_back
typedef long long ll;
typedef long double ld;
using namespace std;

ll c[555][555];
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n; 
    cin >> n;
    cout << 3 * 8 * (1ll << (2 * (n - 3))) + 9 * (n - 3) * (1ll << (2 * (n - 3))) << endl;

    return 0;
}