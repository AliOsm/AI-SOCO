#include<bits/stdc++.h>
#define In_the_name_of_allah_the_merciful ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define Alhamdulillah return 0;
#define pb(x) push_back(x) ;
#define ins insert ;
#define fi first ;
#define se second ;
#define all(v) (v.begin(),v.end());
#define sz(v) ((ll)((v).size()))
#define reset(v,val) memset(v,val,sizeof(v));
#define erep(i, x, n) for ( i = x; i<=(ll)(n); i++)
#define rep(i, x, n) for( i = x; i<(ll)(n); i++)
using namespace std;
typedef  long long int   ll;
const long long int mod=1e9+7;
ll m,k,n,a[5000005],vis[500005],res=0,dp[500005],dp2[500005],ans=0,sm=0;
multiset<  ll >st,st1,st2;
string s1="";
vector<ll>v,vnode[200005],v1,v2,v3;
vector< pair<ll,ll> >vv;
vector< pair< ll,pair <ll,ll> > > vvv;
priority_queue <ll> pq;
map< pair<ll,ll> , ll >cost;


int main(){
    In_the_name_of_allah_the_merciful
	ll i=0,x,y,j=0,z=0,q,s=1000000,nb0=0,nb=0,nbmax=0,nbinf=0,nb2=0,mm=5e8,idx=0,ok=0,l,r,negatif=0,positif=0,l1,r1;
	multiset<  ll > ::iterator it,it1,it2;
	char c;
  // freopen("a.txt","r",stdin);  
    cin>>n;
    erep(i,1,n){
    	cin>>a[i];sm+=a[i];if(a[i]&1)nb++;
	}
    if(sm&1)return cout<<"First",0;
    if(nb)return cout<<"First",0;
    cout<<"Second";
	Alhamdulillah
 }