#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 5;

int q;
long long n, k;

int main(){


    cin >> q;
    while (q--){
        cin >> n >> k;
        long long ans = k * (n / k) + min (k / 2, n % k);
        cout << ans << '\n';
    }


}
