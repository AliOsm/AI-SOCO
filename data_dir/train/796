#include <bits/stdc++.h>
using namespace std;

string a , ans;
int n,x;

int main() {
	cin >> n >> a;
	for(int i = 0 ; i < n ; i++){
		if(a[i]=='0')ans+=('0'+x),x=0;
		else x++;
	}
	ans+=('0'+x);
	cout << ans;
	
	
	return 0;
}