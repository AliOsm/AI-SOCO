#include<bits/stdc++.h>
using namespace std;
int l,r,x,y;
double k;
int main()
{
    cin>>l>>r>>x>>y>>k;
    for(int i=x;i<=y;++i)
        if((i*k)>=l && (i*k)<=r && i*k-(int)(i*k)==0)
            return cout<<"YES",0;
    return cout<<"NO",0;
}