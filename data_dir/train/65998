#include <bits/stdc++.h>
using namespace std;

int T, ANS, S, q;

int main() {
    scanf("%d %d %d", &T, &S, &q);

    while(S < T) S *= q, ANS++;

    printf("%d\n", ANS);

    return 0;
}
