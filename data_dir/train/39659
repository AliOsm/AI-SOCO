#include <iostream>

using namespace std;

int main()
{
    long long a, b, c;
    cin >> a >> b >> c;
    if(b-a > 1) {
        b = a+1;
    } else if(a - b > 1) {
        a = b+1;
    }
    cout << a + b +2*c;
    return 0;
}