#include<bits/stdc++.h>
using namespace std;


int main()
{
    int n, k;

    cin >> n >> k;

    int ans = k;

    while (n--) {
        int a;
        cin >> a;
        if (k%a==0) ans = min(ans, k/a);
    }

    cout << ans << endl;


    return 0;
}
