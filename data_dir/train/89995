#include <bits/stdc++.h>
using namespace std;
const int MAX=1e5+9;
int n,v[MAX];
int main()
{
	ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	cin>>n;
	for (int i=0,x;i<n;i++) cin>>x,v[x%n]=i;
	for (int i=0,x;i<n;i++) cin>>x,cout<<v[x%n]+1<<' ';
}
