#include <iostream>
#include <algorithm>
#include <math.h>
#include <queue>
#include <map>
#include <vector>
#include <set>
#define endl '\n'
#define use_fast ios::sync_with_stdio(false), cin.tie(0), cout.tie(0)
#define ll long long
using namespace std;

const ll int N = 1e6 + 100, M = 1e8;
map <int , int> m2;
ll dp[N], p[N];

int main () {
	ll t;
	cin >> t;
	for(int i = 0; i < t; i++) {
		ll a;
		cin >> a;
		a += 6;
		cout << a / 7 << endl;
	}
	return 0;
}
