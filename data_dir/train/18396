#include <bits/stdc++.h>

using namespace std;

int n, pos = 1, a[100200];

int main(){
    cin >> n;
    for(int i = 0; i < n; i++) cin >> a[i];
    sort(a,a+n);
    for(int i = 0; i < n; i++){
    	if(a[i]>=pos){
    		pos++;
    	}
  	}
  	cout << pos;
	return 0;
}