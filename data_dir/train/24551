#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,a,b,ans; cin >> n >> a >> b;
    ans = ((a + b) % n + n) % n;
    if(ans == 0) ans = n;
    cout << ans << '\n';
    //main();
    return 0;
}
