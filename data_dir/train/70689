#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ll n, x, y; cin >> n >> x >> y;
    ll need = (ll)ceil((double)(n * y) / 100.0);
    if (need > x) cout << need  -  x << endl;
    else cout << 0 << endl;
    return 0;
}
