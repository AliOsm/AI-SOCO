#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{
    const ll n = 4;
    ll x[n];

    for(ll i=0;i<n;i++) scanf("%I64d",x+i);

    sort(x,x+n);

    for(ll i=0;i<n-1;i++)   cout << x[n-1] - x[i] << " ";
}
