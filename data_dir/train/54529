#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
	int n,m;
	cin >> n >> m;
	if(n*m == 1)
		cout << 1 << '\n';
	else
		cout << setprecision(20) << fixed << (1.0/n) + ((n-1.0)/n)*((m-1.0)/(n*m-1.0)) << '\n';
}