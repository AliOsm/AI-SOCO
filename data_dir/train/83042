#include <bits/stdc++.h>
using namespace std;
int main() {
	int a,b,c,x,y,z;
	cin >> x >> y >> z;
	for (a=1;a<=x;a++) {
		if (x%a==0) {
			b=x/a;
			if (y%b==0) {
				c=y/b;
				if (a*c==z) {
					cout << (a+b+c)*4;
					return 0;
				}
			}
		}
	}
}
