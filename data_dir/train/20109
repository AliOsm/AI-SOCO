#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    set<int> x;
    int a;
    for(int i=0;i<4;i++)
        cin >> a, x.emplace(a);
    cout << 4 - (int)(x.size()) << endl;
    return 0;
}
