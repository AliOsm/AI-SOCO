#include <bits/stdc++.h>

#define SZ(x) (((int)x.size()))

typedef long long ll;
typedef long double ld;

using namespace std;

ll a, b;

int main()
{
	cin >> a >> b;
	for (int i = 1; ; i++)
	{
		a *= 3;
		b *= 2;
		if (a > b)
		{
			cout << i;
			return 0;
		}
	}
	return 0;
}
