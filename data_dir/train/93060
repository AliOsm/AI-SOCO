#include <bits\stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n, k, minVal = 1000000000;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> k;
		minVal = min(minVal, k);
	}
	minVal == 1 ? cout << -1 : cout << 1;

	return 0;
}