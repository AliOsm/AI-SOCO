#include <bits/stdc++.h>
using namespace std;
int32_t main() {
    ios_base :: sync_with_stdio(false);
    long long n, ans = 0;
    cin >> n;
    for (long long i = 0, d; i < n; ++i) {
        cin >> d;
        ans += d & 1;
    }
    cout << min(ans, n - ans);
}