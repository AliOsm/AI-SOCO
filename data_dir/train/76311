#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
	int n, m, a;
	cin >> n >> m >> a;
	cout << ((n + a - 1) / a) * (long long)((m + a - 1) / a) << endl;
	//system("pause");
}