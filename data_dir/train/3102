#include<stdio.h>
char a[1000005];
int main()
{
int fir,sec;
fir = sec = 0;
int i;
scanf("%s",&a);
scanf("%s",&a);
for(i=0;a[i];i++)if(a[i]=='0')fir++; else sec++;
if(fir>sec)printf("%d\n",fir-sec);
else printf("%d\n",sec-fir);
return 0;
}