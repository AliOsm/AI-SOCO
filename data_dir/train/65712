#include <bits/stdc++.h>
using namespace std;

string s;

int main() {
    cin >> s;
    for (char c : s)
        if (c == 'H' || c == 'Q' || c == '9')
            return cout << "YES", 0;
    cout << "NO";
}