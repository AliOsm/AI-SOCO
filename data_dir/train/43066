#include "bits/stdc++.h"

using namespace std;

int main() {
	int l, a, b;
	cin >> l >> a >> b;
	double ans = (a * 1.0) / (a + b);
	ans = ans * l;
	cout << fixed << setprecision(15) << ans;
}