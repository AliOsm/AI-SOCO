#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int n ;
int arr[N];
int main() {
    string a , b;
    cin >> a >> b;
    if(a == b) {
        cout << "-1";
        return 0;
    }
    int ans = max( (int) a.size() , (int) b.size());
    cout << ans ;
}

