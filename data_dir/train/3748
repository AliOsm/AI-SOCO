#include <bits/stdc++.h>
using namespace std;

int main() {
    int k, l, ans = 0;
    cin >> k >> l;
    while(l % k == 0) {
        l /= k;
        ans++;
    }
    ans--;
    cout << (l == 1? "YES": "NO") << endl;
    if(l == 1)
        cout << ans << endl;
    return 0;
}
