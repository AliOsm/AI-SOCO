#include <bits/stdc++.h>
using namespace std;

int lcm(int a, int b) { return a / __gcd(a,b) * b; }

int main() {
	ios_base::sync_with_stdio(0);
    
    int a, b, c;
    cin >> a >> b >> c;
    int ans = c / lcm(a, b);
    cout << ans << '\n';
}
