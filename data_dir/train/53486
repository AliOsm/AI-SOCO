#include <bits/stdc++.h>
using namespace std;

int main ()
{
	int max = 0;
	int n, c;
	cin >> n >> c;
	int a, b;
	cin >> a;
	for(int i = 1; i < n; ++i) {
		cin >> b;
		if(a-b-c > max) {
			max = a - b - c;
		}
		a = b;
	}
	cout << max;
	
	return 0;
}

