#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    //ifstream cin("in.in");
    ll sk, num;
    cin >> sk >> num;
    sk+=sk%2;
    if(sk/2 >= num)cout << num*2-1;
    else cout << (num-sk/2)*2;
    //cin.close();
    return 0;
}
