#include <bits/stdc++.h>
using namespace std;
map <string,int> a;
int main() {
	int n,i;
	string x;
	cin >> n;
	for (i=1;i<=n;i++) {
		cin >> x;
		if (a[x]>0)	cout << x << a[x];
		else cout << "OK";
		a[x]++;
		cout << endl;
	}
}
