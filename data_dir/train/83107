#include<bits/stdc++.h>
#define int long long

using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n, t = 0;
    cin >> n;
    for (int i = 1; i < n; i++)
        t = (t + i) % n, cout << t + 1 << " \n"[i == n - 1];

    return 0;
}
