#include <bits/stdc++.h>

using namespace std;

int t;
long long x, y;

int main()
{
	for(scanf("%d", &t); t--;)
	{
		scanf("%lld %lld", &x, &y);
		if(x > 3 || y <= x || (x == 2 && y == 3))
		{
			printf("YES\n");
			continue;
		}
		printf("NO\n");
	}
}