#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
#define oo INT_MAX/2

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);
    int n,m;
    string a,b,s;
    cin>>n>>m>>a>>b;
     s+=a[0];
     s+=a.back();
     s+=b[0];
     s+=b.back();

    cout<<(s=="<>v^" || s=="><^v" ? "YES" : "NO");
}
