#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main () {
	int a, b;
	cin >> a >> b;
	int d = abs(a-b);


	cout << (d/2)*(d/2+1) + (d&1 ? d/2 + 1 : 0) << endl;
}
