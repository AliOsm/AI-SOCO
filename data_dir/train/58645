/*jai mata di
  let's rock*/
#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		string s;
		cin>>s;
		int n=s.size();
		string str=s.substr(n-2,2);
		if(str=="po")
		{
			cout<<"FILIPINO"<<endl;
		}
		if(n>=4)
		{
			string str=s.substr(n-4,4);
			if(str=="desu" || str=="masu")
			   cout<<"JAPANESE"<<endl;
		}
		if(n>=5)
		{
			string str=s.substr(n-5,5);
			if(str=="mnida")
			  cout<<"KOREAN"<<endl;
		}
	}
	return 0;
}
