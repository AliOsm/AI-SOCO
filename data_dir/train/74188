#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {

	ios_base::sync_with_stdio(0);
	cin.tie(0);
	ll n;
	cin >> n;
	for (int i = 1; i <= n && n - i > 0; i++)
		n -= i;
	cout << n << endl;
	return 0;
}
