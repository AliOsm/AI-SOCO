#include <bits/stdc++.h>
using namespace std;
using LL = long long;

int main() {
    int n; cin >> n;
    int en[2] = {0, 0};
    for (int i = 1; i <= n; i++) {
        int x; cin >> x;
        en[x] = i;
    }
    cout << min(en[0], en[1]) << endl;
    return 0;
}