#include <bits/stdc++.h>

using namespace std;

int n, k = 1;
string s;
map <string, int> m;

int main ()
{
	cin >> n;
	while (n --)
	{
		cin >> s;
		if (m[s] == 0)
			cout << "OK" << endl;
		else
			cout << s << m[s] << endl;
		m[s] ++;
	}
	return 0;
}