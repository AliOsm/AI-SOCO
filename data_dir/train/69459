#include <bits/stdc++.h>

using namespace std;

int main() {
  int r, x1, y1, x2, y2;
  cin >> r >> x1 >> y1 >> x2 >> y2;
  double dist = hypot(abs(x1 - x2), abs(y1 - y2)) / 2;
  cout << (int)ceil(dist / r);
}