#include<bits/stdc++.h>

using namespace std;

int main() {
	int n, ax, ay, bx, by, cx, cy;
	cin >> n >> ax >> ay;
	cin >> bx >> by;
	cin >> cx >> cy;
	if((ax - bx) * (ax - cx) > 0 && (ay - by) * (ay - cy) > 0)
		cout << "YES" << endl;
	else
		cout << "NO" << endl;
	return 0;
}