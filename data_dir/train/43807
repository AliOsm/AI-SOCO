#include<iostream>
#include<string.h>
#include<cstdlib>

using namespace std;
int main()
{
    char str[1000];
    cin>>str;
    int n = (int)str[0];
    if(n>96){
        n-=32;
        str[0]=(char)n;
        cout<<str;
    }
    else if(n>64&&n<91)
      cout<<str;

}