#include <bits/stdc++.h>
using namespace std;
int n , m , i , j , a[10] , b[10] , c[10];
int main()
{
    cin >> n >> m;
    for (i = 0; i < n; i++)
        cin >> a[i];
    for (i = 0; i < m; i++){
        cin >> b[i];
        c[b[i]]++;
    }
    for (i = 0; i < n; i++){
        if (c[a[i]])cout << a[i] << " ";
    }
    cout << endl;
}
