#include <iostream>
#include <vector>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    vector<int> m = {2, 7, 2, 3, 3, 4, 2, 5, 1, 2};
    char a, b;
    cin >> a >> b;
    cout << m[a - 48] * m[b - 48];
    return 0;
}