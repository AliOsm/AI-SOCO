#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
	ll A, B;
	cin >> A >> B;
	ll x, y, z;
	cin >> x >> y >> z;
	ll cntA = 2ll * x + y, cntB = y + 3ll * z;
	ll addA = max(0ll, cntA - A), addB = max(0ll, cntB - B);
	cout << (addA + addB) << endl;
}