#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n; cin>>n;
    set<set<char>> res;
    while (n--) {
        string s; cin>>s;
        set<char> t(begin(s), end(s));
        res.insert(t);
    }
    cout<<size(res)<<endl;
}
