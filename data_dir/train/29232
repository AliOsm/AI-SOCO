#include<bits/stdc++.h>
using namespace std;
#define int long long
main(){
      int a , b , c;
      cin >> a >> b >> c;
      cout << max((a > b) + 2 * c + min(a , b) * 2 , min(2 * b - 1 , 2 * a + 1) + 2 * c + min(1ll , max(1ll*0 , a - b))) << "\n"; 
}