#include <iostream>
using namespace std;
int main() {

	long long int t = 0, pos = 0, a = 0, b = 0, k = 0;

	cin >> t;

	while (t--) {
		cin >> a >> b >> k;
		pos = (a - b) * (k / 2);
		if (k % 2 != 0) {
			pos += a;
		}
		cout << pos << endl;
	}

	return 0;
}