#include <iostream>
using namespace std;
int main()
{
	int a, b, c, ans = 0, n;
	cin >> a >> b >> c;
	ans = ((a+b+c) % 5 == 0) ? ((a+b+c ) / 5) : ((a+b+c ) / 5 + 1);
	cin >> a >> b >> c;
	ans += ((a+b+c) % 10 == 0) ? ((a+b+c ) / 10) : ((a+b+c ) / 10 + 1);
	cin >> n;
	if (ans <= n){
		cout << "YES";
	}
	else{
		cout << "NO";
	}
	return 0;
	
}
