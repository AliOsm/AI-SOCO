#include<bits/stdc++.h>
using namespace std;
#define pi 3.14159265359
#define int long long
#define pii pair<int,int>
#define ld long double
const int mod=1e9 +7;

int dx[] = {-1, 0, 1, 0};
int dy[] = { 0,-1, 0, 1};




int32_t main()
{
     ios::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);

   // freopen("input.txt", "rt", stdin);
  //freopen("output.txt", "wt", stdout);
  
  int n;
  cin>>n;
  int a[n][n];
  int r[n]={},c[n]={};
  for(int i=0;i<n;i++)
  {
      for(int j=0;j<n;j++)
      {
          cin>>a[i][j];
         
      }
  }
int z=a[1][0]*a[1][2]/a[0][2];
z=sqrt(z);


for(int i=0;i<n;i++)
{
    if(i!=1)
    {
        cout<<a[1][i]/z<<" ";
    }
    else cout<<z<<" ";
}
  
      
}