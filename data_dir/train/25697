#include <stdio.h>
using namespace std;

int main(int argc, char **argv)
{
    int k, n, s, p;
    scanf("%d%d%d%d", &k, &n, &s, &p);
    int totalPack = (k * ((n + s - 1) / s) + p - 1) / p;
    printf("%d", totalPack);
}
