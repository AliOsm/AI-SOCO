#include <bits/stdc++.h>

using namespace std;

int n, x, y, ta, tb;

int main() {
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf("%d%d", &ta, &tb);
		x += ta;
		y += tb;
	}
	printf("%d\n", min(x, n - x) + min(y, n - y));
	return 0;
}
