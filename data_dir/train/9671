#include <bits/stdc++.h>

using namespace std;

const int N = int(1e6);
const int INF = 1e9 + 7;

const int base = 10;
const double EPS = 1e-7;

typedef long long ll;
typedef pair < int, int > pii;

ll n;
int main (){
  cin >> n;
  ll ans = n % 10;
  ll d = 10 - ans;
  if (d < ans){
      n += d;
  }

  else {
      n -= ans;
  }

  cout << n;
  return 0;
}
