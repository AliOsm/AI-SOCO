#include <algorithm>
#include <iostream>

using namespace std;

int main() {
	int t; cin >> t;
	while (t--) {
		int n, k; cin >> n >> k;
		cout << (n % 2 == k % 2 && n / k >= k ? "YES" : "NO") << '\n';
	}
	return 0;
}
