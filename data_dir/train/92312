#include <bits/stdc++.h>
using namespace std;

int n, x;
char arr[102];

int main(){

    scanf("%d %s",&n,arr);

    for(int i=0; i<n; i++){
        if(arr[i]=='8')
            x++;
    }

    printf("%d\n",min(x,n/11));

    return 0;
}
