#include<bits/stdc++.h>
using namespace std;
int N, K, X, i, P, H;
int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin>>N>>K>>X;
	H=K*X;
	for (i=K;i<N;i++) {
		cin>>P;
		H+=P;
	}
	cout<<H<<'\n';
}
