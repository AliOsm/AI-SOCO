#include <bits/stdc++.h>

using namespace std;

int g,c;

int main(){
	cin >> g >> c;
	if (g==0 && c>0)
		return cout << "Impossible",0;
	cout << g+max(c-g,0) << " " << g+max(0,c-1);  			
}