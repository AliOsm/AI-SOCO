#include <bits/stdc++.h>
using namespace std;

int main() {
	int q;
	cin >> q;
	for (int i = 0; i < q; i++) {
		int l, r, d;
		cin >> l >> r >> d;
		if (d >= l) {
			r = r + 1;
			cout << ((r + d - 1) / d) * d << endl;
		}
		else
			cout << d << endl;
	}
	return 0;
}
