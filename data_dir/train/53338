#include <bits/stdc++.h>
using namespace std;

long long n, ans;

int main() {
	cin >> n;
	for (int i = 0; i < n; i++) {
		long long x, y;
		cin >> x >> y;
		ans = max(ans, x + y);
	}
	cout << ans;
}

