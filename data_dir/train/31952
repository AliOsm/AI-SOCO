#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main()
{

    int q;
    scanf("%d", &q);
    while(q--){
        ll k, sum;
        scanf("%lld %lld", &k, &sum);
        ll ans = (k-1)*9 + sum;
        printf("%lld\n", ans);
    }
}

