#include<bits/stdc++.h>
#define MOD (ll)(1e9+7)
#define MAX (ll)(1e4+3)
using namespace std;
using ll = long long;

int main () {
	ll n;
	cin >> n;
	ll root = (ll) sqrt (n);
	int xx = root * 2;
	int x = n - root * root;
	if ( x ) {
		xx++;
		if ( x > root ) {
			xx++;
		}
	}
	cout << xx << "\n";
}
