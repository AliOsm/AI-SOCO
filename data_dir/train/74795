#include <bits/stdc++.h>

using namespace std;

int main(){
int n;cin>>n;
stack < string > st;
map <string, int > m;
string temp;
for(int i=0;i<n;i++){
    cin >> temp;
    st.push(temp);
}

while(!st.empty()){
        if(m[st.top()]!=1){
            m[st.top()]=1;
            cout << st.top() << endl;
        }
        st.pop();

}
}
