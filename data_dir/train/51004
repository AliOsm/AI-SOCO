#include<bits/stdc++.h>
#define MAX 10000
#define pb push_back
using namespace std;





int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    int n,x,y,sol=0;
    cin>>n>>x>>y;
    while((x*100)/n < y){x++;sol++;}
    cout<<sol<<endl;

}
