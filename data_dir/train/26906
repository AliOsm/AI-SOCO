#include <bits/stdc++.h>
using namespace std;

int main(){
	int T;
	scanf("%d",&T);
	int len, v, l, r, sum;
	while (T--){
		scanf("%d %d %d %d",&len,&v,&l,&r);
		sum = len / v - r / v + (l - 1) / v;
		printf("%d\n",sum);
	}
	return 0;
}