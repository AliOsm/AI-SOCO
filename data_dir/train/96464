#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n,c=0,res=0;
	string s;
	cin >> n >> s;
	for(int i=0 ; i < n; i++)
	{
		if(s[i]=='x')
			c++;
		else
			c=0;
		if(c >= 3)
			res++;
	}
	cout << res;
}
