#include <bits/stdc++.h> 
using namespace std;

int main()
{
    int n,ans=0;
    cin >> n;
    char f[n];
    cin >> f;
    for(int i = 0 ; i < n ; i += 1){
        if(f[i] % 2 == 0){
            ans += i+1;
        }
    }
    cout << ans;
}