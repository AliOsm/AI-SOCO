#include <bits/stdc++.h>
using namespace std;
int u[1001];
int main(){
  ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(0);
  int n,m;
  cin>>n>>m;
  if(abs(n-m)>1 || n+m==0)cout<<"NO\n";
  else cout<<"YES\n";
  return 0;
}

