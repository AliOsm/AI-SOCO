#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, i, j, a[11], b[11];

    cin >> n >> m;

    for(i = 0; i < n; i++) cin >> a[i];
    for(i = 0; i < m; i++) cin >> b[i];

    for(i = 0; i < n; i++){
        for(j = 0; j < m; j++){
            if(a[i] == b[j]){
                cout << b[j] << " ";
                break;
            }
        }
    }

    cout << endl;

    return 0;
}
