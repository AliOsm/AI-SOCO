// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    int n, l, r;
    cin >> n >> l >> r;
    int x = (1 << l) - 1;
    cout << x + (n-l) << ' ';
    int y = (1 << r) - 1;
    cout << y + (n - r) * (1 << r - 1);
    return 0;
}
