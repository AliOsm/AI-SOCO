#include <bits/stdc++.h>

using namespace std;

int main(){
	int n, k; scanf("%d %d", &n, &k);
	for(int i = k - 1; i > 0; i--) {
		if(n % i == 0 ) {
			cout << n / i * k + i << endl;
			return 0;
		}
	}
	return 0;
}
