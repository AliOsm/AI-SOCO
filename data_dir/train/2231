#include<stdio.h>
#define LL long long
int main(void)
{
	LL n, k;
	scanf("%lld%lld", &n, &k);
	if(k<=(n+1)/2)
		printf("%lld\n", 2*k-1);
	else
	{
		k -= (n+1)/2;
		printf("%lld\n", 2*k);
	}
	return 0;
}