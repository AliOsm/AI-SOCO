#include <bits/stdc++.h>
#define fer(i, a, b) for (int i = a; i <= b; ++i)
using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    cout << 4 * (sqrt(a * b / c) + sqrt(b * c / a) + sqrt(c * a / b)) << '\n';
    return 0;
}
