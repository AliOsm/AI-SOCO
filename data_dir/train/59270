#include "cstdio"
#include "cstring"
#define NMAX 100001
using namespace std;

char x[NMAX];

int main() {
    scanf("%s", x);
    int len = strlen(x);
    printf("%s", x);
    for (int i=len-1; i>=0; i--)
        printf("%c", x[i]);
    return 0;
}