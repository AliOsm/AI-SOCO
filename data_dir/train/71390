#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <algorithm>
#include <queue>
#include <cmath>
#include <list>
#include <set>

using namespace std;

int n,rs=100000,t,S=2001;
int k,w,i,j,ans=0,tmp=0,r=0,u,x=0,y=0;
char ch;
int an=0;
//vector<int>v(S),p(S),c(S),vx(S),vy(S);/
//vector<vector<int> >m(S,vector<int>(S));
//vector<list<int> >ad(S,list<int>());
list<int>l;
queue<int>q;


int main()
{
	ifstream fin("input.txt");
	cin>>n;
	for(i=0;i<n;i++){
		cin>>k;
		if(k%2)
			y++;
		else
			x++;
	}
	if(y%2)
		cout<<y<<endl;
	else
		cout<<x<<endl;
	return 0;
}