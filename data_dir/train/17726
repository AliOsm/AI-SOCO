#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int const N = 100 * 1000 + 16;
int const M = 1000 * 1000 * 1000 + 7;

int main() {
  cin.tie(0);
  cin.sync_with_stdio(0);

  int n;
  cin >> n;
  if(n&1)
    cout << -1;
  else
    for(int i = n; i > 0; --i)
      cout << i << " ";
  cout << flush;
}