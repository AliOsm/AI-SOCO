#include "bits/stdc++.h"
using namespace std;

int main() {

	int tc;
	cin >> tc;

	while(tc--) {

		long long l,r,x;
		cin >> l >> r >> x;

		if(x < l) {
			cout << x << endl;
		} else {
			cout << x*((r + x)/x) << endl;
		}
	}
}