#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(0);
  cin.sync_with_stdio(0);

  int n;
  cin >> n;
  for(int i = 0; i < n; ++i)
    cout << (10'000'000 - (n - i - 1)) << " ";
}