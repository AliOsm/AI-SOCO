#include <bits/stdc++.h>
using namespace std;
int main()
{
    string str1,str2;
    cin >> str1;
    cin >> str2;
    if(!str1.compare(str2))
        printf("-1");
    else cout << max(str1.size(),str2.size());
    return 0;
}
