#include<bits/stdc++.h>

using namespace std;

int main(){

  int n,m;

  cin >> n >> m;

  int soma = 0;

  for(int i = 1;i <= n;i++){
    int x;
    cin >> x;

    cout << (soma + x)/m << " ";
    
    soma = (soma + x)%m;
  }
}