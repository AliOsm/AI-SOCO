//1047B
#include <iostream>
#include <algorithm>
using namespace std;

int n, x, y, ans;

int main() {
	cin >> n;
	for (int i = 0; i < n; ++i) {
		cin >> x >> y;

		ans = max(ans, x + y);
	}
	cout << ans;
}