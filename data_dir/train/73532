#include <iostream>
#include <algorithm>
#include <map>
#include <vector>
#include <set>
#include <cmath>
#include <string>
#include <sstream>
#include <vector>

using namespace std;

int main()
{

   long long n , x , y ;
   cin >> n >> x >> y ;
   if ( (pow( y-n+1 , 2 ) + n-1 >= x )  &&   (y-n+1) >= 1 )
   {
       cout<<y-n+1<<endl;
       for (int i = 1 ; i < n ; i++ )cout<<1<<endl;
   }
   else cout<<-1;
}
