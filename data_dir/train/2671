#include <bits/stdc++.h>

using namespace std;

int t;

int main() {
	scanf("%d", &t);
	while (1) {
		string s = to_string(t);
		int sum = 0;
		for (char i : s)
			sum += i - '0';
		if (sum % 4 == 0)
			break;
		t++;
	}
	printf("%d\n", t);
}
