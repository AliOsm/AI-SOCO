#include <iostream>
#include <stdio.h>
#include<bits/stdc++.h>
#define ll long long
#define read freopen("input/test (4).txt","r",stdin)
#define write freopen("output/test (4).txt","w",stdout)
#define fr first
#define sc second
#include <sstream>
#define ii pair < int , int >
#define mp make_pair
using namespace std;
int main()
{
 ll n;
 cin>>n;
 if(n == 1)
 {
     cout<<-1;
 }
 else
 {
     ll sum = n * (n+1);
     cout<<n<<" "<<n+1<<" "<<(sum);
 }

}
/*
ll a[200101];
vector < vector < int > > v;
ll dp1[200101];
ll dp2[200101];
ll maxx ;
ll solve(int node)
{
    vector < ll > sum;
    ll total = 0;
    for(int i=0;i<v[node].size();i++)
    {
        ll R = solve(v[node][i]);
        sum.push_back(R);
        total+=R;
    }
    dp1[node] = total;
    dp2[node] = -1e18;
    sort(sum.begin(),sum.end());
    sum.
    if(sum.size() > 1)
    {
        int len = sum.size();
        maxx = max(maxx , sum[len-1]+sum[len-2]);
    }
    if(sum.size() > 0)
    {

    }
    return max(dp1[node],dp2[node]);
}
int main()
{
 int n;
 cin>>n;
 maxx = -1e18;
 v.resize(n+1);
 for(int i=1;i<=n;i++)
 {
     scanf("%d",&a[i]);
 }
 for(int i=0;i<n-1;i++)
 {
     int x,y;
     scanf("%d%d",&x,&y);
     v[x].push_back(y);
     v[y].push_back(x);
 }
 ll R  = solve(1);
 if(R != -1e18)
 {
     cout<<R;
 }
 else
 {
     cout<<"Impossible";
 }

}
*/
