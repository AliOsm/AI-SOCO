#include <bits/stdc++.h>

#define SZ(x) (((int)x.size()))

typedef long long ll;
typedef long double ld;

using namespace std;

int a[3];

int main()
{
	cin >> a[0] >> a[1] >> a[2];
	sort(a, a + 3);
	cout << max(0, a[2] - a[1] - a[0] + 1);
	return 0;
}
