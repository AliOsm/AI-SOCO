#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll ans = 1;
int main()
{
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n;
    cin >> n;
    for(int i = 0 ; i < n ; i++)ans+= 4*i;
    cout << ans;



    return 0;
}
