#include <bits/stdc++.h>

using namespace std;

#define amen exit(0)
#define ld long doible
#define INF 1e11+9
#define DIM 1009
#define pb push_back
#define pairll pair<long long,long long>
#define se second
#define fi first
#define fast ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);

long long i,j,k,l,n,m;

long double a1,a2,b1,b2;

int main()
{
    fast;

    cin>>a1>>a2;

    if(a2*log(a1)<a1*log(a2)){
        cout<<"<"<<endl;
    }
    if(a2*log(a1)>a1*log(a2)){
        cout<<">"<<endl;
    }
    if(a2*log(a1)==a1*log(a2)){
        cout<<"="<<endl;
    }

    amen;
}
