#include <bits/stdc++.h>
using namespace std;
int main(){
  string s;
  cin >> s;
  int c = count(s.begin(),s.end(),'a');
  int ans = min((int)s.size(),c+c-1);
  ans=max(ans,0);
  cout << ans <<'\n';
   return 0;
}