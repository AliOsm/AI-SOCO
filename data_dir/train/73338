#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main(){

    int t;
    scanf("%d", &t);

    ll s, a, b, c;
    while(t--){
        scanf("%lld %lld %lld %lld", &s, &a, &b, &c);

        printf("%lld\n", s / c + s / c / a * b);

    }

}
