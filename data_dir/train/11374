#include <bits/stdc++.h>

using namespace std;


int n, v[100002];
string s;

int main()
{
  cin >> n;
  for (int i = 1; i <= n; i++)
    cin >> v[i];
  sort (v + 1, v + n + 1);
  cout << min(v[n] - v[2], v[n - 1] - v[1]);
  return 0;
}
