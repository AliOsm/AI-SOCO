#include <bits/stdc++.h>

using namespace std;


int main()
{
    string s; cin >> s;
    int rem;
    if (s.size() > 2) {
        rem = stoi(s.substr(s.size()-2, 2));
    }
    else{
        rem = stoi(s);
    }
    if(rem % 4 == 0) {
        puts("4");
    }
    else{
        puts("0");
    }
}
