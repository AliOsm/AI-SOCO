#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
	int t;

	cin >> t;
	while(t--)
	{
		int x, y, a, b;
		cin >> x >> y >> a >> b;
		if((y-x) % (a + b) == 0)
			cout << (y-x) / (a + b) << '\n';
		else
			puts("-1");
	}









	return 0;
}
