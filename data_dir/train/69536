#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);cin.tie(0);
	long long s,v1,v2,t1,t2,a1,a2;
	cin>>s>>v1>>v2>>t1>>t2;
	a1=2*t1+v1*s;
	a2=2*t2+v2*s;
	if(a1<a2)
	    cout<<"First";
	else if(a1>a2)
	    cout<<"Second";
	else
	    cout<<"Friendship";
	return 0;
}
