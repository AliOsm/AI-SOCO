#include <bits/stdc++.h>
#define MOD 1000000007

using namespace std;

const double pi = acos(-1);
const int N = 5e4 + 5;

int n, ans;
string s;

int main()
{
    cin >> n >> s;
    while(ans < n && s[ans] == '1') ++ans;
    cout << ans + (ans < n);
    return 0;
}
