#include <iostream>
using namespace std;

int main() 
{
	string a , b;
	cin >> a >> b;
	string ax = string(b.size(), '0')+a;
	string bx = string(a.size(), '0')+b;
	
	if(ax>bx)
		cout << ">";
	if(ax==bx)
		cout << "=";
	if(ax<bx)
		cout << "<";
	return 0;
}