#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>
#define N 100020
using namespace std;

char s[N];

int main(void)
{
    int n;

    scanf("%d", &n);
    scanf("%s", s);

    sort(s, s + n);

    puts(s);

    return 0;
}