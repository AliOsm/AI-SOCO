
#include <bits/stdc++.h>
using namespace std;
#define lli long long int

int main() 
{
	int n,k;
	cin>>n>>k;
	for (int i=0 ; i<n ; i++)
	{
		for (int j=0 ; j<n ; j++)
			cout<<(i==j ? k : 0)<<" ";
		cout<<"\n";
	}
	return 0;
}