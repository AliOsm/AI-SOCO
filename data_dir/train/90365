#include <bits/stdc++.h>
#define ll long long
 
using namespace std;
 
int main ()
{
    ios_base::sync_with_stdio(false);
    int n, a=0; cin >> n;
    for(int i=0;i<n;i++) {
        int x; cin >> x;
        a+=(x&1);
    }
    cout << (a ? "First":"Second") << endl;
    return 0;
}