#include <bits/stdc++.h>
using namespace std;

int a, b;

int main(){

    scanf("%d %d",&a,&b);

    int stp = sqrt(a);

    printf("%s\n", (stp+1)*(stp) <= b ? "Vladik" : "Valera");

    return 0;
}
