#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int k , n , s , p;
    cin >> k >> n >> s >> p;
    int Single = n/s + (n%s != 0);
    cout << (Single * k) / p + ((Single * k) % p != 0);
    return 0;
}
