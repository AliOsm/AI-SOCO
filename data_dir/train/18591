#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main() {

	int n;
	cin >> n;

	for (int a = n; ; a++) {
		int sm = 0, x = a;
		while (x) {
			sm += x%10;
			x /= 10;
		}
		if (sm%4 == 0) {
			cout << a << endl;
			return 0;
		}
	}


}