#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, x;
    int dd[105] = { 0 };
    int d = 0;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x;
        dd[x]++;
        d = max(d, dd[x]);
    }
    cout << d;
}