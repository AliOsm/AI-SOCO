#include <bits/stdc++.h>
using namespace std;
#define REP(i , j , k) for(int i = j ; i < k ; i ++)

int n;
int main(){
    cin >> n;
    if(n <= 2) cout << n << endl;
    else cout << 1 << endl;
    return 0;
}
