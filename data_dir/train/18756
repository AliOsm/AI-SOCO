#include <bits/stdc++.h>
using namespace std;

int main(){
	int n, k, t;
	scanf("%d %d",&n,&k);
	t = (n - k) / 2;
	for (int i = 0; i < n; i++) printf("%d", (i % (t + 1) == t ? 1 : 0));
	printf("\n");
	return 0;
}