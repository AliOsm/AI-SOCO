#include <bits/stdc++.h>

using namespace std;

int main ()
{
    ios_base::sync_with_stdio(false);
    int n, a, b, m=0; cin >> n;
    while(n--) cin >> a >> b, m=max(m,a+b);
    cout << m << endl;
    return 0;
}
