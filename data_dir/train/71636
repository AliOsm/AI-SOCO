#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, x, a, tot = 0;
    cin >> n >> x;
    for (int i = 0; i < n; i++)
        cin >> a, tot += a;
    if (tot + n - 1 != x)
        cout << "NO";
    else
        cout << "YES";
    return 0;
}
