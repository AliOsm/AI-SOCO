#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n , m , k ;
    cin >> n >> m >> k ;
    if(min(m , k) < n)
        cout << "No\n" ;
    else
        cout << "Yes\n" ;
    return 0 ;
}