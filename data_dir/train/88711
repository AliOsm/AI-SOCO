#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <map>
#include <queue>
#include <set>
using namespace std;

const int N = 1000001;
int a[N];
int b[N];
int c[N];
int g[N];
vector<int> gr[10001];

int main() {
	ios::sync_with_stdio(false);
	int n, m, k;
	cin >> n ;
	for (int i = 0; i < n; ++i)
	{
		cin >> a[i];
	}
	sort(a, a + n);
	cout << a[(n-1) / 2];
	return 0;
}