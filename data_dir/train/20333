#include <bits/stdc++.h>
using LL = long long ;


int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    printf("%d %d\n",std::min(a,b),(a - std::min(a,b)) / 2 
            + (b - std::min(a,b)) / 2);
}
