#include <bits/stdc++.h>
using namespace std;

long long n, s;

int main(){
    scanf("%lld", &n);
    s = sqrt(2 * n);
    if(s * (s + 1) / 2 >= n){
        s--;
    }
    n -= (s * (s + 1) / 2);
    printf("%lld\n", n );
    return 0;
}
