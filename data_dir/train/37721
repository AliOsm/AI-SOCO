#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    int n, a, b, c=1; cin >> n >> a;
    for(int i=1;i<n;i++) {
        cin >> b;
        if(a!=b) c++;
        a=b;
    }
    cout << c << endl;
    return 0;
}
