#include <bits/stdc++.h>
using namespace std;

int x, y, z, a, b, c;

int main(){
    scanf("%d %d %d", &x, &y, &z);
    scanf("%d %d %d", &a, &b, &c);
    printf("%s\n", (x != a && y != b && z != c) ? "NO" : "YES");
    return 0;
}
