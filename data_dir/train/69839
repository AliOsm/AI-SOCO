#include <bits/stdc++.h>
using namespace std;

#define    ll    long long int

int main( )
{
    ll a1, a2, a3, a4, a5, a6;
    cin >> a1 >> a2 >> a3 >> a4 >> a5 >> a6;
    ll a = a1 + a2 + a3;
    cout << ( (a * a) - (a1 * a1) - (a3 * a3) - (a5 * a5) ) << "\n";
    return 0;
}
