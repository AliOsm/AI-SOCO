#include<bits/stdc++.h>
using namespace std;
int i, j, k, l, x, y, z, m, n, ans;
int ara[3000];
int main()
{
    cin >> n;
    for(i = 0; i < n; i++) cin >> ara[i];
    sort(ara, ara + n);
    swap(ara[0], ara[n - 1]);
    for(i = 0; i < n; i++) cout << ara[i] << ' ';
    return 0;
}
