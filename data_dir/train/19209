#include "bits/stdc++.h"
using namespace std;

using ll = long long;
using ld = long double;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    ll n, k;
    cin >> n >> k;
    cout << (2 * n + k - 1) / k + (5 * n + k - 1) / k + (8 * n + k - 1) / k<< endl;
}
