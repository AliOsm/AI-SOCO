#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin >> t;
	while (t--) {
		int n;
		int f[3];
		cin >> f[0] >> f[1] >> n;
		f[2] = (f[0] ^ f[1]);
		cout << f[n % 3] << '\n';
	}
	return 0;
}
