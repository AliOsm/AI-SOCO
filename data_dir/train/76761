#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const ll N = 2e5 + 10;
ll n, m, q, ar[N];

int main() {
	cin >> n >> m;
	for (ll i = 0; i < n; i++)
		cin >> ar[i];
	for (ll i = 0; i < n; i++) {
		q += ar[i];
		cout << q / m << " ";
		q %= m;
	}
}

