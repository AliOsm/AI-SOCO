#include <bits/stdc++.h>

using namespace std;

#define INF 1e9
#define DIM 200009
#define ld long double
#define pairll pair<long long,long long>
#define se second
#define fi first
#define amen exit(0)
#define fast ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0)

long long i,j,k,l,n,m,r;

long long a[DIM];

string s;

long long query(long long x,long long y){
    cout<<x<<' '<<y<<endl;
    cout.flush();
    cin>>s;
    if(s=="white")return 1;
    return 0;
}

long long x,y,minb=INF,maxw=-INF;

int main()
{
    cin>>n>>s;

    sort(s.begin(),s.end());
    cout<<s<<endl;

    return 0;
}
