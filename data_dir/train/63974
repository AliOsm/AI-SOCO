#include <bits/stdc++.h>

using namespace std;

int const N = 300 * 1000 + 16;

int n;
char w[N];

int main() {
  cin.tie(0);
  cin.sync_with_stdio(0);

  cin >> n;
  if(n < 3) {
    cout << 0;
  } else {
    cout << n/2 - (n%2==0);
  }
  cout << endl;
}