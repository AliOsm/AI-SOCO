#include <bits/stdc++.h>
using namespace std;

int N, L, R;

int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	cin>>N;
	
	for (int i=0;i<N;i++) {
		cin>>L>>R;
		cout<<R<<' ';
		cin>>L;
		cout<<L+(L==R)<<'\n';
		cin>>R;
	}
	
	return 0;
}
