#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>

using namespace std;


int n;
int main() {
    vector<int> v = {0, 4, 4, 4, 4, 4, 4, 4, 4, 4, 15, 4};
    cin >> n;
    n -= 10;
    if (n <= 0 || n > 11)
        cout << 0;
    else
        cout << v[n];
}
