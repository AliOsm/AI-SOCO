#include <bits/stdc++.h>
using namespace std;
int main() {
	long n,a,b,s;
	cin >> a >> b >> n;
	s=a*10%b;
	if (s==0) cout << a << 0;
	else if (b-s<=9) cout << a << (b-s);
	else {
		cout << -1;
		return 0;
	}
	for (n--;n>0;n--) cout << 0;
}
