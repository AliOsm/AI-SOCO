#include <bits/stdc++.h>
using namespace std;
const int N = 1000000 + 20, M = 500;
int a[N], mark[N];
int mt[M][M];
int n;
int main() {
	int n;
	cin >> n;
	string s;
	cin >> s;
	for (int i = 0; i < n; i++) {
		string t;
		cin >> t;
		if (s == t.substr(0, 3)) {
			a[0]++;
		}
	}
	if (2 * a[0] <= n) {
		cout << "home";
	}
	else {
		cout << "contest";
	}
	return 0;
}
