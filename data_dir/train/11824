#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,mx=0,a[200005];
    cin>>n;
    for (int i=0;i<n;i++)
    {
        cin>>a[i];
        if (a[i]>mx) {cout<<i+1; return 0;}
        mx=max(mx,a[i]+1);
    }
    cout<<-1;
    return 0;
}
