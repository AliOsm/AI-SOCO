#include<bits/stdc++.h>
using namespace std;

int main() {
  string s;
  while (cin >> s) {
    for (int i = 0; i < s.size(); i++)
      cout << (s[i] == '.' ? 0 : (s[i++ + 1] == '.' ? 1 : 2));
    cout << endl;
  }
  return 0;
}
