#include <bits/stdc++.h>

using namespace std ;


int main()
{
    cin.tie(nullptr) ;
    ios_base :: sync_with_stdio(false) ;
    int t , n;
    cin >> t ;
    while(t--)
    {
        cin >> n ;
        cout << (n - 1) / 2 << '\n';
    }
    return 0;
}