#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;

    cin >> T;

    while(T--) {
        int n,s,t;
        cin >> n >> s >> t;
        int res = max(s,t) - ((s+t)-n) + 1;
        cout << res << endl;
    }
    return 0;
}
