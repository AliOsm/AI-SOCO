#include <iostream>
#include <algorithm>
#include <math.h>
#include <map>
#include <vector>
#include <set>
#include <deque>
#define ss second
#define ff first
#define use_fast ios::sync_with_stdio(false), cin.tie(0), cout.tie(0)
#define ll long long
#define pb push_back
using namespace std;

const int N = 2e5 + 100;
typedef pair <int, int> pii;
map <ll, bool> ma, mb;
ll a[N];
pii p[N];
vector <int> v, v1;
set <string> s;

int main() {
	double n;
	cin >> n;
	n++;
	double b = log2(n);
	b = ceil(b);
	cout << b << fixed;
} 
