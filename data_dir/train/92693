#include <bits/stdc++.h>

using namespace std;

int main()
{
  int n, a1, a2, b1, b2;
  cin >> n >> a1 >> a2 >> b1 >> b2;
  cout << (a1 * n + 2 * b1 < a2 * n + 2 * b2 ? "First" : a1 * n + 2 * b1 == a2 * n + 2 * b2 ? "Friendship" : "Second");
}
