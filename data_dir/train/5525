#include<bits/stdc++.h>

using namespace std;

long long a,b,c;

int main(){
	cin>>a>>b>>c;
	if((a>b && c>0)||(a<b && c<0)) {
		cout<<"NO";
		return 0;
	}
	b-=a;
	if(b==0) cout<<"YES"; else
	if(c==0) cout<<"NO"; else
	if(b%c==0) cout<<"YES"; else cout<<"NO"; 
	return 0;
}