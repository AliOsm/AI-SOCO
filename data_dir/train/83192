# include <bits/stdc++.h>
using namespace std;
int main(){
	int n;
	string s;
	cin >> n >> s;
	int res = n / 11, cnt = 0;
	for(int i = 0; i < n; i ++){
		if(s[i] == '8') cnt ++;
	}
	cout << min(res, cnt) << '\n';
}