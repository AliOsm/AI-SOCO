#include "bits/stdc++.h"
using namespace std;

const int N = 70;
 
void solve() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);

    int t; cin >> t;
    while(t--) {
        vector<int> v(3); cin >> v[0] >> v[1] >> v[2];
        sort(v.begin(), v.end());
        if ((v[0] + v[1]) >= (v[2] - 1)) {
            cout << "Yes" << endl;
        } else {
            cout << "No" << endl;
        }
        // int n , m; cin >> n >> m;
        // stack<int> stk;
        // vector<int> a(n), b(m);
        // for(int i = 0; i < n; ++i) cin >> a[i];
        // for(int i = 0; i < m; ++i) cin >> b[i];
        // for(int i = n - 1; i >= 0; --i) stk.push(a[i]);
        // vector<int> rnk(n + 1, (int) 1e9);
        // for(int i = 0; i < m; ++i) {
        //     rnk[b[i]] = i;
        // }
        // long long ans = 0;
        // for(int i = 0; i < m; ++i) {
        //     vector<pair<int,int>> v;
        //     while(!stk.empty() && stk.top() != b[i]) {
        //         v.push_back({rnk[stk.top()], stk.top()});
        //         stk.pop();
        //     }
        //     stk.pop();
        //     ans += 2LL * (int) v.size();
        //     ans++;
        //     sort(v.begin(), v.end());

        // }
        // cout << ans << endl;
    }
}
 
 
int main() {
    solve();
    return 0;
}