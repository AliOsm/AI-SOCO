#include <bits/stdc++.h>

using namespace std;

int n, x[1005];

int main() {
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		scanf("%d", x + i);
	sort(x, x + n);
	printf("%d\n", x[(n - 1)/ 2]);
	return 0;
}
