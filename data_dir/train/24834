#include <bits/stdc++.h>

using namespace std;

main()
{
    ios_base::sync_with_stdio(false);
    int n;
    int a[1000];
    cin >> n;
    for(int i = 0; i < n; i++) cin >> a[i];
    sort(a, a + n);
    if(n & 1) cout << a[n / 2] << endl;
    else cout << a[n / 2 - 1] << endl;
}
