#include <bits/stdc++.h>
using namespace std;

int N, K, L, C, D, P, NL, NP;

int main() {
    scanf("%d %d %d %d %d %d %d %d", &N, &K, &L, &C, &D, &P, &NL, &NP);

    printf("%d\n", min((K * L) / NL, min(C * D, P / NP)) / N);
    return 0;
}
