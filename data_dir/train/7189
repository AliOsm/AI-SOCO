#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main()
{
    ios_base::sync_with_stdio(false);
    ll n, r=0; cin>>n;
    string s; cin>>s;
    for(int i=0;i<n;i++)
        if(s[i]%2==0)
            r+=i+1;
    cout<<r<<endl;
    return 0;
}
