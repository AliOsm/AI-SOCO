#include <bits/stdc++.h>
using namespace std;
string x,z;
char y[1005];
int main(){
    cin>>x>>z;
    for(int i=0;i<x.size();i++){
        if(x[i]<z[i]) {printf("-1"); return 0;}
        else y[i]=min(x[i],z[i]);
    }

    for(int i=0;i<x.size();i++) printf("%c",y[i]);
}
