#include<bits/stdc++.h>
#define int long long

using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int T;
    cin >> T;

    int a, b, c;
    while (T--)
        cin >> a >> b >> c, cout << (a + b + c) / 2 << '\n';

    return 0;
}
