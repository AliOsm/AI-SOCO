//#pragma GCC optimize("-O3")
#include <bits/stdc++.h>
#define ll long long
#define dd double
#define f first
#define s second
#define pb push_back
#define ios ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define y1 ibibibibbibibibiibigbabypanfewewf
using namespace std;
ll const N=5*1e5+5,inf=1e9+7;
ll m,n,l,k,r,ans,a,b,x,y,v,d;
pair <ll,ll> p[N];
string s,t,ss;
int  main()
{
    ios
    ans=1000;
    cin>>n>>k;
    for (int i=0;i<n;i++)
    {
        cin>>a;
        b+=a;
        k=k-min(b,8ll);
        b=b-min(b,8ll);
        x++;
        if (k<=0) break;
    }
    if (k>0) cout<<-1; else cout<<x;
    return 0;

}
