#include <bits/stdc++.h>
using namespace std;
int main() {
	int n, i, a, sum = 0;
	scanf("%d", &n);
	for(i = 0; i < n; i++) {
		scanf("%d", &a);
		sum += a;
	}
	if((sum % n)) n--;
	printf("%d\n", n);
	return 0;
}