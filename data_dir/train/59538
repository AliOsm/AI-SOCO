#include <bits/stdc++.h>

using namespace std;

int main(){
    long long n,m,a,b;
    cin>>n>>m>>a>>b;

    long long d=n/m;
    long long low=d*m;
    long long high=(d+1)*m;

    cout<<min((high-n)*a,(n-low)*b)<<endl;

    return 0;
}
