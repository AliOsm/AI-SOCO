#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define f(i, x, n) for(int i = x; i < (int)(n); ++i)

int main(){
	int n;
	scanf("%d", &n);
	set<int> st;
	f(i, 0, n){
		int t;
		scanf("%d", &t);
		if (t)st.insert(t);
	}
	printf("%d\n", (int)st.size());
}