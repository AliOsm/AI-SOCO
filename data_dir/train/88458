#include <bits/stdc++.h>

using namespace std;

int n, m;

int main()
{
	scanf("%d %d", &n, &m);
	if(!n && m)
		printf("Impossible\n"), exit(0);
	printf("%d %d\n", max(m, n), max(n + m - 1, n));
}