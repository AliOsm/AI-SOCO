#include <bits/stdc++.h>
using namespace std;

int T, N, V, L, R, H;

int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	cin>>T;
	
	for (int i=0;i<T;i++) {
		cin>>N>>V>>L>>R;
		H=N/V;
		H-=R/V;
		L--;
		H+=L/V;
		cout<<H<<'\n';
	}
	
	return 0;
}
