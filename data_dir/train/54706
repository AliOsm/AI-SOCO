#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int n , h;
    cin >> n >> h;
    double a=h/(2.0*n);
    double c=a;
    for(int i=0; i<n-1; i++)
    {
        if(i) cout << " " ;
        double  ans=sqrt(2.0*a*h);
        printf("%.12f",ans);
        a+=c;
    }
    cout << endl;
    return 0;
}
