#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    long long a, b, c;
    cin >> a >> b >> c;
    cout << 4 * (a * b + a * c + b * c) / sqrt(a * b * c);
    return 0;
}
