#include <bits/stdc++.h>

using namespace std;
int ans;
int main()
{
    int n;
    cin>>n;
    int c,x;
    for(int i=0;i<n;i++)
    {
        cin>>x>>c;
        if(c-x>=2)
            ans++;
    }
    cout<<ans;
}
