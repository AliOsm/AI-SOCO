#include <iostream>

using namespace std;

int main() {
	long long n, k;
	cin >> n >> k;
	int min = k == 0 || k == n ? 0 : 1;
	int max = n <= k * 3 ? n - k : k * 2;
	cout << min << ' ' << max << endl;
	return 0;
}
