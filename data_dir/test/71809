#include <bits/stdc++.h>
using namespace std;
int l,r,a;
int main(){
    scanf("%d %d %d",&l,&r,&a);
    while(a>0){
        if(l>=r) r++;
        else if(r>l) l++;
        a--;
    }
    printf("%d",min(l,r)*2);
    return 0;
}
