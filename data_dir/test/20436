#include <bits/stdc++.h>

using namespace std;
 
int main() {
  
    string a,b,x;
    cin>>a>>b>>x;
  a+=b;
  sort(a.begin(),a.end());
   sort(x.begin(),x.end());
   
        if(x!=a)
        {
            cout<<"NO";
            return 0;
        }
    
    cout<<"YES";
}