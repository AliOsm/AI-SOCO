#include <bits/stdc++.h>//Nurstan
using namespace std;

inline void boost(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
}

int main(){
	boost();
	int x, y, a, b;
	cin >> x >> y >> a >> b;
	int k = x * y / __gcd(x, y);
	cout << b / k - (a - 1) / k;
}

