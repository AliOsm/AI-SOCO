#include <bits/stdc++.h>
using namespace std;

int w, h, k, ans;

int main() {
	cin >> w >> h >> k;
	for (int i = 0; i < k; ++i) {
		ans += 2 * ((w - 4 * i) + (h - 4 * i)) - 4;
	}
	cout << ans;
	return 0;
}