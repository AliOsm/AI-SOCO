#include<iostream>
#include<set>
#include<cmath>
#include <algorithm>
#include <vector>
using namespace std;
int main()
{
	int kaz,n,k,s,p;
	cin>>k>>n>>s>>p;
	if(n%s==0)
	kaz=n/s;
	else
	kaz=n/s+1;
	int r=k*kaz;
    if(r%p==0)
    cout<<r/p;
    else
    cout<<r/p+1;
}

