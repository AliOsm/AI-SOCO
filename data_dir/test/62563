#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int m = n, ans = 1;
    while (n >= 10) {
        ans *= 10;
        n /= 10;
    }
    ans *= (n + 1);
    cout << ans - m;
    return 0;
}
