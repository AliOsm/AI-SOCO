#include <bits/stdc++.h>
using namespace std;

string S;

int main() {

    ios::sync_with_stdio(0);
    cin.tie(NULL), cout.tie(NULL);

    int N;
    cin >> N >> S;
    
    for(int i=0, j=1; i<S.size(); i+= j, j++)
        cout << S[i];
    cout << "\n";
    
    return 0;
}