#include <bits/stdc++.h>

using namespace std;

int main()
{
   int x1,y1,x2,y2,x,y;
   cin >> x1 >> y1 >> x2 >> y2 >> x >> y;
   if(((x2-x1)%x)==0 && ((y2-y1)%y)==0 && ((x2-x1) /x + (y2-y1) / y) % 2 ==0)
    cout << "YES\n";
   else
    cout << "NO\n";
    return 0;
}
