#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main ()
{
    ios_base::sync_with_stdio(false);
    string a, b; cin >> a >> b;
    if(a == b) cout << a << endl;
    else cout << 1 << endl;
    return 0;
}
