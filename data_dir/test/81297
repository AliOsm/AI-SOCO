#include <bits/stdc++.h>

using namespace std;

int cnt[4];

int main() {
	for (int i = 0; i < 4; i++)
		scanf("%d", cnt + i);
	puts(cnt[0] == cnt[3] && (cnt[0] || !cnt[2]) ? "1" : "0");
}
