#include <bits/stdc++.h>

using namespace std;

int n, m, k;

int main () {
    cin >> n >> m;
    k = 0;
    while (n > 0){
        k++;
        n--;
        if (k % m == 0) {
          ++n;
        }
    }
    cout << k;
}