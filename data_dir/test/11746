#include "cstdio"

using namespace std;

int main()
{
    int t, n, a, b, c, d;
    scanf("%d", &t);
    while (t--) {
        scanf("%d%d%d%d%d", &n, &a, &b, &c, &d);
        printf("%s\n", ((a + b) * n < c - d) || ((a - b) * n > c + d) ? "No" : "Yes");
    }
    return 0;
}
