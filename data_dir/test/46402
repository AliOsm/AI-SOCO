#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n, m;
    cin >> n >> m;
    long long ans = 0;
    for(int i = 1; i <= n; i++) {
        ans += (i % 5 + m) / 5;
    }
    cout << ans << endl;
    return 0;
}