#include <bits/stdc++.h>
using namespace std;

const int MAXN = 405;

int main()
{
	int n, k;

	scanf("%d%d", &n, &k);
	printf("%d\n", (2 * n + k - 1) / k + (5 * n + k - 1) / k + (8 * n + k - 1) / k);

	return 0;
}
