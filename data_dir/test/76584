#include<iostream>
using namespace std;
int n,a[1001];
int main()
{
        cin >> n;
        for(int i=1; i<=n; i++) cin >> a[i];
        for(int i=1; i<=n; i++) a[i] -= abs(a[i] % 2 - 1);
        for(int i=1; i<=n; i++) cout << a[i] << " ";
}