#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int main(){
	int n,x=0,y=0;
	string a;
	cin>>n>>a;
	for(int i=0;i<n;i++){
		if(a[i]=='A')
			x++;
		else
			y++;
	}
	if(x==y)
		cout<<"Friendship";
	else if(x>y)
		cout<<"Anton";
	else
		cout<<"Danik";
}