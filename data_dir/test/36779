#include <bits/stdc++.h>
using namespace std;

int getTime() {
    int h, m;
    scanf("%d:%d", &h, &m);
    return h * 60 + m;
}

int main() {
    int a = getTime();
    int b = getTime();
    int c = (a + b) / 2;
    printf("%02d:%02d\n", c / 60, c % 60);
    return 0;
}
