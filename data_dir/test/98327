#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int n, k;
ll a;

int main()
{
	cin >> n >> k;
	a = (ll)(n + k) * 2;
	for (int i = 0; ; i++)
	{
		if ((ll)i * (i + 3) == a)
		{
			cout << n - i;
			return 0;
		}
	}
	return 0;
}
