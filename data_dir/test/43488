#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,i,d,t,res=0,check=0;
    scanf("%d%d",&n,&t);
    for(i=0;i<n;i++){
        scanf("%d",&d);
        check+=d;
        if(i<n-1)res+=10;

    }
    if(check+res<=t){
        cout<<(t-check)/5<<endl;
    }
    else cout<<-1<<endl;
    return 0;
}
