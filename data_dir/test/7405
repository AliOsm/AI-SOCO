#include <bits/stdc++.h>

#define ll long long
#define fr first
#define se second
#define pb push_back

using namespace std;

const long long N = 1e3 + 7;

ll res, n, m, cnt, ans , a , b , sum , mx, mn, c , d;
string s;
map<int , int>mp;
vector<int>g;
int main()
{
    ll arr[3];
    cin >> arr[0] >> arr[1] >> arr[2];
    sort( arr , arr + 3 );
    if(2 * (arr[0] + arr[1]) < arr[2])
    return cout << arr[0] + arr[1] , 0;
    cout << (arr[0] + arr[1] + arr[2]) / 3;
}
