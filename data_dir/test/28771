// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    long n, k;
    cin >> n >> k;
    long p = n/(2*k+2);
    cout << p << ' ' << p*k << ' ' << n - p*(k+1);
    return 0;
}
