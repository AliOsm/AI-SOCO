#include<bits/stdc++.h>
using namespace std;

int main()
{
	ifstream cin("input.txt");
	ofstream cout("output.txt");
	ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	int a,b,x;
	cin>>x;
	for (int i=0;i<3;i++)
	{
		cin>>a>>b;
		if (x==a) x=b;
		else if (x==b) x=a;
	}
	cout<<x;
	return 0;
}
