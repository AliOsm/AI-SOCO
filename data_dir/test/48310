#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
using namespace std;
int main()
{
	int n, h;
	cin >> n >> h;
	int ans=0,a;
	for (int i = 0; i < n; i++){
		cin >> a;
		if (a>h)ans += 2;
		else ans++;
	}
	cout << ans;

	return 0;
}