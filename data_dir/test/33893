#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {

   ll n;
   
   cin >> n;
   
   ll x = n * (n + 1) * (n + 2) * (n + 3) * (n + 4) / 120;
   ll y = n * (n + 1) * (n + 2) / 6;
   
   cout << x * y;
   
   return 0;
}