#include <iostream>

using namespace std;

int main()
{
    long long x;
    cin >> x;
    if (!x)
        return cout << 0, 0;
    x++;
    if (x % 2)
        cout << x;
    else
        cout << x / 2;
    return 0;
}