#include <bits/stdc++.h>

using namespace std;

string s;

int main()
{
    // TOO SAD :'(
	cin >> s;
	int ans=(s.size()+1)/2;
	if(s=="1"+string(s.size()-1,'0') && s.size()%2==1)
		ans--;
	if(s=="0") ans--;
	cout << ans << endl;

}