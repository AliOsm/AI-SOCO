#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int main()
{
    int N, M, A;
    cin >> N >> M >> A;

    long long X = N / A + (N%A != 0);
    long long Y = M / A + (M%A != 0);

    cout << X*Y;

}
