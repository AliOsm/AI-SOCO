#include<bits/stdc++.h>
#define fi first
#define se second
#define FOR(a) for(int i=0;i<a;i++)
using namespace std;

typedef long long ll;
typedef pair<int,int> P;
typedef pair<P,int> LP;
const int inf=0x3f3f3f3f;
const int N=2e5+10;

vector<int> v[N];
map<string,int> mp;
int n,m,k,b[N],a[N],f[N],num[N];
int did[N];
string s;
int s1,s2,cnt,ans,flag,tot,t,sum;


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin>>n;
    FOR(n) cin>>a[i];
    for(int i=n-1;i>=0;i--)
    {

        did[a[i]]++;
        if(did[a[i]]==1) {cnt++;b[i]=a[i];}
    }

    cout<<cnt<<endl;
    FOR(n)
    {
        if(b[i]!=0) cout<<b[i]<<" ";
    }




}
