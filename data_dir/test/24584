#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int q;
	cin >> q;
	while(q --> 0) {
		int l, r, d;
		cin >> l >> r >> d;
		cout << (l <= d ? r + d - (r % d) : d) << endl;
	}
	return 0;
}