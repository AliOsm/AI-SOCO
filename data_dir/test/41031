#include<bits/stdc++.h>

using namespace std;

int main () {
	string a,b;
	cin>>a>>b;
	int q=0,w=0;
	for(int i=0;i<a.size();i++){
		if(a[i]!=b[i]){
			if(a[i]=='4'){
				q++;
			}
			else w++;
		}
	}
	cout<<min(q,w)+max(q,w)-min(q,w);
	return 0;
}