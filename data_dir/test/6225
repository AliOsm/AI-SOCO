#include <bits/stdc++.h>
using namespace std ;

int main () {

    long long x , d , a , b ;
    cin >> x >> d >> a >> b ;
    long long ans = 1e18 ;
    ans = min(ans , (x - (x/d)*d)*b) ;
    ans = min(ans , ( ((x+d-1)/d)*d - x ) * a ) ;
    cout << ans << endl;
}
