#include <bits/stdc++.h>
using namespace std;

int n, mx=-1;

int main(){

    scanf("%d",&n);

    for(int i=0, act; i<n; i++){
        scanf("%d",&act);
        mx = max(mx,act);
    }

    printf("%d\n",max(0,mx-25));

    return 0;
}
