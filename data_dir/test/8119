#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n,ans=0,p=1;
    cin>>n;
    while(n>=p)
        n-=p,p<<=1,ans++;
    if(n)
        ans++;
    cout<<ans<<endl;
    return 0;
}
