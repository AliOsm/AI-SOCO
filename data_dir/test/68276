#include <bits/stdc++.h>
using namespace std;

signed main(){
  ios::sync_with_stdio( 0 );
  int n, a, b; cin >> n >> a >> b;
  for( int i = 0; i < n; ++i ){
    int x; cin >> x;
    cout << 1LL * x * a % b / a << " \n"[ i + 1 == n ];
  }
  return 0;
}
