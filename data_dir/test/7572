#include <bits/stdc++.h>
using namespace std;
string str;
int main() {
	int n, i;
	cin >> n >> str;
	for(i = 0; i < n; i++)
		if(!(n % (i + 1))) reverse(str.begin(), str.begin() + i + 1);
	cout << str << "\n";
	return 0;
}