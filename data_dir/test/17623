#include "bits/stdc++.h"
using namespace std;
const int N = 5000;

char get(int x) {
	return (char) (x + 'a');
}

int main() {
	int n;
	cin >> n;
	long long p = 0;
	while(n--) {
		int x;
		cin >> x;
		if(x >= 0) {
			p += x;
		} else {
			p += -x;
		}
	}
	cout << p ;
	return 0;
}