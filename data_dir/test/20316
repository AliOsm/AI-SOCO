#include <iostream>
#include <math.h>
#include <cstring>
#include <memory.h>
#include <algorithm>

using namespace std;

int main(void) {
	string a,b,c;
	bool z;
	cin >> a >> b >> c;
	a=a+b;
	sort(a.begin(),a.end());
	sort(c.begin(),c.end());	
    if (a==c) cout << "YES";
	else cout << "NO";
}
