#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main () {
	int x,y,z,t1,t2,t3;
	cin >> x >> y >> z >> t1 >> t2 >> t3;

	int ans = abs(x-y)*t1;
	cout << (ans  >= t3*3 + abs(x-z)*t2+ abs(x-y)*t2 ? "YES" : "NO") << endl;
}
