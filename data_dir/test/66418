#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long N;
    scanf("%lld", &N);

    long long x = 2, diff = 0;

    for(long long i=1; i<=N; i++)
    {
        long long y = i*(i+1);
        printf("%lld\n", y/i*y-x/i);
        x = y;
    }

    return 0;
}
