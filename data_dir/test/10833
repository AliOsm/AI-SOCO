#include <bits/stdc++.h>
using namespace std;
long long int n;
int main ()
{
    #ifndef ONLINE_JUDGE 
		freopen (".in", "r", stdin); 
    #endif 
    scanf ("%I64d", &n);
    if (n % 2 == 0)
    	cout << n / 2;
    else
    	cout << -(n + 1) / 2;
	return 0;
}            