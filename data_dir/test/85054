#include <bits/stdc++.h>

using namespace std;

#define ll long long int

int main()
{
    ll n,i,k,j;

    cin>>n>>k;

    for(i=1; i<=k; i++)
    {
        if(n%10==0)
        {
            n/=10;
        }

        else n--;
    }

    cout<<n<<endl;
}
