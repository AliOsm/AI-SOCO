#include<iostream>
using namespace std;
int main(){
	
	string s;
	int a , b , ans = 0;
	cin >> s;
	
	b = s[s.size() - 1] - '0';
	a = b;
	if(s.size() > 1)	a += (s[s.size() - 2] - '0')*10;
	a%=4;
	
	if(a == 0)	cout << "4";
	else	cout << "0";
	
}