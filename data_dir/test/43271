#include <bits/stdc++.h>

using namespace std;
    
int main(){
	double d;
	int n,t,sum=0;
	cin >> n;
	for (int i=0;i<n;i++)
		{
			cin >> t;
			sum+=t;
		}
	cout.precision(12);
	cout << (double)sum/n;
	return 0;	
}
