#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main()
{
    ios_base::sync_with_stdio(false);
    int q; cin >> q;
    while(q--) {
        ll k, x; cin >> k >> x;
        cout << 9*(k-1) + x << endl;
    }
    return 0;
}
