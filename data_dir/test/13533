#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int main() {
    string a, b;
    while ( cin >> a >> b ) {
        cout << ( a == b ? -1 : max<int>(a.size(), b.size())) << endl;
    }
    return 0;
}