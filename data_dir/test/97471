#include <iostream>
#include <stdlib.h>
using namespace std;

int main(){
	int a, b, s;
	cin >> a >> b >> s;
	if(abs(a) + abs(b) == s  || ((s - (abs(a) + abs(b))) % 2 == 0 && s > abs(a) + abs(b))) cout << "Yes" << endl;
	else cout << "No" << endl;
	return 0;
}