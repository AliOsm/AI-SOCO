#include <iostream>
#include <cmath>

using namespace std;
typedef long long ll;

string a,b;

int main() {
  cin >> a >> b;
  if(a == b) cout << a << "\n";
  else cout << "1\n";
  return 0;
}