#include "bits/stdc++.h"
using namespace std;

const int N = 2e5 + 5;


void getAC() {
	ios_base :: sync_with_stdio(false);
	cin.tie(NULL);

	int t; cin >> t;
	while(t--) {
		int x; cin >> x;
		cout << 1 << ' ' << (x - 1) << '\n';
	}
}
 
int main() {
	getAC();
	return 0;
}