/*
for youuuu meme :p
    */

#include<bits/stdc++.h>
 using namespace std;

 typedef long long ll;

 /*ll dist (ll  x1 , ll x2 )
  {
      if( x1 >= x2 )
                return x1 - x2 ;
      return x2 - x1 ;
  }

  ll TREV ( ll ARR[ ] , ll N , ll ind )
  {
     // ll SUM = 0 ;
     // for( int i = 0 ; i < N ; i++ )
          //      SUM += dist( ARR[ i ] , ARR[ ind ] );
   //   return SUM ;
      return dist ( ARR[ 0 ] , ARR[ ind ] ) + dist ( ARR[ ind ] , ARR[ N - 1 ] );
  }*/

 int main ( )
        {
            ll N , Z = 0 ;
            cin >> N ;
            ll ARR[ N ] , A [ N ];
            for( int i = 0 ; i < N ; i++ )
                            cin>> A[ i ] ;

            sort( A , A + N ) ;
            cout<< A[ ( N - 1 ) / 2 ] <<endl; 
           /* ll j = 1 ;
            ARR [ 0 ] = A [ 0 ] ;
            for (int i = 1 ; i < N ; i++ )
                        if( A[ i ] != A[ i - 1 ] )
                                    ARR [ j++ ] = A[ i ] ;
            N = j ;
            ll TMP = TREV ( ARR , N , N / 2 ) ;
            j = N / 2 ;
            Z++ ;
            ll TMP1 = TREV ( ARR , N , N / 2 - Z ) ;
            bool OK = false ;

            cout<<TREV(ARR,N,N/2)<<endl<<TREV(ARR,N,N/2-1)<<endl<<TREV(ARR,N,N/2-2)<<endl;;
            if( TMP >= TMP1 )
                    cout<< ARR[ N / 2 - 1 ] << endl ;
            else
                    cout<<ARR[ N / 2 ] << endl ;*/

            return 0;
        }
