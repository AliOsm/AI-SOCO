#include <bits/stdc++.h>

using namespace std;

int mid(int a, int b) {
	return a + (b - a) / 2;
}

int main() {
	int tc;
	scanf("%d", &tc);
	while (tc--) {
		int a, b, c, d;
		scanf("%d %d %d %d", &a, &b, &c, &d);
		printf("%d %d %d\n", b, c, c);
	}
}
