//1077A
#include <iostream>

long long t, x, a, b, k;
int main() {
	std::cin >> t;
	for (int i = 0; i < t; ++i) {
		std::cin >> a >> b >> k;
		std::cout << ( (a - b)*(k / 2) + a * (k % 2) ) << "\n";
	}
}