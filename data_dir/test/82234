
#include <bits/stdc++.h>
#define lli long long int
using namespace std;


int main()
{
	ios_base::sync_with_stdio(false);
	
	int n;
	cin>>n;
	bool one = false;
	for (int i=0,x ; i<n ; i++) { cin>>x; one|=(x==1); }
	
	cout<<(one ? "-1" : "1");
	
	return 0;
}