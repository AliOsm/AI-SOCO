#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    string ans = "";
    for(int i = 0;i < n;i++){
        if(i % 4 < 2){
            ans += 'a';
        }
        else ans += 'b';
    }
    cout << ans;
}
