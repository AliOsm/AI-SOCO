#include <bits/stdc++.h>
#define ll long long
using namespace std;
string s;
int n;
int main()
{
    cin>>n;
    while(n--)
    {
        cin>>s;
        string k;
        for(int i=s.size()-1;i>=0;i--)
        {
            k+=s[i];
            if(k=="op")cout<< "FILIPINO";
            else if(k=="used"||k=="usam")cout<< "JAPANESE";
            else if(k=="adinm")cout<<"KOREAN";
            else continue;
            break;
        }
        cout<<"\n";
    }
}
