#include <bits/stdc++.h>
using namespace std;

int n, k;

int main() {
    ios::sync_with_stdio(false), cin.tie(0);
	cin >> n >> k;
	int z = (n - k) >> 1;
	z++;
	for (int i = 0; i < n; i++)
		cout << ((i % z) == z - 1);
    return 0;
}
