#include <bits/stdc++.h>
using namespace std;

int N, D, Q, X, Y;

int main() {
    cin >> N >> D;

    cin >> Q;

    while (Q--) {
        cin >> X >> Y;

        cout << ((X + Y >= D && X + Y <= (2 * N - D) && (Y - X) <= D && (X - Y) <= D) ? "YES" : "NO") << endl;
    }
}
