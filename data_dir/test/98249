#include <bits/stdc++.h>
using namespace std;

int N, X;
set <int> S;

int main() {
    scanf("%d", &N);

    while(N--) {
        scanf("%d", &X);
        if(X) S.insert(X);
    }

    printf("%d\n", S.size());

    return 0;
}
