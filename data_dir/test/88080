#include<iostream>
#include<cmath>

using namespace std;

int main()
{
	int x, y, n;
	cin >> x >> y >> n;
	x = abs(x), y = abs(y);
	if(x + y > n || (x + y) % 2 != n % 2)
		cout << "No" << endl;
	else
		cout << "Yes" << endl;
	return 0;
}
