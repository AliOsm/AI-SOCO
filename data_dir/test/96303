#include<bits/stdc++.h>
using namespace std;
#define forr(i,a,n)	for(long long int i=a; i<n; i++)
#define loop(i,a,n)	for(long long int i=a; i>=n; i--)

int main() 
{
	ios_base :: sync_with_stdio(false),cin.tie(NULL),cout.tie(0);
	
	double a,b;
	cin>>a>>b;
	double x=a/2.0;
	if(b<=x)
		cout<<min(int(a),int(b+1));
	else
		cout<<max(1,int(b-1));
	
	return 0;
}