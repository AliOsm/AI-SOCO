#include<bits/stdc++.h>
#define LL long long
#define fi first
#define sc second
#define pb push_back
using namespace std;

int main() {
	string s;
	int ans = 0;
	cin>>s>>s;
	for(int i = 0; i < s.size(); i++) {
		if(s[i] % 2 == 0) ans += i + 1;
	}
	cout<<ans<<endl;
	return 0;
}
