#include <bits/stdc++.h>
#define ll long long
using namespace std;

ll n, m, s = 0, av, c;
int main(){
    cin >> n >> m;
    for(ll i = 1; i <= n; i++){
        s += (m + i % 5) / 5;
    }
    cout << s;
    return 0;
}
