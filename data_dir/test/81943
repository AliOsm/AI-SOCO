#include <bits/stdc++.h>
using namespace std;

int main() {
	int a, b, c, d;
	cin >> a >> b >> c >> d;
	if ( b - 1 <= c and c <= ( b + 1 ) * 2 ) {
		cout << "YES\n";
	} else if ( a - 1 <= d and d <= ( a + 1 ) * 2 ) {
		cout << "YES\n";
	} else {
		cout << "NO\n";
	}
	return 0;
}