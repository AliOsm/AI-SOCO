#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	long long n;
	cin >> n;

	long long k = floor ((-1.0 + (sqrt (8.0 * n + 1.0))) / 2.0);
	
	n -= (k * (k + 1) / 2);

	cout << (n != 0 ? n : k);
}
