#include<bits/stdc++.h>
#define ll long long
#define pb push_back
using namespace std;

ll n,k,x=1,d=-1;

main()
{
    cin>>n>>k;
    if(k == 1){cout<<n;return 0;}
    while(x <= n)
        x *= 2;
    cout<<x-1;
}
