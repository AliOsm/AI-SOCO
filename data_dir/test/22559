#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, cur = 0, ans = 0;
  cin >> n;
  while (n--) {
    int x, y;
    cin >> x >> y;
    cur += y - x;
    ans = max(ans, cur);
  }
  cout << ans;
}