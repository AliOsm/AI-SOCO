#include <bits/stdc++.h>
using namespace std;

int n, a, b, i, h[2020];

int main() {
	cin >> n >> a >> b;
	for (i = 0; i < n; ++i) cin >> h[i];
	sort(h, h + n);
	cout << h[n - a] - h[n - a - 1];
	return 0;
}