#include <iostream>

using namespace std;

int main() {
	int n, k, s = 0;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		int a;
		cin >> a;
		s += a;
	}
	int m = n * (2 * k - 1) - s * 2;
	if (m < 0)
		m = 0;
	cout << m << endl;
	return 0;
}
