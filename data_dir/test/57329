#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n ;
    cin >> n ;
    set < pair < string , string > > st ;
    for ( int i = 1; i <= n ; i ++ ){
        string b , c ;
        cin >> b >> c ;
        st.insert( { b , c } );
    }
    cout << st.size();
}
