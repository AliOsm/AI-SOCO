#include "cstdio"
#include "algorithm"
using namespace std;
int x[3];
int main() {
    for (int i=0; i<3; i++)
    scanf("%d", &x[i]);
    sort(x, x+3);
    printf("%d", x[2]-x[0]);
    return 0;
}