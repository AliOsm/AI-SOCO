//In the name of Allah

#include<bits/stdc++.h>

using namespace std;

string s1, s2;

int main()
{
    cin >> s1 >> s2;
    if(s1 != s2)
        cout << 1 << endl;
    else
        cout << s1 << endl;
    return 0;
}
