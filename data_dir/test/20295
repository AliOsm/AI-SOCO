#include <bits/stdc++.h>
using namespace std;

int main(){
	int n; cin >> n;
    string s; cin >> s;
    int cnt = 0;
    for(int i = 0; i < n; i++)
        if(i+1 < n and s[i] != s[i+1])
            cnt++, i++;
    cout << n-cnt << endl;
	return 0;
}
