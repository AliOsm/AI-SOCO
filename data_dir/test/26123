#include <bits/stdc++.h>
using namespace std;

int n, ans, x1, y1, x2, y2;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x1 >> y1 >> x2 >> y2;
        int a = abs(y1 - y2) + 1, b = abs(x1 - x2) + 1;
        ans += a * b;
    }
    cout << ans;
}