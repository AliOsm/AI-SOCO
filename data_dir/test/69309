#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const ll mod = 1e9 + 7;

int n, k;

int main() {
    cin >> n >> k;
    if (n == k || k == 0) {
        cout << "0 0";
        return 0;
    }
    cout << "1 " << min(n - k, k * 2);
}
