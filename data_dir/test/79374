#include<bits/stdc++.h>
using namespace std;
int main(){
  int y, x, n;
  cin >> n >> x >> y;
  string s; cin >> s;
  int ans = 0;
  for (int i = 0; i < x; ++i) {
    if (s[n - 1 - i] - '0' != (y == i)) ++ans;
  }
  cout << ans << '\n';
}
