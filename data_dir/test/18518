#include<bits/stdc++.h>
using namespace std;
typedef long long int uli;
int main(){
  string s;
  cin>>s;
  int i=0;
  while(i<int(s.size()) && s[i]=='0')i++;
  int z=0;
  for(int j=i;j<int(s.size());j++)z+=(s[j]=='0');
  if(z>=6)puts("yes");
  else puts("no");
  return 0;
}
