#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main()
{
    int n , k , ans = INT_MAX , i , p;
    scanf("%d %d",&n,&k);
    for(i = 0 ; i < n ; i++)
    {
        scanf("%d",&p);
        if(k%p == 0)
        {
            ans = min(ans,k/p);
        }
    }
    printf("%d\n",ans);


    return 0;
}

