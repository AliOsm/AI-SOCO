#include <bits/stdc++.h>

using namespace std;

int main()
{
    string A, B;    cin >> A >> B;
    string Pile;    cin >> Pile;

    A += B;

    sort(A.begin(), A.end());
    sort(Pile.begin(), Pile.end());

    cout << (A == Pile ? "YES\n" : "NO\n");

    return 0;
}
