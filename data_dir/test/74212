#include <bits/stdc++.h>
using namespace std;

int N, S, X;

int main() {
    cin >> N;
    
    for(int i=0;i<N;i++) {
        cin >> X;
        S += X;
    }

    cout << ((S % N) ? (N - 1) : (N)) << endl;
}