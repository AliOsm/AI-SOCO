#include <bits/stdc++.h>
using namespace std;
long long n,ans,a[100005];
main ()
{
    cin>>n;
    
    for (int i=1; i<=n; i++)
        cin>>a[i];
        
    sort(a+1,a+n+1);
    
    for (int i=1; i<=n; i++)
        if (a[i]>ans) ans++; 
    
    cout<<++ans<<endl;
}