#include <bits/stdc++.h>

using namespace std;

int a, b, c;
int x, y, z;

int main() {
    cin >> x >> y >> z;
    cin >> a >> b >> c;
    cout << ((x <= a && x + y <= a + b && x + y + z <= a + b + c) ? "YES" : "NO");
}