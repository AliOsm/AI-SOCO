#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
using LL = long long;

int main() {
    int n = 0;
    scanf("%d", &n);
    printf(n % 2 == 1 ? "Ehab\n" : "Mahmoud\n");
    return 0;
}
