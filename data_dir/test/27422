#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    int n;
    cin >> n;

    if (n % 3 == 1) {
        --n;
    }
    if (n % 3 == 2) {
        ++n;
    }
    n /= 3;
    cout << n / 12 << " " << n % 12 << endl;
    return 0;
}
