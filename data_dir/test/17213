#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, h;
    cin >> n >> h;
    cout << setprecision(10) << fixed;
    for (int i = 1; i < n; ++i) {
        cout << h * sqrt((double)i / n) << ' ';
    }
    cout << '\n';
}