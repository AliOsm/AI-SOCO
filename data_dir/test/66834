#include <stdio.h>
using namespace std;

int main(int argc, char **argv)
{
    int a, b, c, x, n, cnt = 0;
    scanf("%d%d%d%d", &a, &b, &c, &n);
    while (n--) {
        scanf("%d", &x);
        if ((x > b) && (x < c)) {
            cnt++;
        }
    }
    printf("%d", cnt);
}
