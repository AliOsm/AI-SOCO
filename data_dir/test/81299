#include <bits/stdc++.h>
using namespace std;

int n, i;
char s[101];

int main() {
	cin >> n >> s;
	for (i = 1; i <= n; ++i) {
		if (n % i == 0) {
			reverse(s, s + i);
		}
	}
	cout << s << endl;
	return 0;
}