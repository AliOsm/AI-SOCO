#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main ()
{
    ios_base::sync_with_stdio(false);
    int n, a; cin >> n;
    map<int,int> x;
    for(int i=1;i<=n;i++) cin >> a, x[a]=i;
    for(int i=1;i<=n;i++) cout << x[i] << " ";
    return 0;
}
