#include<bits/stdc++.h>
using namespace std;
long long a,s,d,f,g,h,j,k,l,i,n,m;
string x;

main(){
    cin>>n;
k=1000000;
    for(i=0;i<n;i++){
        cin>>a;
        if(a<=500000) l=max(l,a);
        if(a>500000) k=min(k,a);
    }

    cout<<max(l-1,(1000000-k));
}
