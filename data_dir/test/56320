#include <bits/stdc++.h>
using namespace std;

int main(){
  int n,tmp,odd=0;
  scanf("%d",&n);
  for(int i=0;i<n;i++){
    scanf("%d",&tmp);
    odd+= tmp % 2;
  }
  if(odd % 2 || odd != 0)puts("First");
  else puts("Second");
  return 0;
}