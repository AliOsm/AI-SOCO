#include <bits/stdc++.h>
int main() {
    std::string s, t;
    while (std::cin >> s >> t) {
        std::string a;
        for (int i = 0; i < (int)s.size(); ++i) {
            a.push_back('0' + ((s[i] - '0') ^ (t[i] - '0')));
        }
        std::cout << a << std::endl;
    }
}