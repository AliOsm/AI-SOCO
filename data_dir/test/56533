//besmellah
#include <bits/stdc++.h>
using namespace std;

int main() {
	int l, r, a;
	cin >> l >> r >> a;
	if (l > r)
		swap(l, r);
	int x = min(r - l, a);
	l += x;
	a -= x;
	cout << (l + a / 2) * 2;
}
