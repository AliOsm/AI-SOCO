#include<iostream>
#include<algorithm>
#define ll long long
using namespace std;
int main()
{
	int n;
	cin >> n;
	ll a, tot = 0;
	for (int i = 0; i < n; i++)
	{
		cin >> a;
		tot += ((a - 1)*i) + a;
	}
	cout << tot;
	return 0;
}