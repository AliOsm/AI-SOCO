#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, m, i, sum = 0, s = 0, s1 = 0;

    cin >> n >> m;

    for(i = 1; i <= n; i++){
        if(i % m == 0){
            n++;
        }
        sum++;
    }

    cout << sum << endl;

    return 0;
}
