#include <iostream>
#include <cstdio>
#include <set>
#include <queue>
#include <algorithm>
#include <cmath>
using namespace std;

vector<int> g[26];

int main() {
    int n;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) {
            printf("%c", char('a' + (i % 4)));
    }
                                         
    
    return 0;
}
