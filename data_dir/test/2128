#include <bits/stdc++.h>
using namespace std;

int a, b, c;
long long all;

int main(){
    scanf("%d %d %d", &a, &b, &c);
    all = (c * (a - b) + b - 1) / b;
    printf("%lld\n", all);
    return 0;
}
