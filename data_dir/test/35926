#include<bits/stdc++.h>

using namespace std;

typedef int ll;

int main()
{
    string a,b;
    cin>>a>>b;
    int n;
    cin>>n;
    cout<<a<<" "<<b<<endl;
    string x,y;
    while(n--)
    {
        cin>>x>>y;
        if(x==a)
            a=y;
        else
            b=y;
        cout<<a<<" "<<b<<endl;
    }
    return 0;
}
