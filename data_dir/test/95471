#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e5 + 7;

set<int> st;

int main()
{
	ios::sync_with_stdio(false);

	int n;
	cin >> n;
	for (int i=0; i<n; i++)
	{
		int x;
		cin >> x;
		if (x <= n)
			st.insert(x);
	}
	cout << n-st.size() << "\n";

	return 0;
}
