#include <bits/stdc++.h>
using namespace std;

// why am I so weak

char s[155];
char t[155];

int main() {
	scanf("%s", s);
	scanf("%s", t);

	for (int i = 0; i < strlen(s); i++) {
		printf("%d", (s[i] - '0') ^ (t[i] - '0'));
	}

	puts("");

	return 0;
}

