

#include <bits/stdc++.h>
#include <iomanip>
using namespace std;

int main () {
	int n, k; scanf("%d %d", &n, &k);
	int Min = min(min(1, k), n - k);
	int Max = min(n - k, k * 2);
	cout << Min << ' ' << Max << endl;
	return 0;
}
