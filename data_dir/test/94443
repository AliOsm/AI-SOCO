#include <bits/stdc++.h>
using namespace std;

int n, k, ans, cnt[] = { 2, 5, 8 };

int main() {
	cin >> n >> k;
	for (int i = 0; i < 3; ++i) {
		ans += (n * cnt[i] + k - 1) / k;
	}
	cout << ans;
	return 0;
}