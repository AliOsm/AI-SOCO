#include "bits/stdc++.h"
using namespace std;

int main(){

	long long len;
	cin >> len;

	long long ans = 0;
	char prev = '+';

	for(long long i=0;i<len;i++){
		char ch;
		cin >> ch;

		if(ch == prev){
			ans++;
		}
		prev = ch;
	}

	cout << ans << endl;
}