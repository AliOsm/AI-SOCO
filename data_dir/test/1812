#include <bits/stdc++.h>

using namespace std;

bool b[11];
int a[11];

int main()
{
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < m; i++)
    {
        int x;
        scanf("%d", &x);
        b[x] = true;
    }
    for (int i = 0; i < n; i++)
    {
        if (b[a[i]])
        {
            printf("%d ", a[i]);
        }
    }
    return 0;
}
