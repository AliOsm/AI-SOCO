#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int n, m;

int main() {
	cin >> n >> m;
	if (m == 0) cout << 1 << endl;
	else cout << min(n/2, m) - max(0, m-(n+1)/2);
}
