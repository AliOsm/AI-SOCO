#include<bits/stdc++.h>

using namespace std;

typedef long long ll;


int main() {
    int t;
    cin>>t;
    while(t--){
        string s;
        cin>>s;
        reverse(s.begin(),s.end());
        if(s[0]=='o')
            cout<<"FILIPINO"<<endl;
        else if(s[0]=='a')
            cout<<"KOREAN"<<endl;
        else
            cout<<"JAPANESE"<<endl;
    }
    return 0;
}
