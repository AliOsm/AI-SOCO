#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
const int maxn = 5e5;

int n, m;
int q = 1;
int main(){
  cin >> n >> m;
  if(n > 28){
    cout << m;
  }
  else {
    q = pow(2, n);
    cout << m % q;
  }
}
