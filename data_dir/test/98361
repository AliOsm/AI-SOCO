#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    while(c >= 0) {
        if(c % b == 0) {
            printf("Yes\n");
            return 0;
        }
        c -= a;
    }
    printf("No\n");
    return 0;
}
