#include <bits/stdc++.h>

#define llong long long
#define pb push_back
#define over999 1234567890
#define elif else if
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using namespace std;

string s;
llong x, y, a[100005], b[100005], n;

int main()
{
    cin >> n;
    for(int i=0;i<n;i++)
    {
        cin >> a[i];
    }
    
    for(int i=n-1;i>=0;i--)
    {
        b[i]=a[i]+a[i+1];
    }
    
    for(int i=0;i<n;i++)
    {
        cout << b[i] << " ";
    }
    // cin >> s;
    
    // for(int i=0;i<s.length();i++)
    // {
    //     if(s[i]=='L')x--;
    //         elif(s[i]=='R')x++;
    //             elif(s[i]=='U')y++;
    //                 else y--;
    // }
    // if((x+y)&1)return cout << -1, 0;
    
    // cout << (x+y)/2;
    return 0;
}
//googlegoose