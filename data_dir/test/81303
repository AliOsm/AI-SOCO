#include <bits/stdc++.h>

using namespace std;


int main(){
    int n;
    cin>>n;
    int big=n+(10-(n%10));
    int sml=n-(n%10);
    if(abs(big-n)<=abs(sml-n)){
        cout<<big<<endl;
    }
    else{
        cout<<sml<<endl;
    }
    return 0;
}