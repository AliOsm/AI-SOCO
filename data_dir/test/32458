#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define f(i, x, n) for(int i = x; i < (int)(n); ++i)

int const N = 100000;
char s[N + 1];

int main(){
	int n;
	scanf("%d%s", &n, s);
	sort(s, s + n);
	printf("%s\n", s);
}