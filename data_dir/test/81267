#include <bits/stdc++.h>
using namespace std;

signed main(){
  ios::sync_with_stdio( 0 );
  int T, S, Q; cin >> T >> S >> Q;
  for( int i = 1; ; ++i ){
    if( S * Q >= T ){
      cout << i << endl;
      exit( 0 );
    }
    S *= Q;
  }
  return 0;
}