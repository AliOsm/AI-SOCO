#include <stdio.h>

int main()
{
	int n, k, i;

	scanf ("%d %d", &n, &k);

	for (i = 1; i <= (k + 1) / 2; i++)
		printf ("%d %d ", i, k - i + 2);
	if (k % 2 == 0)
		printf ("%d ", k / 2 + 1);

	for (i = k + 2; i <= n; i++)
		printf ("%d ", i);

	return 0;
}
