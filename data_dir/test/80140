#include <iostream>
#include <bits/stdc++.h>
#define ll long long int
using namespace std;


int main(){
	string a,b;
	cin>>a>>b;
	cout<<a[0];
	int n = a.length();
	for(int i=1;i<n;i++){
		if(a[i]<b[0])
			cout<<a[i];
		else
			break;
	}

	cout<<b[0];
}