#include <bits/stdc++.h>
using namespace std;

int c[5];
int sum;

int main() {
  for (int i = 0; i < 5; ++i)
    cin >> c[i], sum += c[i];
  if (sum % 5 == 0 && sum)
    cout << sum / 5;
  else cout << -1;
  return 0;
}
