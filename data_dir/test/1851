#include <bits/stdc++.h>
using namespace std;

int n, ans;
string s, t;

int main(){



    cin >> n >> s;
    ans = n;
    for (int i = 0; i < n - 1; ++i){
        if (s[i] != s[i + 1]) --ans, ++i;
    }
    cout << ans;

}
