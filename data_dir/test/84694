#include <bits/stdc++.h>

using namespace std;

int n, a, b, c, t, ans, ta;

int main() {
	scanf("%d%d%d%d%d", &n, &a, &b, &c, &t);
	while (n--) {
		scanf("%d", &ta);
		ans += max(a, a + (c - b) * (t - ta));
	}
	printf("%d\n", ans);
	return 0;
}
