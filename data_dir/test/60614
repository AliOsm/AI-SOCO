#include <bits/stdc++.h>

using namespace std;

long long n, s = 0, x;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x;
        s += x;
    }
    for (int i = 0; i < n; i++) {
        cin >> x;
        s -= x;
    }
    cout << (s >= 0 ? "YES" : "NO");
}
