#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	int cnt[4];
	for (int i = 0; i < 4; i++) cin >> cnt[i];
	if (cnt[2] && cnt[0] == 0) cout << 0;
	else if (cnt[0] == cnt[3]) cout << 1;
	else cout << 0;
	return 0;
}