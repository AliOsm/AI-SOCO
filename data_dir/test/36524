#include <bits/stdc++.h>
using namespace std;
int main() {
	long n,m,a[1001],x,y,i;
	long long s;
	cin >> n >> m;
	for (i=1;i<=n;i++)
		cin >> a[i];
	s=0;
	for (i=1;i<=m;i++) {
		cin >> x >> y;
		s+=min(a[x],a[y]);
	}
	cout << s;
}
