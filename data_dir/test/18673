#include <bits/stdc++.h>
using namespace std;

int main() {
	int _t;
	cin >> _t;
	
	while (_t--) {
		long long a, b, k;
		cin >> a >> b >> k;
		printf("%lld\n", (k + 1) / 2 * a - (k - (k + 1) / 2) * b);
	}
}
