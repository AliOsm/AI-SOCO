#include <bits/stdc++.h>

using namespace std;

int k, n, w;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  cin >> k >> n >> w;
  cout << max(0, k * w * (w + 1) / 2 - n) << '\n';

  return 0;
}
