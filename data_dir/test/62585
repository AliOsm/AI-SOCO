#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
  ll l, r, x, y, k;
  cin >> l >> r >> x >> y >> k;
  for (ll b = x; b <= y; b++) {
    ll a = b*k;
    if (a >= l and a <= r) { cout << "YES\n"; return 0;}
  }
  cout << "NO\n";
  return 0;
}

