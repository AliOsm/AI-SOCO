#include <bits/stdc++.h>
using namespace std;
int main() {
	long n;
	string s;
	cin >> s;
	n=s.size();
	for (long i=n-2;i>=0;i--)
		if (s[i]==s[i+1]) s.erase(i,2);
	if (s.size()>0) cout << "NO";
	else cout << "YES";
}
