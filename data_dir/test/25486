#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(NULL);

	string s;
	cin >> s;
	char cur = s.back();
	int d = cur-'0';
	cout << d % 2 << '\n';

	return 0;
}

