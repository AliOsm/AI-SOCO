#include <bits/stdc++.h>

using namespace std;

int main ()
{
    ios_base::sync_with_stdio(false);
    string s; cin >> s;
    int c=0;
    for(int i=s.size()-1;i>0;i--)
        if(s[i]=='1') c=1;
    if(s.size()%2==0) c=0;
    cout << s.size()/2 + c << endl;
    return 0;
}
