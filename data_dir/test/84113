#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 5;

int l, r, x, y, k;

int main(){


    cin >> l >> r >> x >> y >> k;
    for (int i = l; i <= r; ++i){
        int b = i / k;
        if (b * k == i && b >= x && b <= y) return cout << "YES\n", 0;
    }
    cout << "NO";


}
