#include <bits/stdc++.h>
using namespace std;

const int MAXN = 200005;

int main()
{
	long long N, M, K, L, ans;

	cin >> N >> M >> K >> L;
	ans = (K + L + M - 1) / M;
	if(K + L <= M * ans && M * ans <= N)
		cout << ans << endl;
	else
		cout << -1 << endl;

	return 0;
}
