#include<bits/stdc++.h>
using namespace std;
int main()
{
    int ts,n, k;
    scanf("%d",&ts);
    while(ts--)
    {
        scanf("%d %d",&n,&k);
        printf("%d\n", 2*k);
    }

    return 0;
}
