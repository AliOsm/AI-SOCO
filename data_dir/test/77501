#include<bits/stdc++.h>

using namespace std;

int main() {
	
	ios_base::sync_with_stdio(0);
	cin.tie(NULL);
	
	int n;
	string s;
	cin>>n>>s;
	if(n & 1) cout<<"contest";
	else cout<<"home";

	return 0;
}