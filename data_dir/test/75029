#include<bits/stdc++.h>
using namespace std;

int main(){
    int n; cin>>n;
    int ans=-1e8;
    while(cin>>n){
        if(n<0)ans=max(ans,n);
        else{
            int s=round(sqrt(n));
            if(s*s==n);
            else ans=max(ans,n);
        }
    }
    cout<<ans<<endl;
}
