#pragma GCC optimize("-O3")
#include <bits/stdc++.h>
#define ll long long
#define f first
#define s second
#define pb push_back 
#define ios ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define y1 ibibibibbibibibiibigbabypanfewewf
using namespace std;
ll const N=5*1e5+5;
ll x1,r,x2,c,q,y1,l,e,y2,d,ans,a,aa[N],aaa[N],b,y3,k,pref[5005][4];
ll m,n;
string t,s,ss;
set <ll> st;
pair <ll,ll> p[N];
vector <ll> v;
int  main()
{
    ios
cin>>n>>q;
for (int i=1;i<=n;i++)
{
    cin>>m;
    x2=0;
    for (int j=0;j<m;j++)
    {
        cin>>x1;
        if (x1<q) x2=1;     
    }
    if (x2==1) v.pb(i);
}
cout<<v.size()<<endl;
for (int i=0;i<v.size();i++)
{
    cout<<v[i]<<" ";
}
}