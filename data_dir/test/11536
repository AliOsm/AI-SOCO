#include <stdio.h>
using namespace std;

int main(int argc, char **argv)
{
    int t, le, v, l, r;
    scanf("%d", &t);
    while (t--) {
        scanf("%d%d%d%d", &le, &v, &l, &r);
        printf("%d\n", (le / v) - (r / v - (l + v - 1) / v + 1));
    }
    return 0;
}
