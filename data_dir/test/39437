#include<bits/stdc++.h>

using namespace std;

typedef long long LL;
typedef unsigned long long uLL;
typedef long double ldb;
typedef pair<int,int> pii;

int main()
{
ios_base::sync_with_stdio(0);
	LL n,t,a=2,b=3,ans=0;
	cin>>n;
	while(a<=n)
	{
		t = b;
		b += a;
		a = t;
		++ans;
	}
	cout<<ans;
}