#include <bits/stdc++.h>
using namespace std;

const int N = 100 + 10;
int a[N];

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < 2 * n; i++)
        cin >> a[i];
    sort(a, a + 2 * n);
    cout << (a[n - 1] < a[n]? "YES": "NO") << endl;
    return 0;
}
