#include<bits/stdc++.h>
using namespace std;

#define fi(i,a,b)    for(long long i=a;i<=b;i++)
#define fr(i,a)      for(long long i=0;i<a;i++)
#define fd(i,a,b)    for(long long i=b;i>=a;i--)
#define clr(x)       memset(x,0,sizeof(x))
#define cln(x)       memset(x,-1,sizeof(x))
#define __           printf(" ")
#define _            printf("\n")
#define _o           printf("1\n")
#define stree        long long lft=node<<1,rht=(node<<1)|1,mid=(s+e)>>1
#define snode        long long s,long long e,long long node
#define slft         s,mid,lft
#define srht         mid+1,e,rht
#define sin()        if(S<=s&&e<=E)
#define sout()       if(S>e||s>E)
#define mod          1000000007
#define read()       freopen("in.txt","r",stdin)
#define write()      freopen("out.txt","w",stdout)
#define sfl(x)       scanf("%I64d",&x)
#define sfll(x,y)    scanf("%I64d %I64d",&x,&y)
#define sflll(x,y,z) scanf("%I64d %I64d %I64d",&x,&y,&z)
#define pfl(x)       printf("%I64d",x)
#define pfll(x,y)    printf("%I64d %I64d",x,y)
#define pflll(x,y,z) printf("%I64d %I64d %I64d",x,y,z)
#define xx           100000

typedef long long ll;
typedef pair<long long,long long> pll;

long long n,k,A,B;
vector<long long>a;

long long Cnt(long long s,long long e){
    return upper_bound(a.begin(),a.end(),e)-lower_bound(a.begin(),a.end(),s);
}

long long calc(long long s,long long e){
    long long cur=Cnt(s,e);
    if(cur==0) return A;
    long long ret=B*cur*(e-s+1);
    if(s==e) return ret;
    long long mid=(s+e)/2;
    return min(ret,calc(s,mid)+calc(mid+1,e));
}

main(){
    sfll(n,k);
    sfll(A,B);
    long long cur;
    fr(i,k){
      sfl(cur);
      a.push_back(cur);
    }
    sort(a.begin(),a.end());

    cout<<calc(1,(1ll<<n))<<endl;


    return 0;
}
