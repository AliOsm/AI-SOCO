#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1005;
string st0, st1, s0, s1;

int main()
{
    int n;

    // freopen("D:\\1.in", "r", stdin);
    // freopen("D:\\1.out", "w", stdout);

    cin >> st0 >> st1;
    cin >> n;
    cout << st0 << " " << st1 << endl;
    while(n--)
    {
        cin >> s0 >> s1;
        if(st0 == s0)
            st0 = s1;
        else
            st1 = s1;
        cout << st0 << " " << st1 << endl;
    }

    return 0;
}
