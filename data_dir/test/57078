#include <bits/stdc++.h>

using namespace std;

int min(int n) {
  if (n % 10 == 0) return n / 10;
  return n - 1;
}

int main() {
  int n, k;
  scanf("%d %d", &n, &k);
  for (int i = 0; i < k; i++) n = min(n);
  cout << n << endl;
  return 0;
}
