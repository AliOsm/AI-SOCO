#include <bits/stdc++.h>
using namespace std;
int32_t main(){
    ios_base :: sync_with_stdio(false);
    long long n, ans = 0;
    cin >> n;
    for (long long i = 2; i < n; ++i)
        ans += i * (i + 1);
    cout << ans;
}