#include <bits/stdc++.h>
using namespace std;

int N, R;
char S[105];

int main() {
    scanf("%d", &N);
    scanf("%s", S);

    while(R < N && S[R] == '1') R++;

    printf("%d\n", min(R + 1, N));

    return 0;
}
