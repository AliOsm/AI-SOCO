#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int N = 5 * 1e5 + 5;
ll a[N];

int main() {
	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int n;
	string s;
	cin >> n >> s;
	sort(s.begin(), s.end());
	cout << s << endl;
}
