#include <bits/stdc++.h>

using namespace std;

int main() {
	int N, M, result = 0;
	cin >> N >> M;
	while (N != M) {
		if (M % 2 == 0 && M > N)
			M /= 2;
		else
			M++;

		result++;
	}

	cout << result;
	return 0;
}