#include<bits/stdc++.h>
using namespace std;
main()
{
    long n;
    while(cin>>n)
    {
        long i,a,b,cnt=0;
        for(i=1;i<=n;i++)
        {
            cin>>a>>b;
            if((b-a)>=2)
                cnt++;
        }
        cout<<cnt<<endl;
    }
}
