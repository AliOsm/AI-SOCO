#include <bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define pb push_back
#define popb pop_back
#define fo(i , a , b) for (int i = (a); i < (b); ++i)
#define forr(i , b) fo( i , 0 , b )

const ll max9 = 10 + 1e9 ,  max6 = 10 + 1e6 , max12 = 10 + 1e12 , max15 = 10 + 1e15 ;
const ll min6 = -1 * max6 , min9 = -1 * max9 , min12 = -1 *max12 , min15 = -1 * max15 ;
const ll R = 7 + 1e7 , NN = 10 + 1e5 ;
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int a , b ; cin >> a >> b ; int now = 1 ;
    while (1){
        if (now % 2 == 1) { if (a - now >= 0) {a -= now ; now ++ ;} else {cout << "Vladik" ; return 0 ;}  }
        else { if (b - now >= 0){b -= now ; now ++ ;} else {cout << "Valera" ; return 0 ;} }
    }
    return 0;
}
