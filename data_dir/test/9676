#include <bits/stdc++.h>
using namespace std;

int main(){
	string str;
	cin>>str;
	int k=str[0]-32;
	char p=static_cast<char>(k);
	cout<<(str[0]>=97?p:str[0])<<str.substr(1,str.length()-1);

}