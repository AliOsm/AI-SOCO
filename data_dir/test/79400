#include <bits/stdc++.h>
using namespace std;
const int MAXN = 100 + 20;
int mark[MAXN], ans;
int main() {
	int n;
	cin >> n;
	for (int i = 0; i <n; i++) {
		int x; 
		cin >> x;
		mark[x]++, ans = max(ans, mark[x]);
	}
	cout << ans;
	return 0;
}
