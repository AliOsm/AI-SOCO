#include <bits/stdc++.h>
using namespace std;



class Solution {
  int n, k;
public:
  void run() {
    cin >> n >> k;
    cout << 3 + 2 * (n - 1) + min(n - k, k - 1) + n - 1 << '\n';
  }
};


int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  Solution().run();
}
