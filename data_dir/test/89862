#include <bits/stdc++.h>
using namespace std;
 
const int N = 10000 + 20;
  
int main() {
	int n;
	cin >> n;
	int ans = 0;
	for (int i = 1; i < n; i++) {
		ans += i * (n - i);
	}
	cout << ans + n;
	return 0;
}
