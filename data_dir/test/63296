#include <bits/stdc++.h>
using namespace std;

int main( )
{
	int n, i, j, k, l;
	
	cin >> n;
	
	k = 1, l = n * n;
	
	for (i = 1; i <= n; i++)
	{
		for (j = 1; j <= n / 2; j++)
		{
			cout << k << " " << l << " ";
			k++;
			l--;
		}
		cout << "\n";
	}
	
	return 0;
}