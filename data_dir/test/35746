#include<bits/stdc++.h>
#define ll long long int
#define r_0 return 0
#define fr first
#define sc second
#define pb push_back
#define sz size()
#define TIME ios_base::sync_with_stdio(0)
using namespace std;
ll n,l,m,k,one=1,mx,mn,x,y;
int sum,cnt,ans;
char c,ch;
bool b,boo;
int a[10000001];
string s1,s2,s3,s;
set < int > se;
vector< int > v;
float f,fl,flo,x1,x2,x3,x4,y1,y2,y3,y4;
float arr[1001];
double doubl;
int main()
{
    TIME;
    cin >> n;
    for( int i = 0 ; i < n ; i ++ ){
        cin >> a[i];
    }
    for( int i = 1 ; i < n-1 ; i ++ ){
        if( a[i-1] == 1 && a[i] == 0 && a[i+1] == 1)
    {
        a[i-1]=0;
        a[i+1]=0;
        cnt ++;
    }
    }
    cout << cnt ;

}

/*cin >> n;
    for( int i = 2 ; i <= n ; i ++ ){
        m =0 ;
        for(int j = 2 ; j <= sqrt(i) ; j ++ )
            if( i % j == 0 )
                m = 1;
            if( m == 0 && n % i == 0 )
                cout << i << " ";
        }*/



//sinfi 10


/*for 11
case 18
begin 14
int 7
if 17
    */


