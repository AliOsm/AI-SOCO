#include <bits/stdc++.h>

using namespace std;

int n,d[101],a,b,ans;

int main(){
    cin >> n;
    for (int i=1;i<n;i++)
        cin >> d[i];
    cin >> a >> b;
    for (int i=a;i<b;i++)
        ans+=d[i];
    cout << ans;
}