#include <bits/stdc++.h>

using namespace std ;


int main()
{
    ios_base::sync_with_stdio(0) ;
    cin.tie(0) ;
    
    long long a[3], b[3] , n ; 
    cin >> a[0] >> a[1] >> a[2] >> b[0] >> b[1] >> b[2] >> n ; 
    long long aa = a[0] + a[1] + a[2] ; 
    aa = aa/5 + bool(aa%5) ; 
    long long bb = b[0] + b[1] + b[2] ; 
    bb = bb/10 + bool(bb%10) ; 
    if ( aa + bb <= n ) cout << "YES" ; 
    else cout <<"NO" ; 
    
    return 0 ;
}
