#include<bits/stdc++.h>
#define In_the_name_of_allah_the_merciful ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define Alhamdulillah return 0;
#define pb push_back
#define ins(x) insert(x)
#define fi first
#define se second
#define lb lower_bound
#define ub upper_bound
#define all(v) (v.begin(),v.end())
#define sz(v) ((ll)((v).size()))
#define reset(v,val) memset(v,val,sizeof(v))
#define erep(i, x, n) for ( i = x; i<=(ll)(n); i++)
#define rep(i, x, n) for( i = x; i<(ll)(n); i++)
using namespace std;
typedef  long long int   ll;
const long long int mod=1e9+7;
ll dx[]={0,0,1,-1};
ll dy[]={1,-1,0,-1};
ll m,k=0,n,sm=0,vis[500005]={0},res, a[500005],b[500005];
set<  pair<ll,ll> >st,st2;
string s1="one",s2="zero",s,ch="";
vector<ll>v1,v[500005];
vector< pair<ll,ll> >vv;
vector< pair<long double,ll> > vvA,vvB;
vector< pair< pair <ll,ll>,ll > > vvv;
priority_queue <ll> pq;
map< ll , ll >red;
map< pair<ll,ll> ,ll >mp;
ll xa,xb,xc,ya,yb,yc,xres;


void dfs(ll x){
   vis[x]=1;
   if(v[x].size())for(ll i=0;i<v[x].size();i++){
        ll y=v[x][i];
        if(!vis[y]){
            k++;
            a[y]=k;
        }
   }
   k++;
   b[x]=k;
  if(v[x].size()) for(ll i=v[x].size()-1;i>=0;i--){
     ll y=v[x][i];
     if(!vis[y])dfs(y);
   }
}



int main(){
In_the_name_of_allah_the_merciful

	ll i=0,x,y,j=0,z=0,q,nb0=0,nb=0,nbmax=0,nbinf=0,nb2=0,mm=5e8,idx=0,ok=0,l,r,negatif=0,positif=0,l1,r1;
	ll w,h,type,t,p;
	set<  pair<ll,ll> > ::iterator it,it1,it2;
	char c;
  //  freopen("a.txt","r",stdin);
     cin>>n;
     for(i=1;i<=n;i++)v[i].clear();
     for(i=1;i<n;i++){
        cin>>x>>y;
        v[x].pb(y);
        v[y].pb(x);
     }
     memset(vis,0,sizeof(vis));
     a[1]=1;
     k=1;
     dfs(1);
     for(i=1;i<=n;i++)cout<<a[i]<<" "<<b[i]<<endl;

Alhamdulillah
 }
