#include <iostream>
using namespace std;
int Xe,Ye,X1,Y1,x,y;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >>X1>>Y1>>Xe>>Ye>>x>>y;
    if( (X1-Xe)%x==0 && (Y1-Ye)%y==0 && (abs(X1-Xe)/x)%2 == (abs(Y1-Ye)/y)%2 )cout<<"YES";
    else cout<<"NO";
}
