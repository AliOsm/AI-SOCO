#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a,b,c, mid; scanf("%d%d%d",&a,&b,&c);
    mid = (a+b+c)-(min(a,min(a,b))+max(a,max(b,c)));
    int res = abs(max(a,max(b,c))- mid) + abs(min(a,min(b,c))-mid);
    printf("%d\n",res);
    return 0;
}
