#include <stdio.h>
using namespace std;

const int MAXN = 100010;
int n, k;

int main() {
    scanf("%d%d", &n, &k);
    for(int i = 1; i <= n - k; ++i) printf("%d %d ", 2 * i, 2 * i - 1);
    for(int i = n - k + 1; i <= n; ++i) printf("%d %d ", 2 * i - 1, 2 * i);
}