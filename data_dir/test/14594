#include <bits/stdc++.h>
using namespace std;

int n, m;

int main(){
    scanf("%d %d", &n, &m);
    for(int i = 0, a, b; i < m; i++){
        scanf("%d %d", &a, &b);
    }
    for(int i = 0; i < n; i++){
        printf("%d", i % 2);
    }
    printf("\n");
    return 0;
}
