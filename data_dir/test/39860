#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main ()
{
    ios_base::sync_with_stdio(false);
    int n; cin >> n;
    int k = (n-1)/2;
    if(n%2 == 0 && k%2 == 0) k--;
    cout << k << " " << n-k << endl;
    return 0;
}
