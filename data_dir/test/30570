#include <bits/stdc++.h>
using namespace std;
vector<int> v;
int ans;
int main() {
	string a, b;
	cin >> a >> b;
	if (a == b) {
		cout << -1;
		return 0;
	}
	if (b.size() > a.size())
		cout << b.size();
	else
		cout << a.size();
	return 0;
}
