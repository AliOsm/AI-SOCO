#include<bits/stdc++.h>
#define ll long long
#define pb push_back
#define sz size()
#define sc second
#define fr first
#define r_0 return 0
#define TIME ios_base::sync_with_stdio(0);

const ll N = 1e5+1;

using namespace std;

    ll n , m , l , k , cnt , ans ,x , y , mn = 1e9 , mx = -1e9 ;
    set < ll > st;
    pair < ll , ll > p [N];
    vector < ll > v , d;
    map < ll , ll > mp;
    ll a[N];
    string s , t ;
int main()
{TIME
    cin >> n >> m ;
    cnt = n - m;
    ans = m;
    if( m >= n ){
    cout<<n-1;
    return 0;
    }
    for ( ll i = 2 ; i <= cnt ; i ++ )
    {
        ans += i;
    }
    cout << ans ;
}













