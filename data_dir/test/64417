#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main()
{
	int n;
	scanf("%d",&n);
	if (n < 6) puts("-1");
	else {
		puts("1 2\n1 3\n1 4");
		for (int i = 5; i <= n; i++) printf("2 %d\n",i);
	}
	for (int i = 2; i <= n; i++) printf("1 %d\n", i);
}
