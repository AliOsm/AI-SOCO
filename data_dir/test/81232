#include <bits/stdc++.h>
using namespace std;

int main()
{
	long double a,b,c;
	cin >> a >> b >> c;
	a = sqrt(2)*a*a*a/12.0;
	b = sqrt(2)*b*b*b/6.0;
	c = (sqrt(5)+5)*c*c*c/24.0;
	cout << fixed << setprecision(10) << a+b+c << endl;
    return 0;
}
