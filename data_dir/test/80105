#include <iostream>
using namespace std;
int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    if((a *((c*(c+1))/2)) <= b) cout << 0;
    else cout << (a *((c*(c+1))/2)) - b;
    return 0;
}
