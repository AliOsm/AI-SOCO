#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e5+5, M = 64, mod = 1e9+7;
int main()
{
	string s;
	cin >> s;
	if(s == "0") cout << 0 << endl;
	else cout << (s.size()+(count(s.begin(), s.end(), '1')>1))/2 << endl;	
}
