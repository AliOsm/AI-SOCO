#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	int n, x;
	cin >> n >> x;
	int s = 0;
	while (n--) {
		int a;
		cin >> a;
		s += a;
	}
	int ans = (abs(s) + x-1) / x;
	cout << ans << '\n';
}