// practice with Dukkha
#include <algorithm>
#include <iostream>

using namespace std;

int main() {
	int n, k; cin >> n >> k;
	for (int r = k - 1; ; r--)
		if (n % r == 0) {
			cout << n / r * k + r << '\n';
			return 0;
		}
	return 0;
}
