#include<bits/stdc++.h>

using namespace std;

int n, a, i;
string s;

int main(){
	cin>>n>>s;
	for(auto ch : s) 
	    if(ch=='A') a++; else
	    if(ch=='I') i++;
	if(i==0) cout<<a; else
	if(i==1) cout<<i; else cout<<0;
	cout<<endl;
	return 0;
}