#include <iostream>

using namespace std;

int k[4],MIN;
long long sum;

int main()
{

    for(int i=0 ; i<4 ; i++)    cin >> k[i];
    MIN=min(k[0],min(k[2],k[3]));
    sum=256*MIN+(32*min(k[0]-MIN,k[1]));
    cout << sum << endl;
}
