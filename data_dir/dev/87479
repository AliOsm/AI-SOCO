#include <bits/stdc++.h>
using namespace std;
int n,a[5001];
main ()
{
	cin>>n;
	
	for (int i=1; i<=n; i++)
		cin>>a[i];
		
	for (int i=1; i<=n; i++)
		if (i==a[a[a[i]]])
		{
			cout<<"YES"<<endl;
			return 0;
		}
		
	cout<<"NO"<<endl;	
} 