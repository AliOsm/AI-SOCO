#include<iostream>
#include<algorithm>
using namespace std;

int main()
{
	long long tot = 0;
	long long n, m;
	cin >> n >> m;
	if (m >= n / 2)
	{
		tot = (n - 1)*n / 2;
	}
	else
	{
		tot = (((n - 1)*n) / 2) - (n - (2 * m))*((n - (2 * m))-1)/2;
		
	}
	cout << tot;

	return 0;
}