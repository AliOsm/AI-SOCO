#include<bits/stdc++.h>
using namespace std;
int main(){
	double n,r;
	double angle;
	cin >> n >> r;
	double PI = acos(-1);
	double val = sin(PI/(n));
	cout << fixed << setprecision(10) << (val * r)/(1.0 - val);
}