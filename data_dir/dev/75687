#include <bits/stdc++.h>
#define ll int
#define pb push_back
#define f first
#define s second
#define y1 bbp
using namespace std;
int const N=1e6+4,NN=2e5+3,inf=1e9+7;ll const md=998244353;
ll a[N],pref[N][5],dp[N],b[N],n,l,r,k,m,x1,y,x,y1,kk,sz,ans;
long long q,qq;
char ch;
vector <ll> v[N],ot;
bool us[N];
map <string,ll> mp;
string s[N],t;
main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    cin>>n;
    for (int i=1;i<=n;i++)
    {
        cin>>s[i];
    }
    for (int i=n;i>0;i--)
    {
        if (!mp.count(s[i])) cout<<s[i]<<endl;
        mp[s[i]]++;
    }
    return 0;

}