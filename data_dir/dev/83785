#include <iostream>

using namespace std;

int main() {
	int n;
	cin >> n;
	string s(n, 'a');
	for (int i = 0; i < n; i++)
		if (i % 4 == 0 || i % 4 == 1)
			s[i] = 'b';
	cout << s << endl;
	return 0;
}
