#include <bits/stdc++.h>

using namespace std;

long long n,k,a[1000002],i,sum;

int main() {
    cin>>n>>k;
    for (i=1;i<=n;i++)
        cin>>a[i];
    sort(a+1,a+n+1);
    for (i=1;i<=k;i++)
    sum+=a[i];
    cout<<sum<<endl;
}
