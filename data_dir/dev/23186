#include <bits/stdc++.h>

using namespace std;

int t, n;

int main() {
	scanf("%d", &t);
	while (t--) {
		scanf("%d", &n);
		if (n % 2) {
			printf("7");
			n -= 3;
		}
		n /= 2;
		while (n--)
			printf("1");
		puts("");
	}
}
