#include <bits/stdc++.h>

#define SZ(x) (((int)x.size()))

typedef long long ll;
typedef long double ld;

using namespace std;

int a, b, c, x, y, z;

int main()
{
	cin >> x >> y >> z >> a >> b >> c;
	if (a < x)
	{
		cout << "NO";
		return 0;
	}
	a -= x;
	if (a + b < y || a + b + c < y + z)
	{
		cout << "NO";
		return 0;
	}
	cout << "YES";
	return 0;
}
