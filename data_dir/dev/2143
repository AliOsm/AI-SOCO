#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int a = 0, b = 0, n, t;
    cin >> n;

    for (int i = 0; i < n; i++)
        cin >> t, a += t;
    for (int i = 0; i < n; i++)
        cin >> t, b += t;

    if (a >= b)
        cout << "Yes\n";
    else
        cout << "No\n";
    return 0;
}
