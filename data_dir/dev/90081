#include <iostream>
#include <algorithm>
#include <ctime>
#include <iomanip>
#include <set>
#include <map>
#include <vector>
#include <stdio.h>
using namespace std;
const long long maxInt = (int)1e9;
const int maxN = (int)1e5 + 228;

int i, j, n, m, x, y, _x, _y;

int main() {
//	freopen(".in", "r", stdin);
//	freopen(".out", "w", stdout);

	cin >> x >> y;
	cin >> _x >> _y;

	cout << max(abs(x - _x), abs(y - _y)) << "\n";
}
