    #include <bits/stdc++.h>
    #define r_0 return 0;
    #define ll long long
    #define ld  long double
    #define pb push_back
    #define fr first
    #define se second
    #define TIME ios_base::sync_with_stdio(NULL);
    #define tcn cin.tie(NULL);
    #define tct cout.tie(NULL);
    using namespace std;
    ll n , mx , cnt , m , ans , mn =1e9;
    ll a , b , c;
    vector<ll>v;
    string s ,s1, s2;
    const ll N =1e4 + 7;
    ll used[N];
    int main(){
        TIME tcn tct

         /// freopen("pwgen.in","r",stdin);
        /// freopen("pwgen.out","w", stdout);
        vector<pair<ld , ll > >p;
        cin >> n >> m;
        cnt =m - n;
        if( !cnt ){
            return cout<<n,0;
        }
        for( int i = 2; i<= 9;i ++ ){
            p.pb({cnt / i , i});
        }
        sort(p.rbegin(),p.rend());
            cout<< 2;
    }
