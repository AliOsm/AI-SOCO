#include <cmath>
#include <iostream>

using namespace std;

int main() {
	int n;
	cin >> n;
	int a = sqrt(n);
	int m = a * 2;
	if (a * a >= n)
		m += 0;
	else if (a * (a + 1) >= n)
		m += 1;
	else
		m += 2;
	cout << m << endl;
	return 0;
}
