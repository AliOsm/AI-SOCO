#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	string s;
	cin>>s;
	int i=0,k=0;
	while (s[i]=='a') i++;
	for (;i<s.size();i++) 
		if (s[i]=='a') break;
		else s[i]--,k++;
	if (!k) s[s.size()-1]='z';
	cout<<s;
	return 0;
}
