#include <bits/stdc++.h>

using namespace std;

int n,m;

int main(){
	cin >> n >> m;
	cout << min(n,m)+1 << endl;
	for (int i=0;i<min(n,m)+1;i++)
		cout << min(n,m)-i << " " << i << endl;	
}