#include <bits/stdc++.h>

using namespace std;
const int N = 1e5 + 3;
int main() {
#ifndef ONLINE_JUDGE
	freopen("input.in", "rt", stdin);
#endif
	int w1, h1, w2, h2;
	scanf("%d %d %d %d", &w1, &h1, &w2, &h2);
	printf("%d\n", w1 * 2 + h1 * 2 + 4 + h2 * 2);
	return 0;
}
