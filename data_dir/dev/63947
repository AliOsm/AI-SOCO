#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,a,b,i;
    cin>>n;
    int nt[n];
    for(i=0;i<n;i++)
    {
        cin>>nt[i];
    }
    sort(nt,nt+n);
    if(nt[0]==1)
        cout<<"-1";
    else
        cout<<"1";
    return 0;
}
