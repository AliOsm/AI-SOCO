#include "bits/stdc++.h"
using namespace std;

int main() {

	long long n;
	cin >> n;

	long long ans = 0;

	for(long long i=1;i<=n;i++) {

		long long a,b,c;
		cin >> a >> b >> c;

		if(a + b + c > 1) {
			ans++;
		}
	}

	cout << ans;
}