#include <bits/stdc++.h>
using namespace std;

int n, a, b, c, ans;

int main() {
    cin >> a >> b >> c >> n;
    ans = n - a - b + c;
    
    if(ans < 1 || c > a || c > b) ans = -1;
    cout << ans << endl;
    return 0;
}