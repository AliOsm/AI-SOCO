// practice with Dukkha
#include <algorithm>
#include <iostream>

using namespace std;

int main() {
	int l, r, a; cin >> l >> r >> a;
	if (l > r)
		swap(l, r);
	cout << (l + a <= r ? l + a : (l + r + a) / 2) * 2 << '\n';
	return 0;
}
