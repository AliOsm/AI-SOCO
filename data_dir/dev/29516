#include <bits/stdc++.h>

#define SZ(x) (((int)x.size()))

typedef long long ll;
typedef long double ld;

using namespace std;

int n, sum;
string s;

int main()
{
	cin >> n >> s;
	for (int i = 1; sum < n; i++)
	{
		sum += i;
		cout << s[sum - 1];
	}
	return 0;
}
