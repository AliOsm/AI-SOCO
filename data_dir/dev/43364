#include<bits/stdc++.h>
using namespace std;

#define ll           long long
#define pii          pair<ll,ll>
#define bug(a)       cerr << #a << " : " << a << endl;
#define FastRead     ios_base::sync_with_stdio(false);cin.tie(NULL);

const int MAX = 1e6;

int main()
{
    FastRead

    int n;

    cin >> n;

    int a[n+2];

    for(int i=1;i<=n;i++)
        cin >> a[i] , a[i]+=1e5;

    cout << n+1 << endl;

    cout << 1 << " " << n << " " << 100000 << endl;

    int cur = 0;
    for(int i=1;i<=n;i++)
    {
        cout << 2 << " " << i << " " << a[i]-cur << endl;
        cur++;
    }
}
