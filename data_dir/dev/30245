#pragma GCC optimize("03")
#include <bits/stdc++.h>
#define ll long long
#define F first
#define S second
using namespace std;
main () {
	int t;
	scanf("%d",&t);

	while (t--) {
	  ll n,k;
	  cin>>n>>k;
	  cout<<n-n%k+min(n%k,k/2)<<endl;
	}
}