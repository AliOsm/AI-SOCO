#include<bits/stdc++.h>
using namespace std;
#define ll long long
int A[300005];
int main()
{
    int n,i;
    cin>>n;
    for(i=0;i<n;i++)
        cin>>A[i];
    sort(A,A+n);
    cout<<A[(n-1)/2];
    return 0;
}
