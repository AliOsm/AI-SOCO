#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll n;

int main() {
    scanf("%I64d", &n);
    int k = ceil((sqrt(1.0 + 8 * n) - 1) / 2) - 1;
    int ans = n - 1ll * k * (k + 1) / 2;
    printf("%d\n", ans);
    return 0;
}
