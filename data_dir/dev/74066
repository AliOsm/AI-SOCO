#include<stdio.h>

int main()
{
    int len, i, j = 1;
    char in[56];
    scanf("%d", &len);
    scanf("%s", in);

    for (i = 0; i < len; i += j)
    {
        printf("%c", in[i]);
        j++;
    }
    printf("\n");
    return 0;
}
