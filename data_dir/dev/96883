#include <bits/stdc++.h>

using namespace std;
vector<pair<int,int> > myvector;
int main()
{
    int n;
    cin >> n;
    for(int i = 1;i <= n;i++)
    {
        int useless;
        cin >> useless;
        myvector.push_back(make_pair(useless,i));
    }
    sort(myvector.begin(),myvector.end());
    for(int i = 0;i < myvector.size();i++)
        cout << myvector[i].second << " ";

}
