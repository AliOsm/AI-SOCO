// KALAM
# include <bits/stdc++.h>

using namespace std;

const int N = 5000 + 77;
int n , k;
int main(){
   ios::sync_with_stdio(0);cin.tie(0);
   cin >> n >> k;
   int res = min(abs(k - 1) + n - 1 , abs(n - k) + n - 1);
   cout << res + n + n + 1;
   return 0;
}
