#include <bits/stdc++.h>
using namespace std;


long long x, y, xx, yy;

int main(){


    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin >> x >> y >> xx >> yy;
    long long ans = max(abs(x - xx), abs(y - yy));
    cout << ans;

}
