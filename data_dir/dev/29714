#include<bits/stdc++.h>
using namespace std;
 
typedef long long ll;
#define pb push_back
#define Mehrkish ios::sync_with_stdio(false),		cin.tie(0),			cout.tie(0)
 
const int maxx = 1e6 + 19;
ll b[maxx], c[maxx], d, ans, jvb, minn = 99999999999, maxn = -999999999999, n, m, x, y, dp[maxx], k, sum, z, a[maxx];
vector <int> v;
string s;
bool flag = true;
 
void Input(){
	cin >> n;
}
 
void Solve(){
	for ( int i = 2; i < n; i++ ){
		ans += i * (i + 1);
	}
	cout << ans << endl;
}
int main(){
	Mehrkish;
	Input(),	Solve();
	return 0;
}