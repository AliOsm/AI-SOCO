#include <bits/stdc++.h>
using namespace std;

int a,b;
int ans1,ans2;

int main(){
	scanf("%d %d", &a, &b);
	for (int i = 1 ; a >= 0 && b >= 0; i++) {
		if (i%2 == 1) a -= i;
		else b -= i;
	}
	if (a < 0) puts("Vladik");
	else puts("Valera");
}