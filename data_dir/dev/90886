#include <iostream>
#include <algorithm>
#include <set>
#include <vector>
#include <map>
#include <queue>
#include <stack>
#include <string>
#include <cstring>
#include <cstdio>
// Kobe is the GOAT
// DON'T DIVIDE
// Greedy, Prefix, Sets/Bsearch, Dfs/bfs, DP
// Stack, Bitmask, Dsu 
using namespace std;

int x, y, z, a, b, c;

int main()
{
	ios::sync_with_stdio(0);
	cin >> x >> y >> z >> a >> b >> c;

	if(a < x || (a - x + b < y) || (z > a + b + c - x - y))
		cout << "NO\n";
	else
		cout << "YES\n";

	return 0;
}
