// practice with Dukkha
#include <algorithm>
#include <iostream>

using namespace std;

int main() {
	int a, b; cin >> a >> b;
	if (a > b)
		swap(a, b);
	int c = (a + b) / 2;
	cout << (c - a) * (c - a + 1) / 2 + (b - c) * (b - c + 1) / 2 << '\n';
	return 0;
}
