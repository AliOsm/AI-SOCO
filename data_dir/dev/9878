#include <bits/stdc++.h>

using namespace std;

int main()
{
    string s ,  t  ;
    cin >> s ;
    string a[10] ;
    for (int i = 0 ; i< 10 ; i++ ) cin >> a[i] ;
    for (int i = 0 ; i< s.size() ; i += 10 )
    {
       string t = s.substr(i,10) ;
       for (int j = 0 ; j<10 ; j++ )
       if ( t == a[j]) { cout<<j; break ; }
    }
}
