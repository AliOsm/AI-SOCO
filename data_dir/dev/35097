#include <cstdio>
#include <algorithm>

using namespace std;

int main() {
  int tt;
  scanf("%d", &tt);
  while (tt--) {
    int a[3];
    scanf("%d%d%d", a + 0, a + 1, a + 2);
    sort(a, a + 3);
    puts(a[0] + a[1] >= a[2] - 1 ? "Yes" : "No");
  }
}
