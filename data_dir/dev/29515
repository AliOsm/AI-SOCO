#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ll n, s;

    cin >> n >> s;

    if(s % n == 0) cout << (s / n) << endl;
    else cout << (s / n) + 1 << endl;

    return 0;
}
