#include <bits/stdc++.h>
using namespace std;

int a, b, c, n, r;

int main(){
    scanf("%d %d %d %d",&a,&b,&c,&n);
    r = n - (a + b - c);
    printf("%d\n", (r > 0 && r<=n && c<=a && c<=b) ? r : -1);
    return 0;
}
