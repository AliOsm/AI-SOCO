#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
using LL = long long;

int main() {
    int n = 0;
    scanf("%d", &n);
    if (n % 10 > 5) {
        n += 10 - n % 10;
    } else {
        n -= n % 10;
    }
    printf("%d\n", n);
    return 0;
}
