#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(0);
  cin.sync_with_stdio(0);
  
  int t;
  cin >> t;
  while(t--){
    ll s, a, b, c;
    cin >> s >> a >> b >> c;

    ll take = s/c;
    take += (take / a) * b;
    cout << take << "\n";
  }
}