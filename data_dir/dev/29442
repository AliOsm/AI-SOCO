#include <iostream>
using namespace std;
int main(){
	int n, m, kol = 0;
	cin >> n >> m;
	for(int a = 0; a < 32; a++){
		for(int b = 0; b < 32; b++){
			if((a * a) + b == n && (b * b) + a == m) kol++;
		}
	}
	cout << kol << endl;
	return 0;
}
