#include<stdio.h>
#include<iostream>
#include<string>
using namespace std;
int a,s,d,l;
string x;
main(){
freopen("input.txt","r",stdin);
freopen("output.txt","w",stdout);
cin>>x>>l;
if(x=="front" && l==1) cout<<"L";
if(x=="front" && l==2) cout<<"R";
if(x=="back" && l==1) cout<<"R";
if(x=="back" && l==2) cout<<"L";
}