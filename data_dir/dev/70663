#include<bits/stdc++.h>
using namespace std;
int a, b, c;
int main(){
  scanf("%d %d %d",&a,&b,&c);
  for(int x = 0; a*x <= c; x++)
   if((c-a*x)%b==0){
     printf("Yes");
     return 0;
   }
  printf("No");
  return 0;
}
