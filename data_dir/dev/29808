#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

ll a, b, s;

int main() {
  ios::sync_with_stdio(0);
  cin >> a >> b >> s;
  
  cout << (((a+b+s)%2==0 && s >= abs(a)+abs(b)) ? "Yes" : "No") << endl;
}