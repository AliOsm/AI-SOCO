#include <bits/stdc++.h>
using namespace std;

long long n, x, y;

int main(){

    scanf("%lld %lld %lld", &n, &x, &y);

    long long a = max(x - 1ll, y - 1ll), b = max(n - x, n - y);

    printf("%s\n", (b >= a) ? "White" : "Black");

    return 0;
}
