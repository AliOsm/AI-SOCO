// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    int n = 10, i;
    string s, p;
    map<string, int> m;
    cin >> p;
    for(i = 0; i < n; ++i)
        cin >> s, m[s] = i;
    for(i = 0; i < 8; ++i)
        cout << m[p.substr(n*i, n)];
    return 0;
}
