#include<bits/stdc++.h>

#define ll long long
using namespace std;
int main() {

	ios_base::sync_with_stdio(0);
	cin.tie(0);
	ll n, s;
	cin >> n >> s;
	cout << (s + n - 1) / n << endl;
	return 0;
}
