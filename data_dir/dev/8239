/// Computer Won't Turn On? Nothing To Worry ;))
/// NOTHING CAN STOP ME I'M ALL THE WAY UP
#include <bits/stdc++.h>

#define ss second
#define ff first
#define pb push_back
#define faster ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)

using namespace std;

typedef long long ll;
typedef long double ld;

const long long N=100100,mod=1e9+7,mod2=1e9+9,mod3=998244353,sq=450,base=727,lg=21,inf=1e18+10;

ll n,m,x,y,w,z,k,t,q,d,ans,a[N],b[N];

int main(){
    faster;
    cin>>n;
    for(int i=0;i<n;i++)cin>>a[i],b[i]=a[i];
    sort(b,b+n);
    for(int i=0;i<n;i++)if(a[i]!=b[i])x++;
    if(x<=2)cout<<"YES";
    else cout<<"NO";
    return 0;
}
/// Writer  S100101
