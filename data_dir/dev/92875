#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    ll n,i,j;
    cin>>n;
    vector<ll> a(n);
    for(i=0;i<n;i++)
        cin>>a[i];
    vector<ll> b(n);
    for(i=0;i<n;i++)
        cin>>b[i];
    ll tot1=0,tot2=0;
    for(i=0;i<n;i++)
        tot1+=a[i];
    for(i=0;i<n;i++)
        tot2+=b[i];
    if(tot2<=tot1)
        cout<<"Yes"<<endl;
    else
        cout<<"No"<<endl;
}
