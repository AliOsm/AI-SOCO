#include <bits/stdc++.h>

using namespace std;

int main ()
{
    ios_base::sync_with_stdio(false);
    string a; cin >> a;
    cout << a; reverse(a.begin(),a.end());
    cout << a << endl;
    return 0;
}
