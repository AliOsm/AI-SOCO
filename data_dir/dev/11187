#include <iostream>
#include <cstdio>

using namespace std;

int a, b, c, d;

int main() {
    cin >> a >> b >> c >> d;
    cout.precision(9);
    cout << fixed << 1.0 * a * d / (a * d + b * c - a * c);
    return 0;
}