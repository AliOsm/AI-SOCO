#include<bits/stdc++.h>
using namespace std;

int main()
{
    map<string,int>mp;
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n;
    string s,p;
    int c=INT_MIN;
    cin>>n;
    for(int i=0; i<n; i++)
    {
        cin>>s;
        mp[s]++;
        if(mp[s]>c)
        {
            c=mp[s];
            p=s;
        }
    }
    cout<<p<<endl;

    return 0;
}
