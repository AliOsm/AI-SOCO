#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
int n, m, ans, a;

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> a;
        if (a > m)
            ans++;
    }
    cout << ans + n;
}