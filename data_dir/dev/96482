#include <iostream>

using namespace std;

int main() {
    long long a, b, c;
    int q;
    cin >> q;
    while(q--) {
        cin >> a >> b >> c;
        cout << (a + b + c) / 2 << endl;
    }
}