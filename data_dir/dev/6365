#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;
    int n, s, t;
    for (int i = 0; i < T; i++) {
        cin >> n >> s >> t;
        int x = (s + t) - n;
        s -= x;
        t -= x;
        cout << max(s, t) + 1 << endl;
    }
}
