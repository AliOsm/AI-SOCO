#include<iostream>
using namespace std;
int main(){
   string a,b;int p1=0,p2=0;
   cin>>a>>b;
   for(int i=0; i<a.size(); i++){
      a[i]=tolower(a[i]);
      b[i]=tolower(b[i]);
     // if(a[i]>b[i])p1++;if(a[i]<b[i]) p1--;
   }
   cout<<a.compare(b);
   return 0;
}