#include<bits/stdc++.h>
#define r_0 return 0;
#define ll long long int
#define ld long double
#define pb push_back
#define fr first
#define se second
#define in freopen("input.txt","r",stdin);
#define out freopen("output.txt","w" stdout);
#define TIME ios_base::sync_with_stdio(NULL);
#define tcn cin.tie(NULL);
#define tct cout.tie(NULL);
using namespace std;
ll n,k,cnt,ans,mn,mx,a,b,c,m;
string s,s1;
vector<int>v;
map<ll,ll>mp;
set<int>st;
ll arr[107];
int main()
{
TIME tcn tct
cin >> n >> m;

    for(int i=0;i<m;i++){
       cin>>b;
       for(int j=b;j<=n;j++){
            if(arr[j]!=0)
                break;
            else
                arr[j]=b;
       }
    }
    for(int i=1;i<=n;i++)
        cout<<arr[i]<<" ";
    return 0;
}



