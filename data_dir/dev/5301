#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

bool arr[20];
int a[20];

int main()
{
    int n,m;
    cin>>n>>m;
    for(int i=0;i<n;i++)
        cin>>a[i];
    for(int i=0;i<m;i++){
        int x;
        cin>>x;
        arr[x]=1;
    }
    for(int i=0;i<n;i++){
        if(arr[a[i]])
            cout<<a[i]<<" ";
    }
    return 0;
}