#include <iostream>
#include <algorithm>
using namespace std;

int A, B;

int main()
{
    ios::sync_with_stdio(0);

    cin >> A >> B;
    if (A > B)
        swap(A, B);

    int ans = 1;
    for (int i = 1; i <= A; i++)
        ans *= i;

    cout << ans << "\n";

    return 0;
}
