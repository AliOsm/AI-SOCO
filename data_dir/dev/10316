#include <bits/stdc++.h>
using namespace std;
int main() {
	int n,i,x,y,l,r;
	cin >> n;
	l=0;
	r=0;
	for (i=1;i<=n;i++) {
		cin >> x >> y;
		if (x==1) l++;
		if (y==1) r++;
	}
	cout << min(l,n-l)+min(r,n-r);
}
