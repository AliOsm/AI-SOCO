#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    #ifndef ONLINE_JUDGE
    freopen("input.txt","r",stdin);
    #endif // ONLINE_JUDGE
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll n;
    cin>>n;
    if (n == 2)
    {
        cout<<3<<endl;
        cout<<2<<" "<<1<<" "<<2;
        return 0;
    }
    if (n == 3)
    {
        cout<<4<<endl;
        cout<<2<<" "<<1<<" "<<3<<" "<<2;
        return 0;
    }


/*
    if (n%2==0)
    {
    cout<<2 * n - 2<<endl;
    ll i;
    for(i = 2; i<n; i++)
        cout<<i<<" ";
    cout<<1<<" "<<n<<" ";
    for(i = 2; i<n; i++)
        cout<<i<<" ";
        return 0;
    }
    */
    ll i;
    cout<<n / 2 * 3 + (n%2)<<endl;
    for(i = 1; i<=n; i++)
        if (i%2==0) cout<<i<<" ";


for(i = 1; i<=n; i++)
        if (i%2==1) cout<<i<<" ";
    for(i = 1; i<=n; i++)
        if (i%2==0) cout<<i<<" ";






    return 0;
}
