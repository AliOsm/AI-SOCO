#include <bits/stdc++.h>
 
using namespace std;
int n;bool m[1010];
int main(){
	cin >> n;for(int i = 1, x;i <= n;i++)cin >> x, m[x] = 1;
	for(int i = 1;i <= 998;i++)if(m[i] && m[i+1] && m[i+2])cout << "YES",exit(0);cout << "NO";
}