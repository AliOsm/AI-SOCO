#include <iostream>
using namespace std;

int main() {
    int t; cin >> t;
    while ( t-- ) {
        int n; cin >> n;
        if ( n & 1 ) { cout << 7; n -= 3; }
        cout << string(n / 2, '1') << endl;

    }
    return 0;
}