//#include "stdafx.h"
#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <math.h>

using namespace std;

typedef long long ll;

const int NMax = 200001;
const double eps = 1e-9;
const int MD = (int) 1e8;

#define Min(x,y) ((x) > (y) ? (y) : (x))
#define Max(x,y) ((x) > (y) ? (x) : (y))
#define Sqr(x) ((x) * (x))
#define Abs(x) ((x) < (0) ? (-(x)) : (x))

ll a,b,c,d;

int main()
{
	#ifndef ONLINE_JUDGE
      freopen("input.txt", "r", stdin);
      freopen("output.txt", "w", stdout);
	#endif
	cin >> a >> b >> c >> d;
	printf("%.11f",(a * d) * 1.0 / (b * c + a * d - a * c));

	return 0;	
}
   