#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main ()
{
    ios_base::sync_with_stdio(false);
    ll n; cin >> n;
    if(n == 0) cout << 0 << endl;
    else if(n&1) cout << n/2+1 << endl;
    else cout << n+1 << endl;
    return 0;
}
