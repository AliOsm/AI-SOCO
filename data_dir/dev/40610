#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cin.tie(0);
    long long n, p = 1, t, k = 2, s = 1;
    cin >> n;
    while (k <= n)
    {
        t = k;
        k += p;
        p = t;
        s++;
    }
    cout << --s;
    return 0;
}