#include<bits/stdc++.h>
using namespace std;

int t, x, y;

bool solve() {
	cin >> x >> y;
	if (x == 1)
		return y == 1;
	if (x <= 3)
		return y <= 3;
	return true;
}

int main() {
	cin >> t;
	while (t--)
		cout << (solve()? "YES\n": "NO\n");
}
