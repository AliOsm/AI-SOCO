#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d", &n);
    vector<int> a(n);
    for(int i = 0; i < n; ++i)
    {
        scanf("%d", &a[i]);
    }
    sort(a.begin(), a.end());
    for(int i = 0; i < int(3e8); ++i)
    {
        int j = i;
        j = sqrt(j);
        j *= j;
        j = sqrt(j);
    }
    for(int i = 0; i < n; ++i)
    {
        printf("%d ", a[i]);
    }
    
    return 0;
}