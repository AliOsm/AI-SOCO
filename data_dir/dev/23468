#include <iostream>
using namespace std;

string s, t;

int main() {
	cin >> s;
	cin >> t;
	int m = t.size() - 1;
	for (int i = 0; i < s.size(); i++) {
		if (s[i] != t[m]) {
			cout << "NO";
			return 0;
		}
		m--;
	}
	cout << "YES";
	return 0;
}