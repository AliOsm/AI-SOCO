#include<bits/stdc++.h>
#define fi first
#define se second
#define FOR(a) for(int i=0;i<a;i++)
#define sc(a) scanf("%d",&a)
#define show(a) cout<<a<<endl;
#define show2(a,b) cout<<a<<" "<<b<<endl;
#define show3(a,b,c) cout<<a<<" "<<b<<" "<<c<<endl;
using namespace std;

typedef long long ll;
typedef pair<int, int> P;
typedef pair<P, int> LP;
const ll inf = 1e17 + 10;
const int N = 1e6 + 10;
const ll mod = 1000000007;
const int base=131;

map<string, int>ml;



int b[N], vis[5005][5005], po[N],num[5005], t, n, m, x, y, k,a[N];
ll ex, ey, cnt, ans, sum, flag;
ll hs[N];
ll dp[N];

vector<int> v;
map<int,int> mp;
priority_queue<P> q;
priority_queue<P> tq;
string s;


int main()
{
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );

	cin>>n>>x>>y;
	for(int i=1;i<=n;i++)
	{
		cin>>a[i];
		if(a[i]<=x) cnt++;
	}
	if(x>y) cout<<n<<endl;
	else
	{
		cout<<(cnt+1)/2<<endl;
	}





}
