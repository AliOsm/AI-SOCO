#include <bits/stdc++.h>

using namespace std;

int a, b, c, d;

int main() {
    cin >> a >> b >> c >> d;
    if (a > d) swap(d, a);
    if (b > d) swap(d, b);
    if (c > d) swap(d, c);
    cout << d - c << " " << d - b << " " << d - a;
}