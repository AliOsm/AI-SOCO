#include <bits/stdc++.h>
using namespace std;

int main(){
	int a, b, c;
	cin >> a >> b >> c;
	if (a >= b && a >= c) cout << max(0, a - b - c + 1) << '\n';
	else if (b >= a && b >= c) cout << max(0, b - a - c + 1) << '\n';
	else cout << max(0, c - a - b + 1) << '\n';
	return 0;
}