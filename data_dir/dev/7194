#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  scanf("%d", &n);
  if (n == 1) {
    puts("-1");
    return 0;
  }
  vector<int> v;
  v.push_back(n);
  v.push_back(n + 1);
  v.push_back((n + 1) * n);
  for (auto i : v) printf("%d ", i);
  return 0;
}
