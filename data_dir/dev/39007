#include<bits/stdc++.h>
using namespace std;
int N, P, i;
int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin>>N;
	for (i=0;i<N;i++) {
		cin>>P;
		if (P>=7*(P%3))  {
			cout<<"YES\n";
		}
		else {
			cout<<"NO\n";
		}
	}
}
