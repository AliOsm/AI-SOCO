#include<bits/stdc++.h>
using namespace std;
long long A, B, H;
int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin>>A>>B;
	while (A+B>1) {
		H+=A/B;
		A=A%B;
		swap(A,B);
	}
	cout<<H<<'\n';
}
