#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0), cin.tie(NULL), cout.tie(NULL);
	int a , b , c ;
	cin >> a >> b >> c ;
	int mn = 303;
	for (int i = 1 ; i <= 100 ; ++i)
		mn = min(mn , abs(i - a) + abs(i - b) +  abs(i - c));
	cout << mn ;

}
