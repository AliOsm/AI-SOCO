#include <bits/stdc++.h>
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	int n; cin >> n;
	int result = 0;
	for (int x = 1; x <= (n + 1) / 2; x++)
		if ((n - x) % x == 0)
			result++;
	cout << result << endl;
}