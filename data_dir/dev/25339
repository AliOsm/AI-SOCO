#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(int argc, char **argv) {
	int a, b;
	scanf("%d%d", &a, &b);
	if (abs(a - b) <= 1 && (a != 0 || b != 0))
		puts("YES");
	else
		puts("NO");
	return 0;
}
