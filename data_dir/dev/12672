#include <cmath>
#include <iostream>

using namespace std;

int main() {
	int a, b;
	cin >> a >> b;
	int ka = sqrt(a);
	int kb = sqrt(b);
	if (kb * (kb + 1) > b)
		kb--;
	cout << (ka > kb ? "Valera" : "Vladik") << endl;
	return 0;
}
