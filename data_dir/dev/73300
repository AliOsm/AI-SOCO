#include <iostream>
#include <algorithm>
using namespace std;
int main() {

  int y = 0, b = 0, r = 0;

  cin >> y >> b >> r;

  int m = min(y, b - 1);

  m = min(m, r - 2);

  cout << 3 * m + 3;

  return 0;
}
