#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	int n; cin >> n;
	int lc = 0, rc = 0;
	for (int i = 0; i < n; ++i)
	{
		int x, y; cin >> x >> y;
		if (x > 0) ++rc;
		else ++lc;
	}
	if (lc <= 1 || rc <= 1) cout << "yes\n";
	else cout << "no\n";
}