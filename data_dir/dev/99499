#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <set>
#include <queue>
#include <deque>
#include <map>
#include <stack>
#include<bitset>
#include<list>
#include<cassert>
#include<numeric>

using namespace std;

const int N = 1000;
int main() 
{
	int n;
	int sum1 = 0;
	int sum2 = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		int tmp;
		cin >> tmp;
		sum1 += tmp;
	}
	for (int i = 0; i < n; i++)
	{
		int tmp;
		cin >> tmp;
		sum2 += tmp;
	}
	if (sum2 > sum1)
	{
		cout << "No";
	}
	else
	{
		cout << "Yes";
	}
	



}


