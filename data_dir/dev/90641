#include <bits/stdc++.h>
#define INT long long int
using namespace std;
INT n, k;

int main()
{
    cin>>n>>k;
    while( k )
    {
        if( n%10 ) n--;
        else n/=10;
        k--;
    }
    cout<<n<<endl;
    return 0;
}
