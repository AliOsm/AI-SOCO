#pragma GCC optimize ("O3")
#include <bits/stdc++.h>
#define abdelrahman010 ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
typedef long long ll;
using namespace std;
const int N = 1e5 + 5;
int main() {
    abdelrahman010
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        long double d = (long double)1 / (2 * sin(acos(-1) / (4 * n)));
        cout << fixed << setprecision(9) << d << endl;
    }
    return 0;
}