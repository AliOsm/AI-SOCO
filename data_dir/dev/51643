#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    int n,d,x,ans=0;
    cin>>d>>n;
    for(int i=0;i<n-1;i++){
        cin>>x;
        if(x<d){
            ans+=(d-x);
        }
    }
    cin>>x;
    cout<<ans;
    return 0;
}

