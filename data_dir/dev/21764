#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair <int, int> pii;

const int N = 1e5 + 5;

int main() {
	cout << fixed << setprecision(4);
	int n;
	cin >> n;
	long double ans = 0;
	while(n) {
		ans += (double)1 / (double)n;
		n--;
	}
	cout << ans << "\n";
}
