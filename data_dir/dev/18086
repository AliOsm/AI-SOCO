#include <vector>
#include <map>
#include <set>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <iostream>
#include <iterator>
#include <cassert>
#include <queue>
#include <cmath>
using namespace std;
#define REP(i,n) for(int i=0;i<(n);i++)
typedef long long ll;

int main()
{
	int n,h;
	cin>>n>>h;
	for(int i=1;i<n;i++)
		printf("%.10lf ",h*sqrt(double(i)/n));
	puts("");
	return 0;
}
