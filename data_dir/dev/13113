#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
	int n;
	stringstream s;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		int id = i % 4;
		if (id < 2)
			s << 'a';
		else
			s << 'b';
	}
	cout << s.str() << endl;
	return 0;
}
