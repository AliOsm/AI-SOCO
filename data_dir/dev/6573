#include<bits/stdc++.h>
using namespace std;
int n,ara[110],dp[110] ;
int main()
{
    int n;
    cin >>n;



    while(n--)
    {
        int f=1;
        string s;
        cin >>s;

        sort(s.begin(),s.end());
        for(int i=0; i<s.size()-1; i++)
        {
            if(abs(s[i]-s[i+1])==1)
            {

            }
            else
            {
                f=0;
            }
        }

        if(   f== 1 )
            cout <<"YES"<<endl;
        else
            cout <<"NO"<<endl;
    }


}
