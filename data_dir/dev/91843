#include <iostream>
#include <string>

using namespace std;

string a,b;

int main()
{
	cin >> a >> b;

	if (a != b)
		cout << ((a.size() > b.size()) ? a.size() : b.size());
	else
		cout << -1;


	getchar();getchar();
	return 0;
}