#include<bits/stdc++.h>
using namespace std;
main()
{
	int n,m,i,ans=0;
	cin>>n>>m;int a[n];
	for(i=0;i<n;++i)cin>>a[i];
	sort(a,a+n);
	i=0;
	while(m &&a[i]<0)ans+=abs(a[i]),m--,i++;
	cout<<ans;
}