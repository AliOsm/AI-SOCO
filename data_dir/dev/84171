#include <bits/stdc++.h>

using namespace std;

int n, k, x, a[123456];
main() {
	cin >> n >> k >> x;
	long int ans = 0;
	for(int i = 0; i < n; ++i){
		cin >> a[i];
		if(n - i > k)
			ans += a[i];

	}
	ans = ans + (x * k);
	cout << ans;
}
