#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

ll n;

int main() {
  ios::sync_with_stdio(0);
  cin >> n;
  ll anw = 0;
  while(n > 0) {
    anw++;
    n&=n-1;
  }
  cout << anw << endl;
}