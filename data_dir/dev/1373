/*
0463B. Caisa and Pylons
*/

#include<bits/stdc++.h>
using namespace std;

int main(){
  long n, a=0, b, s=100000, mi = 100000, z=0;
  cin >> n;
  while(n--){
    cin >> b;
    s+=(a-b);
    a = b;
    mi = min(mi, s);
  }
  cout << max(z,100000-mi);
}