#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int x,y,z;
    cin >> x >> y >> z;
    int c=x+y+z;
    cin >> x >> y >> z;
    int m=x+y+z;
    int t;
    cin >> t;
    if(((m+9)/10)+((c+4)/5)<=t)
        cout << "YES";
    else
        cout << "NO";
    return 0;
}
