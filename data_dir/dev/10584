#include <bits/stdc++.h>

using namespace std;

long long cpp(long long x) {
	return x*(x-1)/2;
}

int main() {
	long long m,n;
	cin >> n >> m;
	cout << cpp(n/m+1)*(n%m)+cpp(n/m)*(m-(n%m)) << " " << cpp(n-m+1);
}
