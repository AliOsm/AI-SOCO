#include<iostream>
#include<cstdlib>
#include<vector>
#include<climits>
#include<cctype>
#include<map>
#include<list>
#include<cstdio>
#include<algorithm>
#include<memory.h>
#define L long long int
#define LD long double

using namespace std;

int main()
{
    int t=1,n,a,b;
    while(t--)
    {
              cin>>n>>a>>b;
              int a[n];
              for(int i=0;i<n;i++)
              cin>>a[i];
              sort(a,a+n);
              //int k=a[b-1];
              cout<<a[b]-a[b-1];
              
              
              
    }
    return 0;
}
