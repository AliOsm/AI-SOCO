#include <bits/stdc++.h>

#define ll long long
using namespace std;

long long   n, ans = 0, s = 1, c = 1, x = 2;
int main(){
    cin >> n;
    while(x <= n){
        s = c;
        c = x;
        x = s + c;
        ans++;
    }
    cout << ans;
    return 0;
}
