#include<bits/stdc++.h>

using namespace std;

long long ans,a[1000];
int main()
{
    int n;
  cin>>n;

   for (int i=0;i<n;i++)
    cin>>a[i];

   for (long long  i=0;i<n;i++)
    ans+=i*(a[i]-1)+a[i];

   cout<<ans;
  return 0;
}
//neka sou pocne
