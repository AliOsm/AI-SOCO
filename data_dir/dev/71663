#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,k;
    cin>>n>>k;
    int red=n*2;
    int green=n*5;
    int blue=n*8;
    int ans=red/k+(red%k!=0);
    ans+=green/k+(green%k!=0);
    ans+=blue/k+(blue%k!=0);
    cout<<ans;
}