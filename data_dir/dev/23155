#include <fstream>
#include <stdio.h>
#include <algorithm>

using namespace std;

int main(){
    int n,x;
    scanf("%d",&n);
    for (int i=0;i<n;i++){
        scanf("%d",&x);
        printf("%d\n",max(0,x-2));
    }
    return 0;
}
        
