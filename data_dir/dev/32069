#include <bits/stdc++.h>
using namespace std;

int n, k;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    cout << fixed << setprecision(20);
    cin >> n >> k;

    cout << (long double)(n * pow(1.000000011, k)) << endl;
    return 0;
}
