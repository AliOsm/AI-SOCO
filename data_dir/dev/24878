#include<bits/stdc++.h>
using namespace std;
int main () {
	int a,b,c=0;
 	cin >> a >> b;
 	int arr[a+1];
 	for(int i=1;i<=a;i++)
 		cin >> arr[i];
 		for(int i=1;i<=a;i++)
 		{
 			if(arr[i]+b<=5)
 			{c++;
 			
 			}
 		}
 		cout<<c/3;

	return 0;
}