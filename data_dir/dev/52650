#include <bits/stdc++.h>
using namespace std;

int k, n, s, p;

int main() {
	ios::sync_with_stdio(false), cin.tie(0);
	cin >> k >> n >> s >> p;
	cout << (k * ((n + s - 1) / s) + p - 1) / p;
	return 0;
}
