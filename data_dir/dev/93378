#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <queue>
using namespace std;

int64_t n,k;
int arr[1000001];
int bad[160];
int main() {
	int64_t A, B, a, b, c,X,Y;
	cin >> A >> B >> a >> b >> c;
	X = a * 2 + b;
	Y = b + c * 3;
	cout << max(0ll, X - A) + max(0ll, Y - B);
	return 0;
}