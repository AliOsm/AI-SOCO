#include <bits/stdc++.h>
using namespace std;
int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int t, x, n;
  cin >> t;
  for (int i = 0; i < t; ++i) {
    cin >> n >> x;
    cout << (x << 1) << "\n";
  }
  return 0;
}