#include <bits/stdc++.h>
using namespace std;
int main()
{
	int a[3];
	cin >> a[0] >> a[1] >> a[2];
	for(int i=0;i<3;i++)
	{
		for(int j=i+1;j<3;j++)
		{
			if(a[i]>a[j])	swap(a[i],a[j]);
		}
	}
	cout << (a[1]-a[0])+(a[2]-a[1]) << endl;
	return  0;
}
