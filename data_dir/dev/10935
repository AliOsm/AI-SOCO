#include <bits/stdc++.h>
using namespace std;

int main() {
	int n, a, b, c, ans;
	cin >> a >> b >> c >> n;
	ans = n - a - b + c;
	cout << (ans <= 0 || c > a || c > b ? -1 : ans);
	return 0;
}