//besmellah
#include <bits/stdc++.h>
using namespace std;

int main() {
	long double r, a, b, c, d;
	cin >> r >> a >> b >> c >> d;
	cout << (int)ceil(sqrt((c - a) * (c - a) + (d - b) * (d - b)) / (2 * r));
}
