#include <bits/stdc++.h>
using namespace std;

// g++ -std=c++17 -o out sol.cc && ./out

int32_t main() {
	int n; cin >> n;
	long long cells = 1;
	for (int i = 2; i <= n; i++) {
		long long additional = 4 + (i - 2) * 4;
		cells += additional;
	}
	cout << cells << endl;
}