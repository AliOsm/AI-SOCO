#include <iostream>
using namespace std;

int main(void)
{   
    int n;
    cin >> n;
    long long sum = 0;
    for (int i = 0, t; i < n; ++i)
            cin >> t,
            sum += t;
    cout << n  - (sum % n != 0);
    return 0;
}