#include <bits/stdc++.h>
using namespace std;
int cnt[3005];
int main() {
	int n, i, k;
	scanf("%d", &n);
	for(i = 0; i < n; i++) {
		scanf("%d", &k);
		cnt[k]++;
	}
	i = 1;
	while(cnt[i])
		i++;
	printf("%d\n", i);
	return 0;
}