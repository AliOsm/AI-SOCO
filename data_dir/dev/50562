//besmellah
#include <bits/stdc++.h>
using namespace std;

int main(){
    unsigned int n, k, a, h = 100;
    cin >> n >> k;
    for (int i = 0; i < n; i++){
        cin >> a;
        if (k % a == 0 && k / a < h)
            h = k / a;
    }
    cout << h;
}