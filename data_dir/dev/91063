#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int main() {
    int t; cin >> t;
    while ( t-- ) {
        long long a, b;
        while ( cin >> a >> b ) {
            cout << (a - b == 1 ? "NO" : "YES") << endl;
        }
    }
    return 0;
}