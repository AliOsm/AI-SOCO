//			be name khoda

#include <bits/stdc++.h>

using namespace std;

#define int long long
const int N = 200002;

 main()
{ 
	int n, m, ans;
	cin >> n >> m;
	ans = n;
	while(n >= m)
	{
		ans += n / m;
		n = n % m + n / m;
	}
	cout << ans << endl;
}
