#include <bits/stdc++.h>
using namespace std;

long long T, n, k, ans;

int main(){
    scanf("%d", &T);
    while(T--){
        scanf("%lld %lld", &k, &n);
        ans = 9 * (k - 1) + n;
        printf("%lld\n", ans);
    }
    return 0;
}
