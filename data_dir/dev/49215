#include <iostream>
#include <set>
using namespace std;
#define for(a,n) for(int i = 0;i<n;i++)
int main()
{
    
    int yy,n,m,res;
    cin>>n>>m;
    res = n;
    while(n/m)
    {
        res+=n/m;
        yy = n%m;
        n/=m;
        n+=yy;
    }
    cout<<res<<endl;


}
