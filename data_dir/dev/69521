#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main (int argc, char *argv[])
{
	ll n;
	scanf("%lld", &n);
	ll q = n * (n - 1) * (n - 2) * (n - 3) * (n - 4);
	q /= 120;
	ll qq = q * (n - 5) / 6;
	ll qqq = qq * (n - 6) / 7;
	printf("%lld", q + qq + qqq);
			
	return 0;
}

