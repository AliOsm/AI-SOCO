#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main() {
	long long n;
	cin >> n;
	if (n == 0) {
		puts("0");
		return 0;
	}
	if (n&1) cout << (n+1)/2;
	else cout << n+1 << endl;
}
