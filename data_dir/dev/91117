#include <bits/stdc++.h>
#define INT long long
#define P 998244353
using namespace std;
INT n, m, a, b;
int main()
{
    ios::sync_with_stdio(false);
    cin>>n>>m>>a>>b;
    if( n%m == 0 ) return cout<<0, 0;
    INT t = n%m;
    cout<<min((m-t)*a, t*b);
    return 0;
}

