#include <stdio.h.>

int main()
{
    int a[4],i,sum=0;
    for(i=0; i<4; i++)
    {
        scanf("%d",&a[i]);
    }
    getchar();
    while(1)
    {
        char c=getchar();
        if(c=='\n') break;
        sum+=a[c-'1'];
    }
    printf("%d\n",sum);
    return 0;
}
