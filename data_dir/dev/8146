#include <iostream>
#include <algorithm>
#include <set>
#include <vector>
#include <string>
// DON'T DIVIDE
// Try different approaches
using namespace std;

int l, r;

int main()
{
	ios::sync_with_stdio(0);
	cin >> l >> r;

	if(l == r)
		cout << l << "\n";
	else
		cout << 2 << "\n";



	return 0;
}