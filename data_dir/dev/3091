#include <bits/stdc++.h>
using namespace std;

int main( )
{
    int n, k, i, j;
    char ch;
    cin >> n >> k;
    for (i = 1, j = 0; i <= n; i++, j++)
    {
        ch = 'a' + (j % k);
        cout << ch;
    }
    cout << "\n";
    return 0;
}