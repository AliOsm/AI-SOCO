#include <bits/stdc++.h>

using namespace std;

#define int long long
#define double long double

main()
{
    ios_base::sync_with_stdio(0);
    double n, t;
    cin >> n >> t;
    cout << fixed << setprecision(12) << n * pow(1.000000011, t) << endl;
}
