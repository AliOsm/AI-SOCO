#include "cstdio"
#include "algorithm"
using namespace std;
int n, h;
int main() {
    int a, ans=0;
    scanf("%d%d", &n, &h);
    for (int i=0; i<n; i++) {
        scanf("%d", &a);
        if (a>h)
            ans++;
    }
    printf("%d", ans+n);
    return 0;
}
