#pragma GCC optimize("-O3")
#include <bits/stdc++.h>
#define ll long long
#define f first
#define s second
#define ios ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define y1 ibibibibbibibibiibigbabypanfewewf
using namespace std;
ll const N=3*1e5+2;
ll x1,r,t,x2,c,q,y1,l,y2,d,ans,a,aa[N],aaa[N],b,y3,k,pref[5005][4];
ll m,n;
string s;
pair <ll,ll> p[N];
map <string,ll> mp;
set <ll> st;
int  main()
{
    ios
for (int i=1;i<=3;i++)
{
    cin>>x1;
    t+=x1;
}
for (int i=1;i<=3;i++)
{
    cin>>x1;
    c+=x1;
}
if (t%5==0) t/=5; else t/=5,t++;
if (c%10==0) c/=10; else c/=10,c++;
cin>>n;
if (n>=c+t) cout<<"YES"; else cout<<"NO";
}