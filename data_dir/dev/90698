#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int a,b,c;
	cin>>a>>b>>c;
	if((a>b &&b>c) || (c>b && b>a))
	cout<<abs(a-c);
	if((a>c && c>b)||(b>c && c>a))
	cout<<abs(a-b);
	if((c>a && a>b)||(b>a && a>c))
	cout<<abs(c-b);
}
