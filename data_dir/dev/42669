#include<bits/stdc++.h>
#define int long long

using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n;
    string h;
    cin >> n >> h;

    int a = 0, b = 0;
    string s;
    while (n--)
    {
        cin >> s;
        if (s.substr(0, 3) == h)
            a++;
        else
            b++;
    }
    if (a != b)
        cout << "contest\n";
    else
        cout << "home\n";

    return 0;
}
