#include <bits/stdc++.h>

using namespace std;

int t;
long long n, m;

int main() {
	scanf("%d", &t);
	while (t--) {
		scanf("%lld", &n);
		m = n;
		n %= 14;
		puts(m >= 14 && n >= 1 && n <= 6 ? "YES" : "NO");
	}
}
