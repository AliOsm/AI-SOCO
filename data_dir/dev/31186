#include <bits/stdc++.h>
using namespace std;

int main() {
    long long a[3];
    for(int i = 0; i < 3; i++)
        cin >> a[i];
    sort(a, a + 3);
    long long ans = (a[0] + a[1] + a[2])/3;
    ans = min(ans, a[0] + a[1]);
    cout << ans << endl;
    return 0;
}