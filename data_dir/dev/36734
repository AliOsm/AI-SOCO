#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,a,b;
    cin >> n >> a >> b;
    char *s = new char [n];
    cin >> s;
    cout << abs(s[a-1]-s[b-1]) << endl;
    return 0;
}