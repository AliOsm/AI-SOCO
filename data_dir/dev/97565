#include <bits/stdc++.h>

using namespace std;

string s, k;

int main () {
	cin >> s >> k;

	for(int i = 0; i < s.size(); i++) {
		s[i] = tolower(s[i]);
		k[i] = tolower(k[i]);
	}
	if(s < k) cout << -1;
	if(s > k) cout << 1;
	if(s == k) cout << 0;

	return 0;
}