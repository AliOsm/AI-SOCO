# include <bits/stdc++.h>
using namespace std;
# define fi cin
# define fo cout
int d[1005];
int main(void)
{
     int n,k,l,c,d,p,nl,np;
     fi>>n>>k>>l>>c>>d>>p>>nl>>np;
     k *= l;
     c *= d;
     return fo << min(min(k / nl,p / np),c) / n << '\n',0;
}
