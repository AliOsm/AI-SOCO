#include <bits/stdc++.h>
using namespace std;

int N, T, S, X;

int main() {
    scanf("%d %d", &N, &T);

    for(int i=0;i<N;i++) {
        scanf("%d", &X);
        S += X;
    }

    S += N - 1;

    printf("%s\n", S == T ? "YES" : "NO");

    return 0;
}
