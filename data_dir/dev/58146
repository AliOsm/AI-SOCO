#include <iostream>
#include <map>
#include <set>
#include <string>
#include <unordered_map>
using namespace std;

int n;
int sum = 0;


int main() {
	ios_base::sync_with_stdio(0);
	cin >> n;
	for (int i = 0; i < n; i++) {
		int x;
		cin >> x;
		sum += abs(x);
	}
	cout << sum;
}