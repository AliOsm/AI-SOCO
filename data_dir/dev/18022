#include<iostream>

using namespace std;

int main() {

	int n, solved = 0;
	cin >> n;
	for (int i = 0; i < n; ++i) {
		int x, y, z;
		cin >> x >> y >> z;
		int sum = x + y + z;
		if (sum >= 2)
			solved++;
	}
	cout << solved;
	return 0;
}
