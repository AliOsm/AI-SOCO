//In the name of God

#include <iostream>
#include <vector>
using namespace std;

int main()
{
  string s;
  cin>>s;
  char c=s[0];
  for(int i=0;i<s.length();i++)
    c=max(c,s[i]);
  string ans;
  for(int i=0;i<s.length();i++)
    if(s[i]==c)
      ans+=c;
  cout<<ans<<endl;
  return 0;
}
