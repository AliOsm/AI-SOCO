#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main () {
	int n, m;
	cin >> n >> m;
	double ans = 1e9;
	while (n--) {
		int a,b;
		cin >> a >> b;
		ans = min(1.0*a*m/b,ans);
	}
	printf("%0.7f\n",ans);
}
