#include <iostream>

using namespace std;

int main()
{
    long long n , x , y , sum = 0 , sum1 = 0 , p ;
    cin>>n>>x>>y ;

    if(n > y)
    {
        cout<<"-1";
        return 0 ;
    }
    for(long long i = 1 ; i <= n ; ++i)
    {
        if((y - (n - 1)) * (y - (n - 1)) + ((n-1) * 1) < x)
        {
            cout<<"-1";
            return 0 ;
        }
        if(i == 1)
            cout<<y-(n-1)<<endl;
        else
            cout<<1<<endl;
    }
    return 0 ;
}
