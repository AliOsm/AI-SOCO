// in the name of Allah
// and begin ... 

#include <bits/stdc++.h>

#define endl '\n'

using namespace std;

int main() {
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	long long l, r, x, y, k;
	cin >> l >> r >> x >> y >> k;
	for(long long i = x; i <= y; i++) {
		long long foo = i * k;
		if(l <= foo && foo <= r) {
			cout << "YES";
			return 0;
		}
	}
	cout << "NO";
	return 0;
}