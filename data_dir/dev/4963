// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    int a, b;
    cin >> a >> b;
    a = sqrt(a) + 1e-8;
    cout << (a*(a+1) <= b ? "Vladik" : "Valera");
    return 0;
}
