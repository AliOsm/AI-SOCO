// Author: πα

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

int main()
{
    int n, x, p, cur = INT_MAX, ans = 0;
    cin >> n;
    while(n--)
        cin >> x >> p, cur = min(cur, p), ans += x*cur;
    cout << ans;
    return 0;
}
