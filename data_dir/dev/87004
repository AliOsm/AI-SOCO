//prince0fcrows
#include <bits/stdc++.h>
#define int long long
#define f1 first
#define s2 second
#define MX 100005
#define mod 1000000007

using namespace std;
int ara[MX];
vector < int > xy;
vector < int > y;
vector < int > x;
string str, str1, str2;


int32_t main(){
    //freopen("in.txt", "r", stdin);
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int k, i, j, p, c, l, r, a, b, h, n, m, ans;
    while(cin >> k){
        if(k>36){
            cout << -1 << endl;
            continue;
        }
        while(k){
            if(k==1){
                cout << 6;
                break;
            }
            cout << 8;
            k-=2;
        }
        cout << endl;
    }
    return 0;
}







