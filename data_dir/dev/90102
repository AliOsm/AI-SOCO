#include <bits/stdc++.h>
using namespace std;


int main()
{
	int k;
	scanf("%d",&k);
	if(k > 36)
	{
		puts("-1");
		return 0;
	}
	if(k&1)
	{
		printf("4");
		k--;
	}
	while(k)
	{
		printf("8");
		k-=2;
	}
}
