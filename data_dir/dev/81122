#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  scanf("%d", &n);
  int s = 0, mx = 0;
  for (int i = 0; i < n; ++i) {
    int foo;
    scanf("%d", &foo);
    s += foo;
    mx = max(mx, foo);
  }
  printf("%d", mx * n - s);
}