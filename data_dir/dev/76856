#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long k,n,m;
    cin >> k >> n >> m;
    long long h = ceil(double(n)/k);
    long long g = floor(double(m)/k);
    cout << (g-h) + 1;
}