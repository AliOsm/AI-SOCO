#include<bits/stdc++.h>
using namespace std;
int a[110];

int main()
{
  int n;
  cin>>n;
  
  for(int i=0;i<n;i++)
  cin>>a[i];
  
  sort(a,a+n);
  
  long long ans=0;
  
  for(int i=0;i<n;i++)
  {
     ans+=(a[n-1]-a[i]);
  }
  
  cout<<ans;
  
  
  


}