#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,a,ans=0;
    map<int,int> m;
    cin >> n;
    while(n--){
        cin >> a;
        m[a]++;
        ans = max(ans,m[a]);
    }
    cout << ans << "\n";
    return 0;
}