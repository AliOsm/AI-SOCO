#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    int n,x,C[3]={0};
    cin>>n;
    while(n--)cin>>x,C[x]++;

    cout<<min(C[1],C[2]) + (C[1]-min(C[1],C[2]))/3<<"\n";
}
