/* In the name of Allah */
// It's too hard to make a lion angry,
// He really doesn't care what others do around him,
// But if you do hurt his pride,
// He won't let you live another day...
#include<bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin >> t;
	while (t--) {
		string s;
		cin >> s;
		int n = s.size();
		if (s.substr(n - 2) == "po")
			cout << "FILIPINO\n";
		else if (n > 4 && s.substr(n - 5) == "mnida")
			cout << "KOREAN\n";
		else
			cout << "JAPANESE\n";
	}
}
