#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

#define REP(i,n) for(int i = 0; i < n; i++)

int main()
{
    int n;
    cin >> n;
    cout << max(n, max(n/10, n/100*10+n%10));
    return 0;
}
