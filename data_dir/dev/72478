#include <bits/stdc++.h>
using namespace std;

int n, m;

int main() {
	scanf("%d%d", &n, &m);

	if(n == 1 && m == 1) {
		printf("1\n");
		return 0;
	}

	double ans = 1.0 * ((n - 1) * (m - 1)) / (n * m - 1) + 1.0;
	ans /= n;

	printf("%.10lf\n", ans);
	return 0;
}