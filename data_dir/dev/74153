#include <bits/stdc++.h>
using namespace std;
const int N = 1000000;
int a[N];
int main() {
	string s;
	cin >> s;
	for (int i = 0; i < 10; i++) {
		string t;
		cin >> t;
		for (int j = 0; j < 8; j++) 
			if (s.substr(j * 10, 10) == t)
				a[j] = i; 	
	}
	for (int i = 0; i < 8; i++) {
		cout << a[i];
	}
	return 0;
}
