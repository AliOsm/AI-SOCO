#include <bits/stdc++.h>

using namespace std;

#define sz(a) int(a.size())

int main() {
	long long n, k;
	cin >> n >> k;

	long long m = (k-1)/2;
	cout << max(0LL,min(m, n-(m + (k&1 ? 0 : 1)))) << endl;
}