#include <bits/stdc++.h>

using namespace std;

int main()
{
  long long n, m, a, b, c;
  cin >> n >> m >> a >> b >> c;
  cout << max(a * 2 + b - n, (long long)0) + max(b + 3 * c - m, (long long)0);
}
