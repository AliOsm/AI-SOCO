#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n,l,r;
	cin>>n;
	for (int i=1; i<=n; i++)
	{
		cin>>l>>r;
		cout<<l<<" "<<2*l<<endl;
	}
	ios::sync_with_stdio(0); 
	cin.tie(0);
	
	return 0;
}