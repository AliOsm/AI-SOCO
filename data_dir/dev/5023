#include <bits/stdc++.h>
using namespace std;
const int INF=1000228;
int main()
{
   int n, m;
   cin>>n>>m;
   cout<<min(n, m)+1<<"\n";
   for (int i=0; i<=min(n, m);i++)
   {
   	cout<<min(n, m)-i<<" "<<i<<"\n";
   }
   return 0;
}