#include <iostream>

using namespace std;

int factorial(int x)
{
    if(x == 1) return 1;
    return x * factorial(x - 1);
}
int main()
{
    int a , b;
    cin >> a >> b;
    if(b < a) swap(a , b);
    cout << factorial(a);
    return 0;
}
