#include <bits/stdc++.h>

using namespace std;

int n, x[1005], v, m, y, s;

int main()
{
    cin >> n >> m;
    for (int i=1;i<=n;i++)
    {
        cin >> x[i];
    }
    for (int i=1;i<=m;i++)
    {
        cin >> v >> y;
        s+=min(x[v], x[y]);
    }
    cout << s;
}
