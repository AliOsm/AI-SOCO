#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int a[N];
int main(){
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    int n , k;
    cin >> n >> k;
    int off  = (n / k);
    ++off;
    cout << (off * k) << endl;
    return 0;
}
