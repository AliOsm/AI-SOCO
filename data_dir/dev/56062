#include<bits/stdc++.h>

using namespace std;

int n, x, mx;

int main()
{
    cin >> n;
    
    for(int i = 0; i < n; i++)
    {
        cin >> x;
        mx = max(mx, x);
    }
    
    cout << max(0, mx - 25);
    return 0;
}