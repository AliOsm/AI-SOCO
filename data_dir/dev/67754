/* In the name of Allah */
#include<bits/stdc++.h>
using namespace std;

int main() {
	int n, k, a[55];
	cin >> n >> k;
	for (int i = 0; i < n; i++)
		cin >> a[i];
	sort(a, a + n);
	k > n? cout << -1: cout << a[n - k] << ' ' << a[n - k];
}
