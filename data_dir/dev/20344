#include <bits/stdc++.h>
using namespace std;
int n;

int main() {
    int S = 0;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        int x; cin >> x;
        S = (S + x - 1) % 2;
        if (S & 1) cout << 1 << endl;
        else cout << 2 << endl;
    }
}
