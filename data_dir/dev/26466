#include<bits/stdc++.h>

using namespace std;

typedef long long LL;
typedef unsigned long long uLL;
typedef long double ldb;
typedef pair<int,int> pii;

int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);
    int n;
    cin>>n;
    while(n%10==0)
        n /= 10;
    stringstream ss;
    ss<<n;
    string s = ss.str();
    reverse(s.begin(), s.end());
    cout<<(s==ss.str() ? "YES" : "NO")<<endl;
    return 0;
}
