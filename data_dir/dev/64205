#include<bits/stdc++.h>
#define ll long long
using namespace std;
ll n, m = 1, k;
int main()
{
    ios_base::sync_with_stdio( 0 );
    cin.tie( 0 );
    cout.tie( 0 );
    cin >> n >> k;
    while( k -- )m *= 10;
    cout << ( n * m ) / __gcd( n,  m );
}
