#include <bits/stdc++.h>
using namespace std;

int main() 
{
	int n,x,sum=0,t;
	scanf("%d%d",&n,&t);
	for(int i=0 ; i < n ; i++)
	{
		scanf("%d",&x);
		sum += 86400-x;
		if(sum >= t)
		{
			printf("%d",i+1);
			return 0;
		}
			
	}
}