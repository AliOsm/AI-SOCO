#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cout.tie(NULL);
	cin.tie(NULL);
	long long t;
	cin >> t;
	while(t--){
		long long n,x,a,b;
		cin >> n >> x >> a >> b;
		cout << min(abs(a-b)+x,n-1) << endl;
	}
}