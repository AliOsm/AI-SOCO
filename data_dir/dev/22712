//#pragma GCC optimize("-O3")
#include <bits/stdc++.h>
#define ll long long
#define f first
#define s second
#define pb push_back
#define ios ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define y1 ibibibibbibibibiibigbabypanfewewf
using namespace std;
ll const N=5*1e5+5;
ll x1,r,x2,c,q,y,l,e,y2,d,ans,a[N],aaa[N],b,y3,k,pref[5005][4];
ll m,n;
bool us[N];
char z,x;
vector <ll> v;
set <ll> st;
string t,s,ss;
pair <ll,ll> p[N];
int  main()
{
    ios
    cin>>n;
    for (int i=1;i<=n;i++)
    {
        cin>>a[i];
        while(a[i]%2==0)
        {
            a[i]/=2;
        }
        while(a[i]%3==0)
        {
            a[i]/=3;
        }
    }
    for (int i=2;i<=n;i++)
    {
        if (a[i]!=a[1]) return cout<<"No",0;
    }
    cout<<"Yes";
    
}