#include <iostream>
#include <string>

using namespace std;

typedef long long int ll;

int n;
string s;
ll sol;

int main()
{
  ios::sync_with_stdio(false);
  cin>>n>>s;
  for (int i=0;i<n;i++) {
    int d=s[i]-'0';
    if (d%2==0)
      sol+=(i+1);
  }
  cout<<sol<<endl;
}
