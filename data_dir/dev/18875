#include <bits/stdc++.h>

using namespace std;

#define ll      long long int
#define srt(v)  sort(v.begin(),v.end())

int main()
{
    int n;
    cin >> n;
    map < int , int > mp;
    bool ans=true;
    int sum1=0,sum=0;
    for (int i=0;i<n;i++){
        int a;
        cin >> a ;
        sum+=a;
    }
    for (int i=0;i<n;i++){
        int a;
        cin >> a;
        sum1+=a;
    }
    cout << (sum>=sum1?"Yes":"No")<<endl;

    return 0;
}
