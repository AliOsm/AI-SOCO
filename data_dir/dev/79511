#include <bits/stdc++.h>
using namespace std;

int main() {
    int ans = 0;
    int n ;

    cin >> n ;
    while(n) {
        if(n%2) ans++;
        n /= 2;
    }

    cout << ans << endl;
}
