#include <bits/stdc++.h>
#define LL long long
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cout<<"YES"<<endl;
    LL l, r;
    cin>>l>>r;

    for (LL i=l; i<r; i+=2)
        cout<<i<<" "<<i+1<<"\n";
}
